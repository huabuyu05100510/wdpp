# 深审 v2:纯图架构重定位(静态图,DESIGN-STATIC 思路)

> 提议形态:把当前 **运行时按值追踪** 重定位为 **build-time 静态图**(类似 `DESIGN-STATIC.md` 的抽象解释 taint 传播)。
> 状态:`DESIGN-STATIC.md` 已存在完整方案(12 章,15 个 case,5 期落地)。本文件评议**该提议相对于现行运行时实现的可行性、得失、协同方式**。
> 评议基准:`DESIGN.md`(运行时 canonical)、`DESIGN-STATIC.md`(静态)、`impl/l0/src/*` 现行实现。

---

## 0. 结论速览

| 维度 | 判定 | 说明 |
|---|---|---|
| **目标对齐** | ✅ 命中原始目标 | "DOM -> API 涉及哪些",静态图满足(组件级/列表项 API 级) |
| **第一性原理** | ✅ 全对齐 | §1.1-1.5 五条全合规;§14 否决的是 Jalangi(运行时全插桩),不是静态 |
| **可行性** | ✅ 可行 | ts-morph + 16 条抽象解释规则 + origin 适配器;落地路径已规划 |
| **相对现行的得失** | 见 §2 | **失去**:记录级 / cell did / 运行时精度;**获得**:零运行时开销、库内天然免疫、零爆炸半径 |
| **与现行协同** | ✅ 互补 | 静态图答 could,运行时答 did;粒度分工,不互斥 |
| **核心风险** | ⚠️ 三类 | 工程复杂度(ts-morph 大项目慢)、可追深度(opaque 库内部不透)、fiber-only 查询 |
| **推荐** | ✅ 主推,且建议**双轨并行** | 静态为主(零开销、推广所有团队),运行时为辅(记录级 / cell did 按需) |

---

## 1. 提议形态精确定义

**重定位前后对比**:

| | 现行(运行时按值追踪) | 提议(静态图) |
|---|---|---|
| 抽象入口 | 值(原始值) | 表达式(AST 节点) |
| 抽象传播 | `Map<值, passport>` | `Src(e) = ∪ Src(args)` 抽象解释规则 |
| 存储 | `Map<值, passport>` + `WeakMap<obj, 子树并集>` + `byVal` | 静态图(JSON):节点 + 边 |
| 运行时介入 | fetch 拦截 + DOM 写入拦截 + Babel 插桩 | **零**(可选 fiber 只读查) |
| 答案 | did(运行时确认实际流了什么) | could(组件级 ≈ does) |
| 粒度 | 字段级 + 记录级 + cell 级 | 组件 + JSX 子树块 + 叶字段 + 列表项 API |

**关键定性**:**抽象入口从"值"换成"表达式"**,运行时归因消失,所有归因来自 build-time 抽象解释。**这是抽象本质的升级**,不是优化。

---

## 2. 命中现行痛点(逐项验证)

### 2.1 byVal 碰撞 ✅ 根治

`stamp-origin.js:240-253` 的 `__wdpp_byVal` 是按值反查兜底,根因是两个不同 API 返回相同 value 时不可区分。

**静态图回答 byVal 的方式**:不走 byVal。值不参与归因。归因由 `Src(e) = ApiSource(origin 规则 3)` 沿 §3.1 抽象解释规则传播。**根因消失**——值不再是索引,只是 AST 表达式的求值结果。

### 2.2 深拷贝断身份 ✅ 根治

`stamp-origin.js` + `babel-runtime.js` 的 `__passthrough` 用白名单应对深拷贝,自定义深拷贝断。

**静态图回答**:深拷贝是 `CallExpression(cloneDeep, [arg])`,走 §3.1 规则 5(库调用 opaque 分支):`Src(result) = ∪ Src(args)`。**不区分类别**(cloneDeep / merge / format / custom),不靠运行时白名单。**根因消失**——不依赖深拷贝的运行时行为,只依赖调用点的语法形态。

### 2.3 低熵值失效 ✅ 根治

`value-index.js:30-36` 的 `LOW_ENTROPY` 黑名单是值信息论上限。

**静态图回答**:`status = 0` 是 `<td>{user.status}</td>` 的 `Src` = `/users.status`(规则 4 字段访问 refine jsonPath)。**值的数值不参与归因**。**根因消失**。

### 2.4 库内变换墙 ✅ 根治

现行 antd `valueEnum`、dayjs `.format('YYYY-MM-DD')`、库内 `map` 等靠块级兜底,过近似。

**静态图回答**(DESIGN-STATIC.md §3.4 + Case 9):
```jsx
<Table dataSource={users} rowKey="id"/>
```
- `<Table>` 收 `users` prop(规则 12)
- Table 块 -> `users` 的 src = `/users`
- **库内部不细分**(规则 5 opaque 分支,union 不解析)
- Table 块整体 -> `/users` 🟢

**关键洞察**:静态图天然按"组件 prop 源"归因,**不需要运行时方案的结构适配器 / 档 3 库插桩**。**根因消失**——库内不可达,但 prop 源可达,且粒度 = 块(组件)级,**结构等价**。

### 2.5 痛点命中率总结

| 痛点 | 运行时方案应对 | 静态图应对 | 静态优势 |
|---|---|---|---|
| byVal 碰撞 | 块级降级 | 不存在 byVal | **根治** |
| 深拷贝断身份 | `__passthrough` 白名单 | 规则 5 union 不靠白名单 | **根治** |
| 低熵失效 | 块级字段位 | 规则 4 字段 refine | **根治** |
| 库内变换墙 | 结构适配器 / 档 3 | prop 源追踪 | **天然免疫** |

**结论**:静态图**真的命中所有四类根因**,不是表面优化。

---

## 3. 第一性原理审查(逐条对齐)

### §1.1 两端可观测,中间不可观测

- 静态图:build-time 不观测运行时,但**抽象解释穿越了"中间"**(§3.1 规则覆盖所有 JS 构造)——这是从语言语义推断,不是观测。
- **关键区别**:静态图的"穿越中间"是**逻辑上的**(语法 + 规则),不是运行时观测。**符合** §1.1 精神(承认中间不可观测,但有方法近似)。✅

### §1.2 追踪 identity 不追踪 value

- 静态图:**完全不追踪 value**。`Src(e)` 是抽象源集,**与值无关**。
- ✅ **完美对齐,且超越**。

### §1.3 拦截 sink 不观察 DOM

- 静态图:不观察 DOM,**也不拦截**——查询时只读 fiber(可选)找组件/JSX 位置,**只读不写**。
- ✅ **超越原原则**(更彻底)。

### §1.4 按数据流 opt-in 插桩,非全局改写

- 静态图:**零运行时插桩**。**完全合规**。
- ✅ **完美对齐,且超越**(不需要 opt-in,天然零侵入)。

### §1.5 宁可"未知"不可"错误"

- 静态图:**🟢 / 🟡 / ⚪ 三档置信度分级**(DESIGN-STATIC.md §7)。规则 5/9/10 统一 union 保守并集,不解析进 opaque。**sound 过近似,可能多报不漏**。
- ✅ **对齐**(§3.7 soundness 声明已诚实分级)。

### 5 条全对齐,且 §1.2/§1.4 超越运行时方案。

---

## 4. 三类核心风险(诚实)

### 4.1 工程复杂度:ts-morph 大项目分析慢

**风险**:
- ts-morph 项目图加载 + AST visitor 对每文件每表达式算 `Src(e)`——大项目(>1k 文件)首次分析可能 30s+。
- dev 模式每次启动重算。HMR 增量重算复杂。
- 内存占用:ts-morph 程序模型(类型信息 + AST)百 MB 级。

**缓解**:
- 增量分析:只分析改动的文件 + 受影响的下游
- 缓存 `Src(e)`:按文件 hash 缓存,改文件重算
- 持久化图:build 期 emit JSON,dev 启动只加载图,不重算
- 服务端分析 + 客户端加载图(类似 source map 模型)

**评估**:工程量大,但**不是不可行**。类似 Babel/SWC 项目级分析已有先例。

### 4.2 可追深度:opaque 库内部不透

**风险**:
- §3.6 有界扫描补浅层 origin 后 sound-ish,但深层 origin 仍可能漏
- 动态 selector / 动态 fetcher / 跨文件间接调用 -> imprecision
- 自定义 hook 内部 fetch 但 hook 本身不可追(如 hook 返回函数,函数内 fetch)

**缓解**:
- 用户可标注 origin(类似 `/* @origin */` pragma,但提议默认零侵入,可选 opt-in)
- 框架级 origin 适配器(§3.3)扩展 React Query / Redux / Zustand
- 有界扫描深度可配(精度 vs 性能旋钮)

**评估**:**信息论上限**(DESIGN-STATIC.md §3.7 已诚实声明)——深 opaque 不可追,但浅层不漏。**对"找到主要 API 依赖"足够**。

### 4.3 查询依赖 fiber(React-only 实时点查)

**风险**:
- 点 live DOM 查 API 依赖,需要 fiber 反查组件 + JSX 位置
- 非 React(Vue/Angular/vanilla)无 fiber,需各自 devtools 适配器
- 静态图对 React DOM 实时点查友好,对其他框架要适配

**缓解**:
- vanilla:静态浏览(按组件/代码导航,点 DOM 不查)
- Vue/Angular:各自 devtools 读 fiber-like 内部
- fiber 读只读不写,**风险隔离**(DESIGN-STATIC.md §6)

**评估**:**与 DESIGN.md §7.6 框架 sink 适配器同构**,工程量相似。

---

## 5. 静态图 vs 运行时按值——粒度对比

这是关键决策点。**两种方案不互斥,但粒度不同**:

| 粒度 | 静态图 | 运行时按值 |
|---|---|---|
| 组件级 / 列表项 API | ✅(100% 精确,无歧义) | ✅(运行时也答得到) |
| JSX 子树块级 | ✅(规则 11/12 区分) | ⚠️(块级 props 整对象带照,过近似) |
| 叶字段级 | ✅(规则 4/11 refine jsonPath) | ✅(`__readProp` 字段读取 + SM 槽位) |
| 记录级(哪条 item) | ❌(§3.4 实例身份,语言层面出界) | ✅(运行时有 item instance) |
| cell 级 did | ❌(需要运行时确认实际流了什么) | ✅(`textContent = X` 实际写入时确认) |
| 控制边(运行时条件) | ⚠️(静态只能标"可能条件") | ✅(运行时 if/while/for 栈准确) |
| 库内部细分 | ❌(库块整体 = props 的 API) | ⚠️(结构适配器按 key 对应,但精度受限) |
| 跨请求 diff / replay | ❌(静态产物,不捕获运行时态) | ✅(seq 写入记录边,可回放) |

**洞察**:**静态图答 could,运行时答 did**。两者粒度正交,不互斥。

---

## 6. 推荐架构:双轨并行(静态为主,运行为辅)

### 6.1 推荐分工

```
┌─────────────────────────────────────────────────────┐
│ Build-time(零运行时归因):                           │
│   DESIGN-STATIC.md 静态图                           │
│   - 组件级 / 列表项 API 级 / JSX 子树块级 / 叶字段级  │
│   - could(组件级 ≈ does)                           │
│   - 推广所有团队、零摩擦、零爆炸半径                │
└─────────────────────────────────────────────────────┘
                          + (按需)
┌─────────────────────────────────────────────────────┐
│ Runtime(按需, dev-only):                            │
│   DESIGN.md 运行时按值追踪                          │
│   - 记录级 / cell did / 运行时控制边 / replay        │
│   - did(运行时确认)                                │
│   - 特定深度调查、性能调优、AI agent 消费            │
└─────────────────────────────────────────────────────┘
```

### 6.2 静态图为基线

- 推广所有团队零摩擦(零运行时、零侵入)
- DevTools 默认挂静态图(组件 -> API、API -> 组件)
- 满足"原始目标:DOM -> API 涉及哪些"(组件级 / 列表项 API 级)

### 6.3 运行为可选增强

- 需要"运行时 did"场景:记录级确认、控制流分叉实际选择、跨请求 diff
- 需要 cell 级 did:精调查,排查 bug
- AI agent 消费:agent 需要"实际数据流是什么"不是"可能是什么"

### 6.4 协同点

- 静态图提供"块 -> API 集"候选
- 运行时按值提供"块 -> 实际流"的 did
- DevTools 优先显示静态图答案,运行时作为精查入口

---

## 7. 落地路径(继承 DESIGN-STATIC.md §11,微调)

### P1 核心 + origin 适配器 + 单文件抽象解释
- ts-morph 基础
- fetch / XHR / axios origin 识别
- §3.1 规则子集:字面量 / 标识符 / API origin / 成员访问 / 二元运算 / JSX child
- 跑通 Case 1(直 hook + 渲染)

### P2 过程间 + 跨文件 + 组件树 + props
- 规则 5 可分析分支、规则 12
- 跑通 Case 2/13/14(prop 钻取、跨文件、hook 组合)

### P3 完整规则 + 保守并集
- 规则 5 opaque / 9 spread / 10 控制流 union
- 跑通 Case 5/6/7/11(opaque / 动态分派 / spread props / 条件渲染)

### P4 状态库 origin 适配器
- Redux selector / React Query fetcher / Zustand store
- 跑通 Case 10/15

### P5 DevTool UI + React fiber 点查
- 图浏览(组件 -> API、API -> 组件)
- React 下点 live DOM -> fiber 只读 -> 静态图查询
- 跑通 Case 9(库 + 点查)

### P6 与运行时按值追踪协同
- 静态图 JSON + 运行时 graph 双向引用
- DevTools UI 集成:静态为基线,运行为精查

---

## 8. 与 §14 否决方案的区分(关键)

`DESIGN.md §14` 否决的方案:

> 全量 Jalangi2 taint → 完备但操作级全插桩,开销/维护不可承受;minified 代码脆弱。改为定向 transform(仅三类运算)。

**否决的是 Jalangi**(运行时操作级 taint)。**静态图与 Jalangi 完全不同**:

| 维度 | Jalangi(运行时 taint) | 静态图(抽象解释) |
|---|---|---|
| 时机 | 运行时 | build-time |
| 抽象 | 操作节点 | 表达式 |
| 性能 | 运行时 2-10x 慢 | 零运行时 |
| 库 | 必须插桩 | 天然不插桩 |
| minified | 脆弱 | 不接触 |
| 答案 | did | could |

**DESIGN-STATIC.md 不在 §14 否决范围**。**关键区别 = 时机**。

---

## 9. 关键反对意见的回应

### 9.1 "静态图漏 cell did,不够用"

**回应**:
- "原始目标 = DOM -> API 涉及哪些"。**组件级 / 列表项 API 级覆盖 90% 场景**。
- cell did 是"运行时确认",静态图答不到,但**这正是双轨架构的运行为辅部分**。
- "够不够用"取决于场景:**调试 UI 依赖**够用,**精查数据流**需要运行时。**两者并存**。

### 9.2 "静态图基于源码,build 后变 minified/prod 失效"

**回应**:
- 静态图是 build 产物(JSON),**和 source map 一样独立**。prod 也可带(dev 优先)。
- 运行时按值追踪在 prod 更不可用(minified fiber internals 不可读)。
- 静态图 prod 兼容 = source map 已有先例。

### 9.3 "ts-morph 大项目慢"

**回应**:
- 增量分析 + 持久化图(build 期 emit,dev 启动只加载)。
- 与现有 dev tool 链(eslint / tsc / babel)同样的成本结构。
- 服务端分析 + 客户端加载(类似 source map / sentry 模型)。

### 9.4 "我的项目不是 React / TypeScript"

**回应**:
- ts-morph 需要 TS 信息,但**纯 JS 也可**(babel parser + scope 分析,精度降级)。
- 非 React 框架各自 devtools 适配器读 fiber-like 内部。
- vanilla / Vue / Angular / Svelte 都有类似方案空间。

### 9.5 "现有 antd/rwa 测试床怎么办"

**回应**:
- 测试床不需改业务代码(静态图本来就是零侵入)。
- 测试床验证从"运行时精度 100% / 88%"变为"静态图覆盖率 + 精度"。
- 新测试床可加"静态图 + 运行时"双指标对比。

---

## 10. 提议的最终判定

### 10.1 是否可行?

✅ **可行**。DESIGN-STATIC.md 已是完整方案(12 章 + 15 case + 5 期落地)。工程量中等(ts-morph + 抽象解释规则),信息论上限在 §3.7 已诚实声明。

### 10.2 是否值得做?

✅ **值得,且建议作为新主方案**。理由:
- 命题正确(四类根因真的命中)
- 第一性原理全对齐
- 零运行时 = 推广所有团队零摩擦(对比运行时按值的爆炸半径问题)
- 库内天然免疫(对比运行时方案的结构适配器 / 档 3 库插桩)
- 双轨并行:静态为基线,运行为可选增强

### 10.3 与现有运行时按值的关系?

✅ **不互斥,粒度分工**。运行时按值不是废,是降级到"特定场景的深度调查"。**现有 104 conformance 测试 + antd/rwa 测试床都是运行时按值的资产,继续保留**。

### 10.4 风险?

⚠️ 三类(§4):工程复杂度、可追深度、React-only 实时点查。**均可缓解**,不致命。

---

## 11. 给提议者的行动建议

### 11.1 如果要立项"纯图架构重定位"

1. **明确"重定位"语义**:
   - 重定位 = 静态图为新主方案?——**推荐**
   - 重定位 = 替换运行时按值?——**不推荐**(运行时按值仍有"did"价值)
   - 重定位 = 双轨并行?——**最推荐**(§6)

2. **不要从零开始**:`DESIGN-STATIC.md` 已是完整文档,**直接落地 P1**。

3. **保留运行时按值资产**:104 conformance + antd/rwa 测试床 + `impl/l0/src/*` 实现,作为运行为辅模块继续维护。

4. **新增静态图模块**:`impl/static-graph/`(ts-morph + 抽象解释 + 图 emit JSON + DevTools 集成)。

5. **双轨测试床对比**:antd/rwa 同时跑静态图(覆盖率/精度)和运行时(精度/召回),量化双轨价值。

### 11.2 如果只是讨论提议,不动手

回答 §10 三个判定问题即可,不必深入工程。

---

## 12. 评议输出

- **提议方向感正确,实现形态正确**(静态图,不是运行时 taint graph)
- **可行性 ✅**——`DESIGN-STATIC.md` 已是完整方案
- **与运行时按值正交**,推荐双轨并行(静态为主,运行为辅)
- **核心价值**:零运行时开销 + 库内天然免疫 + 推广所有团队零摩擦
- **核心代价**:失去记录级 / cell did(可由运行时补)
- **核心风险**:ts-morph 工程复杂度(可缓解)
- **行动建议**:立项后 P1 直接走 `DESIGN-STATIC.md §11` 路径

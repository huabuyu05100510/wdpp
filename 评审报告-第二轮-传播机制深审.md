# 评审报告（第二轮）：传播机制与上下文模型深审

> 评审对象：《终极技术方案-第一性原理重构.md》§1.3 传播规则、§2.2 上下文栈、§4.2 插桩清单、§5.4 原生补丁
> 前置文档：《评审报告-边界探索与替代方案.md》（第一轮：架构与 React 收口的 8 个 P0 + 7 个 P1）
> 本轮聚焦：传播机制本身的覆盖度。方法 = 拿真实业务代码的常见写法逐条过插桩清单，问"这一行过去之后标签还在吗"。
> 评审日期：2026-07-30

---

## 〇、本轮裁决

第一轮证明：**边画不到 DOM 上**（收口侧 8 个 P0）。
本轮证明：**标签在到达 JSX 之前就会大量死亡**（传播侧）。最要害的是两个：

1. **原生方法变换黑洞**（P0-9）：`toFixed/toUpperCase/trim/slice` 全 CUT——文本上屏前最后一道工序几乎必过这些方法；
2. **上下文模型的词法/动态混用**（P1-架构修正）：render props、事件处理器等"回调在定义组件的 ctx 窗口外执行"的场景系统性失效。

另发现插桩清单的 4 处整行缺失（变量赋值、for-of/for-in、delete、&&/|| 值语义）。

---

## 一、P0-9（新增致命）：原生方法变换黑洞

### 问题

真实代码里，API 文本在上屏前几乎必然经过 String/Number 方法加工：

```jsx
<span>¥{item.price.toFixed(2)}</span>        // 电商价格，toFixed 是标配
<h1>{user.name.toUpperCase()}</h1>
<p>{bio.trim().slice(0, 100)}…</p>
<div>{tags.join(' / ')}</div>                // join 前还有 split/filter
<time>{dayjs(order.createdAt).format('YYYY-MM-DD')}</time>
```

原方案 §1.3 传播表："不透明函数调用 `f(a)` → ∅（CUT）"；§5.4 原生补丁清单只有 **Array/Object.assign/fromEntries/structuredClone/JSON.parse**——**String.prototype、Number.prototype 一个都没有**。`name.toUpperCase()` 的 receiver 是带标签的原始字符串，但调用结果标签 = ∅ → **断流在最后一公里**。`toFixed` 一个方法就足以让电商类应用的价格边全灭。

注意这不只是召回问题，是**验收铁律会当场打脸**：P1 阶段的验收用例只要写"价格渲染出边"就过不了。

### 修复（不需要 patch 原生，codegen 规则即可）

对 `x.m(...args)` 形式的调用，编译期加一条保守并集规则：

```js
// 编译前
const s = user.name.toUpperCase();
// 编译后
const _recv = user.name;            // _recv$ = bit(1)（慢道已有）
const s = _recv.toUpperCase();
const s$ = _recv$;                  // 纯方法：结果影子 ∪= receiver$ ∪ args$
```

- **纯方法**（String/Number/Date 的变换类方法，数学函数）：`result$ = recv$ ∪ args$` —— 保守但正确（may-分析）；
- **变异方法**（`push/splice/sort` 等）：仍走 §5.4 原生补丁处理"写入侧"传播；
- **白名单**：为 String/Number/Date/Math 各建一张纯方法表（每个都是 spec 里写死的，工作量有限）；表外方法维持 CUT；
- 验收：`items.map(i => i.price.toFixed(2))` 渲染 `<span>` 必须出 `price` 数据边。

### 波及修正

原方案第七章性能表和第九章 P0 的"原生替换"模块都要扩清单：String.prototype（toUpperCase/toLowerCase/trim/slice/substring/split/replace/replaceAll/padStart/concat/at）、Number.prototype（toFixed/toPrecision/toString）、String.fromCharCode、Math 全系。

---

## 二、P1-架构修正：词法 ctx 捕获，取代动态 currentCtx() 为主通道

### 问题

§2.2 的上下文栈模型假设：**回调执行时，其所属组件的 ctx 仍在栈顶**。这个假设在以下场景系统性破裂——它们的共同点是"回调在定义组件的 ctx 窗口**之外**执行"：

```jsx
// 场景 1：render props（react-table / Formik / downshift / rc-* 全家桶）
<Table renderRow={(row) => <div>{row.name}</div>} />
```

时序：外层组件 render → enterComponent push → 创建 `<Table>` element（renderRow 只是被**定义**，未被调用）→ return → **exitComponent pop，栈空** → React 渲染 Table（库代码，不 push ctx）→ Table 调用 renderRow → **此刻 contextStack 是空的**。renderRow 内所有插桩走全局回退通道，`__runtime.jsx` 里的 `currentCtx()!` 直接崩溃（第一轮 #20）或 P 标签全丢。

```jsx
// 场景 2：事件处理器里 setState（§2.4 已暴露但未根治）
onClick={() => setName(user.name)}
```

§2.4 的读写两侧不一致：属性读插桩写 `__ctx.lastReturnTaint`（词法 ctx），wrappedSetter 读 `contextStack[top] ?? global`（动态）——点击时栈空，读到的是 global，**写进去的那一份永远读不出来**。

### 修复：词法捕获优先

`__ctx` 本来就是组件函数体内的词法常量。**所有词法位于组件体内的插桩（含嵌套箭头函数、render props 回调、事件处理器）直接闭包捕获这个 `__ctx`**，不再依赖运行时的栈顶：

```js
function Page() {
  const __ctx = __runtime.enterComponent();
  try {
    return __runtime.jsx(Table, {
      renderRow: (row) => {
        // 词法捕获外层 __ctx：即使此刻栈空，ctx 对象经闭包存活
        const _n = row.name;
        const _n$ = (row != null && row[TAINT_FLAG]) ? slowGetProp(row, 'name', __ctx) : 0n;
        if (__ctx) __ctx.lastReturnTaint = _n$;   // 写词法 ctx，不写 global
        return __runtime.jsx("div", { children: _n }, [_n$], __ctx);  // ctx 显式传入
      }
    }, {}, __ctx);
  } finally { __runtime.exitComponent(__ctx); }
}
```

正确性论证：
- ctx 对象被 pop 后**作为闭包变量继续存活**，JS 单线程保证任意时刻只有一个执行流在写它 → 无串味；
- 同一组件的多个实例各有各的 `__ctx`（每次 render 新建）→ 实例间无串味；
- render props 被库在任何时机调用，写侧读侧都是同一个词法 ctx → 一致；
- wrappedSetter 在组件体内创建，同样闭包捕获 `__ctx` → 场景 2 的读写两侧统一；
- **动态 `currentCtx()` 降级为兜底**：只服务于无法词法确定归属的场景（helper 函数、P0-3 的参数通道）。

此修正同时吸收第一轮 #20（normalizeStamp 崩溃）——`__runtime.jsx` 的 ctx 由调用点显式传入，词法外场景传 null 走 `P = 0n` 兜底。

---

## 三、插桩清单的四处整行缺失

§4.2 清单对照真实代码，以下 AST 节点整行缺失：

### 3.1 变量赋值（非声明）—— P1

```js
let label;
label = user.name;      // AssignmentExpression，左值 Identifier
count += item.qty;      // 复合赋值
idx++;                  // UpdateExpression
```

清单只有"变量声明 `const x = e`"一行。重赋值后 `x$` 保持旧值或 ∅ → 断流/陈旧。**修复**：AssignmentExpression/UpdateExpression 左值为 Identifier 时同步更新影子（`x$ = e$`、`x$ |= e$`、`++` 时标签不变）。

### 3.2 for-of / for-in 循环变量 —— P1

```js
for (const u of users) { rows.push(<Row data={u} />); }   // 列表渲染标配
for (const k in config) { ... }
```

清单里 while/for 只有"每轮 pcPush/pcPop"（循环条件的控制边），**循环变量本身的槽位初始化没有条目**：`u` 是原始值时其影子必须从数组 index 槽逐轮取出。**修复**：

```js
// for (const u of users) 编译为
let __i = 0;
for (const u of users) {
  const u$ = users[TAINT_FLAG] ? slowGetProp(users, String(__i), __ctx) : 0n;
  __i++;
  ...
}
// for-in：u$ = obj[TAINT_FLAG] ? slowGetProp(obj, k, __ctx) : 0n
```

### 3.3 `delete obj.field` —— P1（假阳性源）

删除属性不清影子槽 → 后续读到 `undefined` 仍带旧标签 → **假阳性边**。修复：`delete a.b` 插桩后随 `a[TAINT_FLAG] && slowDelete(a, 'b')`。

### 3.4 `&&` / `||` 的值语义未定义 —— P1（假阴性+假阳性双向）

§1.3 只有"比较/布尔化 result.D = ∅"，但 `&&`/`||` **不是布尔化**——结果是操作数本身。两条规则都缺：

- **D 通道**（may-分析）：`a && b`、`a || b` 的渲染值来自 a 或 b → `D = a$ ∪ b$`；
- **P 通道**（控制）：选哪个分支由 a 决定 → `P ∪= a$`。

反例说明必要性：`{isVip && name}` 若按朴素并集只记 D，会把 `isVip` 画成文本的**数据边**（假阳性）；若按 CUT，控制边丢失（假阳性）。注意这与第一轮报告的 `{cond && <el/>}` 不同——那个是元素级，这个是**文本级**，且 §5.5 的 normalizeStamp 模型救不了它（见下条）。

---

## 四、normalizeStamp 模型缺陷（P1）

§5.5：`normalizeStamp(s)` 在 **jsx() 调用时刻**取 `P: pcUnion(currentCtx()!)`。两个漏洞：

1. **undefined 崩溃**：§4.3 示例给 em 传的是 `[undefined]`，而 normalizeStamp 只判 `s === null || s === 0n` → `undefined` 落入 `{D: undefined, ...}` → 后续 `stamp.D === 0n` 为 false → `expandBits(undefined)` 抛异常。示例与实现自相矛盾。
2. **文本级控制边丢失**：`{isVip && name}` 编译后 pcPush/pcPop 发生在**子表达式求值期间**，而 jsx() 在其**之后**才调用 → jsx 时刻 pcStack 已空 → P 拿不到。元素级（`{isVip && <em/>}`）碰巧正确（em 的 jsx 在 push 窗口内执行），文本级必丢。

**修复**：childStamps 的元素类型从 `bigint` 改为**完整 Stamp `{D, P}`，在表达式求值点捕获**（push 窗口内），jsx() 只做透传不做归一化。这也顺便统一了 P0-1 修复后 attrStamps 的语义。

---

## 五、P2 清单（补齐生态与工具链）

| # | 问题 | 对策 |
|---|------|------|
| 1 | **自定义 jsx 签名丢弃 `key`**：automatic runtime 的第三参是 key，`jsx(type, props, stamps)` 把它吞了 → 列表 reconciliation 身份错乱（正确性问题，非标签问题） | 签名改 `jsx(type, props, key, stamps)` 并转发；同时意味着**不能复用 preset-react 的 JSX lowering**——需插件自实现 JSX 转换，并钉死与 @vitejs/plugin-react/esbuild 的执行顺序（enforce:'pre' + plugin-react 关闭 JSX 或改 classic 托管） |
| 2 | **useSelector / useSyncExternalStore 靠运气工作**：selector 在渲染期内被调时碰巧 ctx 在栈顶，但订阅检查等渲染期外调用会污染全局回退通道 | 显式 wrap useSyncExternalStore/useSelector；配"selector 渲染期外调用不串味"单测 |
| 3 | **React.Children.map 也克隆 element**（同 cloneElement） | 与 cloneElement 一并 wrap |
| 4 | **React 19 `use()` hook** 读取 promise/context | 同 useContext 处理 |
| 5 | **computed key `obj[user.id]`**：键的选择本身依赖字段 | 结果 `D ∪= key$`（保守并集，一行 codegen） |
| 6 | **JSON.parse 直调**（localStorage 缓存水合路径）：§5.4 注释提了但 sourceId 无从得知 | 标 `unknown://json.<path>` 源，DevTools UI 标注"来源不明"；引导用户接 stampOrigin 手动补源 |
| 7 | **补丁幂等需要 marker**：HMR 重复执行 runtime 模块会二次包装 Array.prototype | `if (_map.__taint_patched__) return;` |
| 8 | **stampMap 全空也 set**：§7 声称短路但代码仍 `stampMap.set(props, ...)` | stamps 全零时跳过 WeakMap 写，省每次渲染数千次写入 |

---

## 六、修订后的 §4.2 插桩清单（diff）

| AST 节点 | 状态 | 说明 |
|----------|------|------|
| 变量声明 | 原有 | — |
| **变量赋值 / 复合赋值 / ++/--** | **新增** | 左值 Identifier 同步影子 |
| 属性读 | 原有（第一轮修正：显式传 arg$、词法 ctx） | — |
| 属性写 | 第一轮修正（按 e$ 驱动） | — |
| **对象字面量 / 展开** | **新增（第一轮 P0-4）** | 带标签值逐槽 slowSet + 打标 |
| **方法调用 `x.m(...)`** | **新增（本轮 P0-9）** | 纯方法白名单：`result$ = recv$ ∪ args$` |
| **&& / \|\|** | **新增** | `D = a$ ∪ b$`，`P ∪= a$`；JSX 子表达式位置按控制构造处理 |
| if/三元/switch/循环条件 | 原有 | labeled break/continue 需跨层 pop，配穷举单测矩阵 |
| **for-of / for-in** | **新增** | 循环变量逐轮从槽位初始化 |
| **delete** | **新增** | 清影子槽 |
| 函数声明/调用 | 原有 + 第一轮 P0-3 参数通道 | — |
| 组件函数入口 | 原有 + 本轮词法 ctx 修正 + 第一轮 P1-11（ctx 挂 instance、运行时 hook 计数器） | — |
| JSX | 第一轮 P0-1（attrStamps）+ P0-2（props 打标）+ 本轮（key 透传、求值点捕获 Stamp） | — |
| **String/Number 原生补丁** | **新增（本轮 P0-9）** | 或 codegen 规则替代 |

---

## 七、对原方案论断的修正汇总

| 原论断 | 修正 |
|--------|------|
| §1.3 "不透明函数调用 → ∅（CUT），宁漏勿错" | 对 String/Number 纯方法必须放开，否则文本上屏前最后一道工序断流，召回率不可能达标 |
| §2.2 "被调函数通过 currentCtx() 取栈顶 → 始终写入正确的调用者上下文" | 对 render props/事件处理器不成立；词法捕获才是主通道，currentCtx() 降级为兜底 |
| §4.2 插桩清单 | 缺 6 行（赋值、for-of/in、delete、&&/\|\|、对象字面量、方法调用），见上表 |
| §9 P3 "召回 ~90%+" | 在第一轮 8 个 P0 + 本轮 P0-9 修复**之前**不现实；修复后 + 值指纹兜底，90% 是合理目标 |
| §9 P0"原生替换"验收范围 | 必须扩到 String.prototype/Number.prototype，否则 P1 验收用例（价格、大写姓名）当场失败 |

---

## 八、两轮合并后的开工门槛

**允许进入 P0 开发的前提**：第一轮 8 个 P0 + 本轮 P0-9 的修复落入文档；§4.2 按第六节 diff 补齐。

**P0 阶段验收追加**：
- `items.map(i => i.price.toFixed(2))` → `<span>` 出 price 数据边（P0-9 + P0-6）
- `let x; x = user.name;` → 出边（变量赋值）
- `for (const u of users) return <li>{u.name}</li>` → 出边（for-of）
- `delete user.name` 后渲染 → **不**出边（delete 反例）
- render props（react-table 最小复现）→ 出边且不崩（词法 ctx）
- `{isVip && name}` → name 数据边 + isVip 控制边，且**不**出 isVip 数据边（&& 双通道反例）

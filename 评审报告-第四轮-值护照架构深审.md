# 评审报告（第四轮）：《深入浅出》值护照架构深审

> 评审对象：《API-DOM绑定图-深入浅出.md》
> 性质：这不是原方案的修订，而是**架构转向**——"护照属于值，不属于变量"，用 `Map<值, 护照>` 取代槽位影子内存 + fiber 过缝。
> 评审方法：核销它声称消解的问题（对照前三轮），然后对抗性验证它的核心命题在新架构下是否引入了新 P0。
> 评审日期：2026-07-30

---

## 〇、总裁决

**这次转向方向正确，数据边侧显著优于旧架构；但文档低估了三个新 P0，且"按位置追踪是可选优化"的判断必须反转——对控制边它是必选。**

先公正地核销它的真实收益（对照前三轮的 P0 清单）：

| 前三轮的问题 | 新架构下 | 验证 |
|--------------|----------|------|
| 一轮 P0-1 属性边缺失 | ✅ 消解 | setAttribute/property 拦截天然覆盖属性 |
| 一轮 P0-2 props 传递断流 | ✅ 消解 | 值穿透一切传递路径，无需 props 打标 |
| 一轮 P0-5 useState setter 语义破坏 | ✅ 消解 | 不碰 hooks，无 wrappedSetter |
| 一轮 P0-6 数组补丁未打标 | ✅ 消解 | map 结果元素的值已在映射中 |
| 一轮 P0-7 lanes 剪枝错误 | ✅ 消解 | 无 fiber 遍历 |
| 一轮 P0-8 DevTools hook 缺失全灭 | ✅ 消解 | 无 hook 依赖 |
| 一轮 #23 第三方库内部断流 | ✅ **结构性解决** | antd 内部写 DOM 也走 DOM API → 被拦截 → 值查询命中 |
| 一轮 #16 immer/Proxy | ✅ 基本消解 | draft 拷贝的是值，最终对象的原始值在映射中 |
| 二轮 P1-15 组件返回原始值 | ✅ 消解 | 值查询不看 fiber 父子结构 |
| 二轮 词法 ctx 修正 | ✅ 消解 | 条件栈只在用户代码内，无跨组件归属问题 |
| 对象 freeze（一轮 P1-9） | ✅ 消解 | 盖戳只读不写 |
| React 版本耦合/fiber.tag | ✅ 消解 | 框架无关是真金白银的维护性胜利 |

这是前三轮 P0 的**一大半**。DOM 拦截 + 值查询让 React/Vue/Svelte 一视同仁、SSR 扫描无需 fiber、没有 hooks 包装——这些是真的。

**但是。** 核心命题"一个映射，并集，查询"隐藏了三个文档未承认的 P0，且 §四对碰撞的分析只覆盖了**查询侧**，漏掉了更致命的**传播侧**。

---

## 一、P0-A：碰撞的传播侧中毒——假阳性级联无界

§四说碰撞"不精确，但正确——报'可能 x 或 y'是诚实的"。**这只在查询侧成立。** 传播侧完全不同：

```
API:  { user: { age: 7 },  config: { maxRetries: 7 } }
映射:  7 -> {user.age, config.maxRetries}        ← 查询侧的"诚实歧义" ✅

本地代码:
const retryDelay = config.maxRetries * 1000;     // 7000
// 插桩查 passport(7) = {user.age, config.maxRetries}
// 7000 -> {user.age, config.maxRetries}         ← user.age 是**捏造的** ❌

const msg = `重试 ${retryDelay}ms`;               // "重试 7000ms" -> 同上
```

**变换插桩查的是值，值一碰撞，无关字段的护照就混进新值，新值再参与下一次变换——假阳性沿变换链无界放大。** 一次 `0` 的碰撞（`items.length`、`cart.count`、本地 `i` 全是 0）可以污染整条本地计算链。这直接违反文档自己继承的验收铁律"**反例不记边**"——而且不是边角 case，是低熵值（`true/false/0/1/""/200/"success"`）的系统性行为。

量化直说：**这个架构对字符串（名字、URL、ID）是高精度引擎，对数字是降格引擎，对布尔是失效引擎。** 而布尔值恰恰是控制边的主力类型——见下条。

**必须补的设计**（文档目前一个字没有）：
1. **低熵值黑名单**：`true/false/0/1/""/null` + 可配置词表（状态码、枚举文案）**不盖戳、不查询**，诚实显示"低熵值不追踪"；
2. **护照集大小熔断**：查询时 `|passport| > k`（比如 5）降置信为 `collision`，UI 聚合显示"N 个候选字段"而不是画 N 条边；
3. **传播侧交集约束**（把 §七②的思路推广到所有变换）：变换输出护照 = `(∪ 输入护照) ∩ 已有映射中该输出的护照` 当输出值已在册时——巧合值不吸收输入护照。

## 二、P0-B：控制边在布尔上构造性失效——"可选精化层"必须反转为"必选"

§八把控制边说成"小例外"。但看机制：`if (data.user.isVip)` 的条件栈压入的是**条件值的护照** = `passport(true)` = **应用史上所有值为 true 的 API 布尔字段的并集**。三个接口之后这就是几十条控制边的噪声。§八自己强调的"加载/空/内容三态切换是用户最常问的场景"——`loading`、`isEmpty`、`hasError` 全是布尔。

**结论：控制边是值追踪最不适合的场景，不是小例外。** §四/§十把"按位置追踪"列为"可选优化"——对控制边必须反转：**它是控制边可用的前提**。

**但有个廉价的反转方案**（这是本轮最重要的改进意见）——位置层不需要全量复活，只需要一个极小的切片：

> **只对"条件表达式"恢复槽位读追踪。** `if/三元/&&/||/while` 的**条件子表达式**编译时插入旧方案的 `readProp$`（SM + WeakMap，只在条件位置查）。插桩面从"全部属性读"缩到"条件表达式内的属性读"——估计不到属性读总量的 5%，P3 快慢道问题都不需要（量太小，直接全慢道）。控制边精度回到字段级，数据边保持纯值追踪。

条件栈、双支对称、早退区域（§八已正确吸收第三轮结论）原样保留，只是压栈的护照从"查值"换成"条件表达式的槽位读结果"。

## 三、P0-C：值映射无法 GC——无界增长 + 陈旧护照

旧方案的 WeakMap 有免费 GC（§7.3 曾是卖点之一）。新架构：

- **原始值 key 不能用 WeakMap**（WeakMap 只接受对象 key）→ `"Ada"`、`7`、`"LV.7"` 每个盖戳值、每个变换产生的新值，**永驻内存**；
- 对象/数组 key 若混在同一个 `Map` 里，是**强引用** → API 响应对象永不释放；
- 长 dev session（热更新几十次、切账号、翻页几百条）→ 映射无界膨胀，**且护照会陈旧**：用户登出后，旧用户的 "Ada"→{user.name} 仍在册，新 session 里任何地方渲染 "Ada"（哪怕是本地写死的）都画出死人的边。

**必须补生命周期设计**（文档目前只有一句骄傲的"累加的"，这是 bug 不是 feature）：
1. 拆两个容器：`WeakMap<object, passport>`（对象身份）+ `Map<primitive, passport>`（原始值）；
2. 原始值表加**代际（generation）**：护照条目带 generation id，路由切换/登出/手动清空时 bump generation，查询过滤过期代；旧代条目定期 compaction；
3. 单值护照集容量上限（配合一.2 的熔断）。

## 四、P1-D：DOM 侧永远是字符串——类型归一化桥完全没写

盖戳侧的值是**有类型的**（`7` 是 number），DOM 写入侧的值**永远是字符串**（`nodeValue`/`textContent`/`setAttribute` 全是 string）。`{level}` 上屏变成 `"7"`，查 `"7"` **查不到 number 7 的条目**——每一个数字、布尔（经 String()）、bigint 的边都卡在这座没建的桥上。

**修复**（二选一，建议都做）：盖戳时数字/布尔同时存原始值和 `String(v)` 两个 key；查询时尝试数值化回退。注意归一化会加剧碰撞（`"7"` 字符串字段 vs `7` 数字字段合流）——与一.2 的熔断联动。

## 五、P1-E：文档自己的招牌例子在 JSX 相邻文本下降级

§一的例子 `"LV.7"` 来自 `"LV." + data.user.level`（JS 拼接，单值，命中 ✅）。但同样的 UI 写成 JSX 习惯用法：

```jsx
<span>LV.{data.user.level}</span>
```

children 是 `["LV.", 7]`——React 创建**两个文本节点** "LV." 和 "7"。查询："LV." → ∅；"7" → 碰撞集。**前缀语境丢失，精度从"确定"跌到"碰撞"。**

**改进（廉价且通用）**：onDomWrite 查文本节点时，先尝试**拼接相邻文本兄弟**的值再查（`"LV." + "7" = "LV.7"` → 命中 `{user.level}` 确定边），不命中再逐个查。DOM 侧做这个只需 `previousSibling/nextSibling` 看一眼，不需要任何框架知识。

## 六、P1-F：SSR/ hydration 首屏——盖戳拦了，写入没发生

§五把 SSR 水合数据列入盖戳入口（对），但 **hydration 匹配已有 DOM 文本节点时不触发任何 DOM 写**——`nodeValue`/`textContent` setter 一次都不fire → **整个服务端渲染的首屏一条边都没有**，直到首次客户端更新。

**修复（本架构下反而优雅）**：hydration 完成后做一次**全 DOM 值扫描**——遍历 text node/属性查映射补边。旧架构做这事要虚拟 commit + fiber 遍历；新架构就是 `TreeWalker` 走一遍，几十行代码。这是新架构的红利，文档应该认领。

## 七、P1-G：DOM 拦截面的规格错误与补全清单

§九的拦截清单有多处规格问题，照抄会翻车：

| 项 | 文档 | 实际 |
|----|------|------|
| `textContent` | `Element.prototype` | **在 `Node.prototype` 上**（WebIDL），patch Element 会创建遮蔽自有属性，行为微妙 |
| 文本更新 | 只提了 textContent/innerHTML | React 更新文本主要走 **`CharacterData.prototype.nodeValue`/`data` setter** 和 `document.createTextNode`；Vue3 `hostSetText` 也走 nodeValue——**这三个才是主通道**，textContent 反而是少数路径 |
| 属性写入 | "value/className/href/src 同理" | React 对已知属性走 **property 赋值**不走 setAttribute：`value` 在 `HTMLInputElement.prototype` 上、`src` 在 `HTMLImageElement.prototype` 上……需要**按 React DOM property 表逐接口 patch**；`style.width` 在 `CSSStyleDeclaration.prototype`（setProperty + 各属性 setter） |
| 边清理 | "节点重写时先清旧边" | 只覆盖重写。**节点移除**（`removeChild`/`remove()`）和 **`normalize()` 合并文本节点**（合并不触发任何 setter）需要 `MutationObserver` 兜底——与其 patch 删除 API 全家，不如统一用 MutationObserver 做清理通道 |
| `innerHTML` | 列了 | 整串 HTML 查映射必 miss（字段值只是片段）。策略：substring 匹配或 CUT 标"html 片段"，别默默 miss |

## 八、P1-H：缓存与 WebSocket 经 JSON.parse 退化为"全响应并集"

§五说"存储类不用专门拦，值早就在映射里"。推演一遍 `localStorage` 缓存水合：

1. 写入时 `localStorage.setItem(k, JSON.stringify(data))`——stringify 是纯方法，输出串的护照 = **全部字段的并集**；
2. 读出后 `JSON.parse(cached)`——输入串的护照是全响应并集 → parse 输出的**每个叶子**都顶着全响应并集 → 每片叶子都画出 N 条边。**精度当场死亡，比没边还糟。**

WebSocket 同理：库内部 `JSON.parse(event.data)`，叶子全部顶并集。

**修复（一条通用规则，很便宜）**：全局 patch `JSON.parse`——**若输入串有护照，按路径逐叶子重盖**（复用 stampOrigin 的递归，sourceId 取串护照的来源；若无法归因则标 `unknown://json`）；输入串无护照则原样透传。这一条同时救 localStorage、sessionStorage、WebSocket、SSE、postMessage 五条通道——它们全是"串进 parse 出"。

## 九、P2：四档恢复与验收铁律的张力

§七"默认走并集（③）"的产品判断我同意（漏报比低置信过染糟），但要落实：

1. **验收铁律要按档位重述**："反例不记边"只对 ①②档成立；③档的反例标准是"必须标 approx 且 UI 降权"，否则 CI 里正反例没法写；
2. **②的交集规则要精确化**：`result$ = map.get(result) ∩ (∪ inputs$)`；交集为空 → 落入 ③ 还是 CUT 需要定义（建议：输入全无护照 → CUT；输入有护照但交集空 → result 是巧合碰撞 → CUT 比并集诚实）；
3. ③对**多参数**的过染极快（`format(a, b, c, d)` 四个字段护照全并给输出）——建议 ③ 只保留输入护照的**前 k 个**或整体降一级置信。

## 十、P2 杂项边界

| # | 边界 | 说明 |
|---|------|------|
| 1 | `-0`/`NaN` 作 key | Map 用 SameValueZero，`NaN` 可被查（好），`-0` 与 `0` 同 key（可接受） |
| 2 | iframe/多 realm | 每个 realm 独立映射；postMessage 已在盖戳清单 ✅ |
| 3 | 同值不同源的**时序** | "Ada" 先在 A 接口出现后在 B 接口出现——并集是对的；但 A 接口字段下线后仍在册（见 P0-C 代际） |
| 4 | `dangerouslySetInnerHTML` | 见七，html 片段策略 |
| 5 | CSS 生成的内容（`content: attr()`、counter） | 值经 CSSOM，DOM 拦截看不到——写入"做不到"清单，一行即可 |
| 6 | canvas/WebGL 渲染的文本 | 同理做不到，一行 |
| 7 | 浏览器 autofill/扩展改 DOM | MutationObserver 会看到非程序写入，查询 miss 即跳过，天然免疫 ✅ |

## 十一、改进意见汇总（按优先级）

1. **【P0】低熵值策略**：黑名单不盖戳 + 护照集熔断 + 传播侧交集约束（一）
2. **【P0】条件表达式的槽位读侧车**：控制边精度的唯一现实解，插桩面 ~5%（二）
3. **【P0】值映射生命周期**：双容器（WeakMap+Map）+ generation + 容量上限（三）
4. **【P1】类型归一化桥**：盖戳存双形态，查询数值化回退（四）
5. **【P1】相邻文本拼接查询**：救 JSX 习惯写法（五）
6. **【P1】hydration 后全 DOM 扫描**：首屏补边，本架构红利（六）
7. **【P1】DOM 拦截规格修正**：主通道是 nodeValue/data/createTextNode；property 表按 React 对齐；清理走 MutationObserver（七）
8. **【P1】JSON.parse 全局重盖规则**：一条规则救五条通道（八）
9. **【P2】四档规则精确化 + 验收铁律按档位重述**（九）

## 十二、可行性终判与路线图修正

**可行，且比旧架构更可行**——前三轮 P0 消解过半，框架无关、无 hooks 侵入、SSR 扫描极简是真收益。但它不是"一个映射就完了"：剩下的系统是 `值映射 + 变换插桩 + DOM 拦截 + 条件栈(+条件槽位侧车) + 生命周期管理`——比旧架构简单一个量级，但文档 §三"就这三步，没有别的"的宣称过头了，§十三的 P0 阶段其实已经承认了这一点（变换插桩仍在）。

精确度诚实定位：**字符串数据边 ~95% 可用；数字数据边降格（碰撞置信度）；控制边依赖条件槽位侧车，没有侧车则控制边不可用。** Dev 工具定位下这是好买卖。

路线图修正（在原 §十三基础上）：

```
P-1  值指纹层(不变，仍是第一周) + 低熵值黑名单 + 相邻文本拼接查询
P0   引擎内核 + 类型归一化桥 + JSON.parse 重盖 + 值映射生命周期(代际)
     —— go/no-go 加一条:内存曲线(长 session 映射规模必须平稳)
P1   控制边:条件栈 + 双支对称 + 早退区域 + **条件槽位侧车(不是可选项)**
P2   DOM 拦截规格修正 + MutationObserver 清理 + hydration 全扫描
P3   置信度 UI(taint/value-match/approx/collision/断流) + 生态入口补齐
```

# 规则:函数默认依赖输入(Universal Taint Union)

> **版本**:v1 · **日期**:2026-08-06
> **核心条款**:WDPP 引擎对任意函数 / 运算 / 操作的输出,**默认假设其依赖所有输入**。

---

## 1. 规则陈述

```
∀ f: (input₁, input₂, ..., inputₙ) → output
   provenance(output) ⊇ ∪ provenance(inputᵢ)
```

**output 的来源集合,默认等于所有 input 的来源集合的并集。**

- 失败模式优先于精确推断
- 不试图分析函数体内部(库未插桩 / opaque)
- 不依赖调用语义(同步/异步/promise/thunk)
- 不假设部分输入被丢弃

---

## 2. 适用对象

| 类型 | 表达式 | 应用 |
|---|---|---|
| **二元运算** | `a + b` | `__recover(a + b, [a, b])` |
| **多元运算** | `a + b + c` | `__recover((a+b)+c, [a, b, c])` |
| **模板串** | `` `${a}${b}` `` | `__recover(\`${a}${b}\`, [a, b])` |
| **方法调用** | `obj.method(x)` | `__recover(obj.method(x), [obj, x])` |
| **函数调用** | `f(a, b)` | `__recover(f(a, b), [a, b])` |
| **构造** | `new Foo(a, b)` | `__recover(new Foo(a, b), [a, b])` |
| **await** | `await x` | `__recover(await x, [x])` |
| **三元** | `a ? b : c` | `__recover(a ? b : c, [a, b, c])` |
| **短路** | `a && b` / `a \|\| b` | `__recover(a && b, [a, b])` |
| **空值合并** | `a ?? b` | `__recover(a ?? b, [a, b])` |
| **解构** | `const {x, y} = obj` | 每个变量独立 `__readSlot(obj, 'x'/'y')` |
| **字段读** | `obj.x` | `__readProp(obj, 'x')`(字段 passport) |
| **字段写** | `obj.x = y` | `__writeField(obj, 'x', y)` |
| **字段删** | `delete obj.x` | `__deleteField(obj, 'x')` |

---

## 3. 失败模式

| 场景 | 行为 |
|---|---|
| 函数体已知(白名单 / blessed) | 用 `__passthrough` 递归复制(per-field, 更精确) |
| 函数体未知(opaque) | 用 `__recover` over-approx(union inputs) |
| 部分输入实际未参与 | 仍标 union(过近似,sound) |
| 输入不可观测(WASM/native) | union 在边界捕获,内部黑盒 |
| 嵌套调用 `f(g(a), b)` | 内层 union 后传外层 union |

---

## 4. 为什么"默认"不是"全部"

按规则严格应用 `__recover(result, [all_args])` 在所有调用点:

- **过近似**:输出可能标了实际未依赖的 API(无害,DevTools 显示"可能来自")
- **不假精确**:从不隐藏真实依赖
- **可降级**:用户可手动用 `__passthrough` 提升精度(per-field)
- **soundness**:不漏标任何真依赖

---

## 5. 反例:什么场景下 **不** 用 union

| 场景 | 用什么 |
|---|---|
| 库已知纯变换(如 `toUpperCase`) | `__passthrough` 递归 per-field |
| 字段读取(单字段) | `__readProp`(单字段 passport,不 union) |
| 字面量 | 不调用任何 helper(无 passport) |
| 控制边 | `__controlAnd/Or/Ternary`(只 union 控制条件) |

**核心**:只在"产生新值"的边界用 union;读 / 控制流单独处理。

---

## 6. 验证清单(在 babel transform 中强制)

每条 AST 节点改写后,必须满足:

- [x] **BinaryExpression** → `__recover(a op b, [a, b])`
- [x] **CallExpression** → `__recover(fn(a, b), [a, b])`(深拷贝白名单除外)
- [x] **TemplateLiteral** → `__recover(\`${a}\`, [a])`
- [x] **ConditionalExpression** → `__recover(a ? b : c, [a, b, c])`
- [x] **LogicalExpression** → `__recover(a && b, [a, b])`
- [x] **NullishCoalescingExpression** → `__recover(a ?? b, [a, b])`
- [x] **AssignmentExpression** → `__recover(x = y, [y])`
- [x] **AwaitExpression** → `__recover(await x, [x])`
- [x] **TaggedTemplateExpression** → `__recover(tag\`${x}\`, [x, strings])`
- [x] **MemberExpression** (读) → `__readProp(obj, key)`(字段级,非 union)
- [x] **MemberExpression** (写) → `__writeField(obj, key, value)`
- [x] **ObjectPattern** 解构 → `__readSlot(obj, key)`(每个变量)
- [x] **DeleteExpression** → `__deleteField(obj, key)`
- [x] **OptionalCallExpression** → `__recover(a?.(b), [a, b])`
- [x] **OptionalMemberExpression** → `__optionalChain(obj, key)`(短路 + 字段)
- [x] **ThrowStatement** → `__throw(x)`(异常传递)

---

## 7. 实施:运行时验证

不依赖 babel transform 时,我们的 fallback:

1. **DOM 写入时**:沿 DOM 树向上收集所有 data-wdpp-section 属性(union)
2. **同一 section 内多个写入**:每个 element 的 title 是 union
3. **嵌套 section**:外 + 内 union

**等价于**:对任何 DOM 写入 `el.textContent = f(a, b)`,title = `apis(el) = ∪ ancestors_sections`。

---

## 8. 与精确追踪的关系

| 维度 | union(默认) | 精确 |
|---|---|---|
| 覆盖率 | 100% | ~30%(库未插桩) |
| 精度 | over-approx | exact(per-field) |
| 性能 | 快 | 慢(per-field 复制) |
| 适用 | dev / 默认 | audit / 精查 |
| 实现 | `__recover` | `__passthrough` + babel |

**规则**:**默认 union,精确可选**。

---

## 9. 一句话总结

> **任何函数的输出,默认继承所有输入的来源;只有当实现明确选择 `__passthrough` 时才升级到 per-field 精确追踪。**

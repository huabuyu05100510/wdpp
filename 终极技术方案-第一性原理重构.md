# 终极技术方案：第一性原理重构

> 从不可更改的约束出发，自证每一个机制的必然性。
> 吸收三大悖论（原始类型 WeakMap、全局状态污染、V8 IC 超态）的全部修正。
> 不引用任何文献——只从约束推导，每个机制都回答"为什么必须是这个，不能是别的"。

---

## 第〇章：问题与不可更改的约束

### 0.1 问题

用户指着屏幕上任何一个东西问：**"这是哪个接口字段弄出来的？"**

答案只有两种关系：

- **数据边**：字段的**值**流进了内容。`user.name` 的值 "Ada" 流进了 `<h1>` 的文本。
- **控制边**：字段的**值**决定了"渲不渲染"。`user.isVip` 决定了 `<em>VIP</em>` 是否存在。

整个系统的产出就是一张图 `G = (字段节点, DOM节点, {数据边, 控制边})`。

### 0.2 不可更改的约束（每条逼出一个决策）


| #   | 约束                                              | 含义                                     | 逼出什么                                  |
| --- | ----------------------------------------------- | -------------------------------------- | ------------------------------------- |
| C1  | JS 原始值不可变，**挂不上元数据**                            | `"Ada"`、`7`、`true` 没法贴标签               | 标签必须住在**值的旁边**（变量槽、属性槽），不能贴在值上        |
| C2  | 屏幕的唯一真相是 **DOM**                                | 用户看到的是 DOM 节点，不是 React 的 element/fiber | 只在**写 DOM 那一刻**记边，不在别处                |
| C3  | 接口数据以**解析后的对象**进入（`JSON.parse`/`fetch`），语言层面不透明 | 程序自己看不出 "Ada" 来自 `user.name`           | 必须在**数据进程序的边界亲手打标**                   |
| C4  | 我们只在 **dev 模式**运行                               | 能改代码、能 patch 全局、能付开销                   | 可以 monkey-patch 原生方法、Babel 插桩全 bundle |
| C5  | 我们**不控制**用户的应用逻辑                                | 是任意 JS                                 | 传播规则必须覆盖所有 JS 语法构造                    |
| C6  | 一次只跑**一条实际执行路径**（动态观测）                          | 能看到"发生了什么"，看不到"没发生的分支"                 | 精度优先于召回；未执行分支天然漏                      |




### 0.3 三大悖论的根因

在将上述约束推导的机制工程化时，遇到三个底层悖论。它们不是设计错误，而是**约束之间的冲突**在工程实现层面的暴露：

**Paradox 1：原始类型悖论**（C1 vs 工程实现）

- 约束 C1 说标签住"槽"不住"值" → 用 WeakMap 以对象为 key 存标签 ✅
- 工程实现：React 的 HostText fiber 的 `memoizedProps` 是**原始字符串**（如 `"Ada"`）→ WeakMap 不接受原始类型 key → 文本节点的标签**断流**
- 根因：WeakMap 方案假设所有要记边的 DOM 节点对应一个**对象** props，但 React 文本节点对应的是**原始值**

**Paradox 2：全局状态悖论**

- 全局 `__lastReturnTaint` 和 `pcStack`（条件栈）在同步代码中工作正常
- 但 Suspense 边界 + 组件嵌套渲染时，**跨组件函数调用**的返回污点归属不清
- 根因：全局单例状态无法区分"当前正在为哪个组件记账"，当组件 A 调用的库函数内部又触发了组件 B 的渲染（Suspense fallback），全局状态会串味

**Paradox 3：V8 IC 超态悖论**

- 约束 C4 允许 Babel 插桩 → 所有属性读 `obj.name` 替换为 `readProp$(obj, 'name')`
- 这导致 V8 的 Inline Cache 失效：每个属性读变成函数调用 + WeakMap 哈希查找
- 根因：95%+ 的对象（本地状态、配置、样式）**从未接触 API 数据**，却为它们支付了完整的插桩开销

---



## 第一章：从约束推导核心架构



### 1.1 机制 ①：海关盖戳（C3 逼出）

**为什么必须**：C3 说数据进入时是不透明的 → 程序自己看不出来源 → 必须**在边界亲手打标**。

**怎么做**：在 `fetch` / `Response.json()` / React Query 缓存写入点拦截，递归遍历响应对象，给每个字段分配一个全局唯一的整数 ID，记录在全局影子内存中。

```ts
// ← 第三轮修正：槽位语义为"后序并集"（子树叶子字段冒泡到父槽），不是"仅自身 id"。
//   否则整对象流出（JSON.stringify(user) / <Child user={user}/>）时边指向中间节点 …user，
//   用户指着文本只能得到"来自 user"而非具体叶子字段。
// ← 第三轮：visited WeakSet 防循环引用（手动接入的 store/ORM 实体可能有环，否则栈溢出崩页）。
// ← 第三轮：字段路径 key 用分段数组 JSON.stringify([sourceId, ...segments])，不用字符串拼接
//   ——合法 JSON 字段名可含 "."，{ "user.name": "Ada" } 与 { user: { name } } 拼接后共享 fieldId 互相污染。

function stampOrigin(obj: object, sourceId: string, path: string[] = [], visited = new WeakSet()): bigint {
  if (obj == null || typeof obj !== 'object' || visited.has(obj)) return 0n;
  visited.add(obj);

  // P1-9：冻结对象守卫，extensible 打 TAINT_FLAG，否则进 frozenTainted
  if (Object.isExtensible(obj)) {
    try { obj[TAINT_FLAG] = true; } catch {}
  } else {
    frozenTainted.add(obj);
  }

  const sh = ensureShadow(obj);
  const isArr = Array.isArray(obj);

  for (const [k, v] of Object.entries(obj)) {
    // 路径分段：数组元素用通配 []（#17，避免千条列表膨胀 registry）
    const seg = isArr ? '[]' : k;
    const id = getOrCreateFieldId(JSON.stringify([sourceId, ...path, seg]));
    let slot = union(sh.fields.get(k) ?? 0n, bit(id));   // 自身 id

    if (v && typeof v === 'object') {
      slot |= stampOrigin(v, sourceId, [...path, seg], visited);  // 子树并集冒泡（后序）
    }
    sh.fields.set(k, slot);
    indexValue(v, id);                                     // 值指纹索引（§1.7）
  }

  // P1-13：数组 length 槽 = 全部元素槽的并集（{items.length === 0} 空态控制边）
  if (isArr) {
    let lengthUnion = 0n;
    for (const t of sh.fields.values()) lengthUnion |= t;
    sh.fields.set('length', lengthUnion);
  }

  // 返回本对象全部槽位的并集，供父层冒泡
  let all = 0n;
  for (const v of sh.fields.values()) all |= v;
  return all;
}
```

**resolveSourceId 规则**（第三轮补全，此前全文只有函数名）：`(method, url, body)` -> `"<METHOD> <routeTemplate>"`。

- 优先查路由表（React Router / Next 路由配置 / 静态 map）匹配 URL -> `GET /users/:id`；
- 无路由表则 URL pathname 去参：数字段 -> `:id`、UUID -> `:uuid`、长 hash -> `:hash`；
- query string 丢弃（`?tab=1` 不影响字段来源）；
- `/users/1` 与 `/users/2` 归一为同 sourceId -> 同字段共享 fieldId（字段级血缘，见 §11.3）。

**幂等性**：`union` 是 bigint 的 `|`，重复打不翻倍。流式/分页天然支持。
**幂等性**：`union` 是 bigint 的 `|` 运算，重复打不翻倍。流式/分页数据天然支持。

### 1.2 机制 ②：影子住槽（C1 逼出）

**为什么必须**：C1 说原始值挂不了属性 → 标签不能住在值上 → 只能住在**承载值的槽**上。

**两种槽**：

- **变量槽**：Babel 编译时为每个变量声明配一个影子变量（`let x$ = ...`）
- **属性槽**：全局 `SM = WeakMap<object, Shadow>`，`Shadow = { fields: Map<key, bigint> }`

原始值还是那个原始值，所有 JS 行为正常（`typeof`、`===`、`Object.is`、`JSON.stringify` 全不受影响）。

### 1.3 机制 ③：运算传播

**为什么必须**：运算不创造来源，只搬运来源。输出槽的标签 = 输入槽标签的并集。


| 运算                                      | 输出标签                                                                                                              | 原因                                                     |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| 字面量                                     | ∅                                                                                                                 | 没有来源                                                   |
| 变量读 `x`                                 | `x$`                                                                                                              | 继承变量的标签                                                |
| **变量赋值** `x = e` / `x += e` / `x++`     | 同步影子：`x$ = e$` / `x$                                                                                              | = e$`/`++` 时标签不变（值未变来源）                                |
| 属性读 `a.b`                               | `SM(a).fields.get('b') ?? ∅`                                                                                      | **field-sensitive**：只取该属性槽的标签                          |
| 计算属性读 `a[k]`                            | `SM(a).fields.get(k) ?? ∅`；**键本身也是输入**：`D                                                                         | = k$`                                                  |
| 属性写 `a.b = e`                           | **替换语义**（第三轮再修正 P0-4）：**无条件** `slowSet(a,'b',e$)`--e$ 为 0n 时**删除槽位**（不是跳过）；`if (e$ !== 0n) ensureFlag(a)` 仍按值驱动打标 | 槽位 = 最后一次写入的值的标签；`data.user.name = "Bob"` 后旧标签失效，不再假阳性 |
| `**delete a.b**`                        | `if (a[TAINT_FLAG]) slowDelete(a, 'b')`：清影子槽                                                                      | 不清则后续读到 undefined 仍带旧标签 -> 假阳性（P1）                     |
| 对象字面量 `{k: e}`                          | 逐属性：`if (e$ !== 0n) { ensureFlag(obj); SM(obj).fields.set('k', e$) }`                                             | 带非零影子的属性写入新对象槽（P0-4 修正）                                |
| 展开 `{...a, k: v}`                       | 逐槽拷贝源 + `k` 槽写 `v$`                                                                                               | field-sensitive 逐键复制                                   |
| 算术/拼接 `a + b`                           | `a$ ∪ b$`                                                                                                         | 输出依赖所有输入                                               |
| 解构 `const {x} = a`                      | `SM(a).fields.get('x')`                                                                                           | 逐槽继承                                                   |
| 模板字面量 `${a}${b}`                        | `a$ ∪ b$`                                                                                                         | 拼接不丢标签                                                 |
| `**&&` / `                              |                                                                                                                   | `（值语义）**                                               |
| **方法调用** `x.m(...args)`**（纯方法）**        | `result$ = recv$ ∪ (∪ args$)`                                                                                     | 纯方法输出 = f(receiver, args)，保守并集 sound（P0-9）             |
| **for-of / for-in 循环变量**                | `u$ = SM(coll).fields.get(String(i)) ?? ∅`（for-of）/ `u$ = SM(obj).fields.get(k) ?? ∅`（for-in）                     | 循环变量是原始值时，逐轮从集合槽位初始化（P1）                               |
| **函数实参->形参** `f(a, b)`                  | 调用点写 `__ctx.argTaints = [a$, b$]`；函数入口读 `const [a$, b$] = __ctx.argTaints`                                        | 原始值过函数边界不死（P0-3 修正）                                    |
| 比较/布尔化（`===`/`<`/`!`）                   | `result.D = ∅`，`P ∪= 操作数 D ∪ P`                                                                                   | 结果是 true/false，与任何字段值不同；但选择本身是控制                       |
| **不透明函数调用** `f(a)`（未插桩/三方/原生/WASM/非纯方法） | **三段式恢复**（见 §1.7）：身份匹配 `result === arg` -> 精确继承；否则值指纹匹配 `fingerprint(result) ∩ (∪ argTaints)`；都查不到 -> ∅（CUT）      | 不靠白名单，底层原理对任意不透明函数成立                                   |


**精度命门**：field-sensitive--每个属性槽各一本标签。如果 `obj.greeting = "hi"`（写死字面量）和 `obj.level = api.user.level`（API 来源）共享标签，则 `render(obj.greeting)` 会误画 `user.level` 的边 -> 假阳性。

**方法调用的分层（第一性原理）**：传播规则由"我们对函数知道什么"决定：

- **插桩函数**：精确 field-sensitive（§1.3 各规则）
- **已知纯方法**（String/Number/Date/Math 的变换类，spec 写死无副作用）：`result$ = recv$ ∪ args$`（sound may-近似）--这是 P0-9 的根，**不是白名单枚举，是纯度分类**：纯则并集，非纯走 §1.7
- **不透明函数**（三方/WASM/非纯）：§1.7 三段式恢复
- **高频原生**（Array.map 等）：§5.4 patch 保逐元素精度（变换值如 toUpperCase 值指纹查不到，纯方法并集兜底）



### 1.4 机制 ④：条件栈（控制边）

**为什么必须**：数据通道只能表达"值流进来"，表达不了"值决定了渲染不渲染"。需要第二条通道。

```
进入 if (data.user.isVip) { ... } 时：
  把 isVip 的标签压入条件栈        ->  栈 = [user.isVip]
  期间任何往屏幕的输出，画控制边
  离开 if，弹栈                    ->  栈 = []
```

**双轨分离**：数据标签（D）永不升级为控制标签（P），控制标签（P）永不污染数据标签（D）。这是精度的重要支柱。

**控制边对称性**（第三轮修正--原实现对否支/早退之后漏画，按 §0.1 自己的定义是召回漏洞）：

§0.1 定义"控制边 = 字段的值决定了渲不渲染"。据此，**否支和早退之后的代码也由 cond 控制**：

```jsx
{loading ? <Spinner/> : <Content/>}   // Content 渲不渲染由 loading 决定 ✅ 应有控制边
if (isVip) return <Gold/>;
return <Normal/>;                      // Normal 恰因 isVip 为假才渲染 ✅ 应有控制边
```

原实现只在 if 块/三元真支内 pcPush -- 否支和早退之后 pcStack 为空 -> 控制边丢失。"加载/空/内容三态切换"恰是用户最常指着问"这由谁控制"的场景。

**分两档修复**：

- **廉价档（必须）**：三元与 if-else **双支对称 push**--`cond ? A : B` 编译为 A、B 都在 `pcPush(cond$)` 窗口内求值（无论走哪支，渲染都受 cond 支配）：

```js
const __c = cond$;
pcPush(__ctx, __c); try { result = cond ? A : B; } finally { pcPop(__ctx); }
// A、B 内的 DOM 输出都带 cond 控制边
```

- **完整档（早退模式）**：`if (cond) return/throw/break/continue` 之后、同一作用域的**剩余语句**隐含被 cond 控制。Babel 检测分支以跳转语句结尾时，将后续语句包入 else 块（或维护"区域 P"，进入剩余部分时 union 进 pcStack）：

```js
// if (a) return X; if (b) return Y; return Z;
// Z 的 P = a$ ∪ b$（a 为假且 b 为假才执行 Z）
```

**验收反例**：`{loading ? <Spinner/> : <Content/>}` -> Content 节点必须有 loading 控制边；`if (err) return <Error/>; return <Main/>` -> Main 必须有 err 控制边。

### 1.5 机制 ⑤：DOM 收口（C2 逼出）

**为什么必须**：C2 说真相是 DOM → **只在写 DOM 那一刻记边**。不在赋值时、不在 render 时。

```ts
// onDomWrite 支持属性边（P0-1）：attrName 非空时记录的是元素属性边，目标节点 = 元素本身
function onDomWrite(domNode: Node, stamp: Stamp, attrName?: string) {
  if (!stamp || (stamp.D === 0n && stamp.P === 0n)) return;  // 双空短路
  for (const fieldId of expandBits(stamp.D)) recordEdge(fieldId, domNode, 'data', attrName);
  for (const fieldId of expandBits(stamp.P)) recordEdge(fieldId, domNode, 'control', attrName);
}

// ← P0-7 / #18 修正：边的生命周期。原方案边只增不减，节点卸载/字段变更后旧边残留 -> 图越来越脏。
//   按 DOM 节点存边集合，重 walk 时先清该节点旧边再记新边。
const edgeRegistry = new WeakMap<Node, Set<string>>();  // domNode -> edge key 集合

function recordEdge(fieldId: number, domNode: Node, type: 'data' | 'control', attrName?: string) {
  const key = `${fieldId}:${type}:${attrName ?? ''}`;
  let edges = edgeRegistry.get(domNode);
  if (!edges) { edges = new Set(); edgeRegistry.set(domNode, edges); }
  if (edges.has(key)) return;  // 幂等
  edges.add(key);
  graph.addEdge(fieldId, domNode, type, attrName);  // 写入绑定图 G
}

// 重 walk 某节点前清旧边（DOM 节点身份不变但内容/属性变了）
function clearEdges(domNode: Node) {
  const edges = edgeRegistry.get(domNode);
  if (edges) {
    for (const key of edges) graph.removeEdge(key, domNode);
    edges.clear();
  }
}
```



### 1.6 机制 ⑥：React 过缝（C2 + React 架构逼出）

**为什么必须**：React 的 render（同步算 element）和 commit（之后写 DOM）分离 → 写 DOM 时标签变量已随栈帧消失 → 标签必须搭 element 的便车跨过这道缝。

**过缝载体**：element 的 props 对象。render 期把标签存入 `stampMap(props)`（WeakMap），commit 期从 `stampMap.get(fiber.memoizedProps)` 读回。

**过缝载体**：element 的 props 对象。render 期把标签存入 `stampMap(props)`（WeakMap），commit 期从 `stampMap.get(fiber.memoizedProps)` 读回。

### 1.7 机制 ⑦：不透明函数的三段式恢复（撤掉三方库白名单）

**为什么必须**：lodash/immer/zod/react-query 内部是黑盒，插桩看不懂。原方案的"腿三：白名单插桩数据类 node_modules"是工程 hack--要枚举库、要配 Vite exclude、库版本一变就失效。第一性原理要求：**对任意不透明函数，不靠白名单也能恢复标签**。

**底层推导**：不透明函数 `f(a, b) -> result`，我们在边界已知输入标签（P0-3 参数通道），输出标签只可能是三种情况：

```ts
function recoverOpaqueReturn(result: any, args: any[], argTaints: bigint[]): bigint {
  // ① 身份匹配：result === 某个输入（引用相等）-> 精确继承该输入标签
  //    命中 _.identity / _.get 返回对象属性 / Object.assign 浅拷贝 等
  for (let i = 0; i < args.length; i++) {
    if (args[i] === result) return argTaints[i];
  }

  // ② 值指纹匹配：result 的值在"值->字段路径"索引中查到，与输入并集求交
  //    命中 _.get(user,'name') 返回 "Ada" -> 查索引得 {user.name} -> 与输入并集求交
  const inputUnion = argTaints.reduce((a, b) => a | b, 0n);
  if (inputUnion === 0n) return 0n;  // 输入都无标签，输出必无标签（短路）
  const fp = fingerprintLookup(result);  // 值指纹：原始值直查；对象递归属性查
  return fp & inputUnion;  // bigint 位交：只保留"值匹配 且 在输入中"的字段

  // ③ 都查不到 -> 隐式返回 0n（CUT），老实漏报
}
```

**三段式对任意不透明函数成立**--lodash/immer/zod/原生/WASM 一视同仁：


| 场景                                                  | 恢复路径                       | 结果                                   |
| --------------------------------------------------- | -------------------------- | ------------------------------------ |
| `_.identity(x)` / `_.get(obj,k)` 返回对象属性             | ① 身份匹配                     | 精确                                   |
| `_.get(user,'name')` 返回 "Ada"                       | ② 值指纹 ∩ 输入                 | 精确（"Ada" 唯一）                         |
| `_.get(obj,'greeting')` 返回 "hi"（字面量）                | ② 查不到                      | ∅ ✓ 无假阳性                             |
| `immer.produce(base, draft => { draft.x = api.y })` | ② 最终对象属性值 = api.y 值 -> 查索引 | 精确（无需 wrap produce、无需追踪 draft Proxy） |
| `zod.parse(obj)` 返回新对象                              | ② 属性值匹配                    | 精确                                   |
| `arr.map(cb)` 未 patch 时                             | ② 逐元素值匹配                   | 部分（变换值如 toUpperCase 查不到，腿二 patch 补）  |
| WASM 计算                                             | ② 输出值匹配                    | 部分                                   |


**精度分析**：

- ① 身份匹配：**精确**（sound & precise），无假阳性
- ② 值指纹 ∩ 输入：**碰撞时过近似**。仅当两个 API 字段值相同、且都在输入中、且只有一个真流到输出时，才误报。标记为 `value-match` 置信度（区别于 `taint`），可被 UI 降权显示
- ③ CUT：漏报，不假阳性

**消解的工程负担**：

- 撤掉腿三白名单（无需枚举 lodash/immer/zod/react-query）
- 撤掉 Vite `optimizeDeps.exclude` 配置（无需为插桩排除预打包）
- 撤掉 immer produce 包装（#16，Proxy 难题自动消解）
- 撤掉"库顶层缓存原生引用"的腿三兜底

**与腿二（原生替换）的关系**：腿二仍保留 `.map/.filter/.reduce` 等高频原生的**精确逐元素**传播（变换值如 `toUpperCase` 值指纹查不到，必须 patch）；其余一切不透明函数走三段式恢复。两者互补：腿二保高频精度，三段式保长尾覆盖。

**值指纹索引的构建**：在 `stampOrigin` 时同步建 `valueIndex: Map<primitiveValue, bigint>`（值 -> 字段位图）。对象属性值递归入索引。这是 §10 值指纹层的同一份数据，一物两用：既做不透明函数恢复，又做 DOM 端反查兜底。

**值指纹索引的构建**：在 `stampOrigin` 时同步建 `valueIndex: Map<primitiveValue, bigint>`（值 -> 字段位图）。对象属性值递归入索引。这是 §10 值指纹层的同一份数据，一物两用：既做不透明函数恢复，又做 DOM 端反查兜底。

### 1.8 机制 ⑧：纯方法并集传播（P0-9，文本上屏前最后一公里）

**为什么必须**：`name.toUpperCase()`、`price.toFixed(2)`、`bio.trim().slice(0,100)`、`tags.join(' / ')`--API 文本上屏前几乎必过这些 String/Number 方法。§1.7 三段式恢复对它们**失效**：`toUpperCase()` 把 "Ada" 变成 "ADA"，身份不匹配（①），值指纹也查不到 "ADA"（②，索引里只有 "Ada"）-> CUT -> **断流在最后一公里**。`toFixed` 一个方法就足以让电商类应用的价格边全灭，且 P1 验收用例当场失败。

**第一性原理**：传播规则由"我们对函数知道什么"决定。String/Number/Date/Math 的变换类方法，spec 写死**无副作用、输出仅依赖 receiver + args**--这是**纯度性质**，不是库枚举。既然纯，输出必依赖输入 -> `result$ = recv$ ∪ args$` 是 sound 的 may-近似。非纯方法（`Date.now`/`Math.random` 等访问外部状态的）仍走 §1.7。

**实现（codegen 规则，不 patch 原生）**：

```js
// 编译前
const s = user.name.toUpperCase();
const p = item.price.toFixed(2);

// 编译后（纯方法表命中）
const _recv = user.name;
const _recv$ = (_recv != null && (_recv[TAINT_FLAG] || frozenTainted.has(_recv))) ? slowGetProp(_recv, /*..*/) : 0n;
const s = _recv.toUpperCase();
const s$ = _recv$;                          // 纯方法：result$ = recv$ ∪ args$（此处无 args）

const _p = item.price;
const _p$ = ...;
const p = _p.toFixed(2);
const p$ = _p$;                             // toFixed(2) 的 2 是字面量，args$ = 0n
```

**纯度分类表**（spec 固定，工作量有限，非库枚举）：


| 类别                       | 纯方法（`result$ = recv$ ∪ args$`）                                                                                                                       | 非纯（走 §1.7）                                |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| String.prototype         | toUpperCase/toLowerCase/trim/trimStart/trimEnd/slice/substring/substr/split/replace/replaceAll/padStart/padEnd/concat/at/charCodeAt/repeat/normalize | -                                         |
| Number.prototype         | toFixed/toPrecision/toExponential/toString                                                                                                           | -                                         |
| String                   | fromCharCode/fromCodePoint                                                                                                                           | -                                         |
| Math                     | abs/floor/ceil/round/min/max/pow/sqrt/log/exp/sin/...（全系数学函数）                                                                                        | random（外部熵）                               |
| Date.prototype           | getFullYear/getMonth/getDate/getHours/.../toISOString（对固定 receiver 纯）                                                                                | now（静态，无 receiver）                        |
| Array.prototype（变换类，非变异） | join/slice/concat/flat/flatMap/includes/indexOf/find/findFilter                                                                                      | push/splice/sort/reverse（变异，走 §5.4 patch） |


**变异方法**（`push/splice/sort` 等）仍走 §5.4 原生补丁处理"写入侧"传播；纯变换方法用 codegen 并集规则。两者互补：codegen 覆盖 String/Number（高频、无副作用），patch 覆盖 Array 变异（需逐元素精确）。

**验收**：`items.map(i => i.price.toFixed(2))` 渲染 `<span>` 必须出 `price` 数据边。

---

---



## 第二章：三大悖论的第一性解法



### 2.1 Paradox 1 解法：放弃"值级护照"，改用"位置级护照"



#### 问题重述

`<h1>{data.user.name}</h1>` 编译为 `jsx("h1", { children: "Ada" })`。在 fiber 树中：

- HostComponent h1 fiber：`memoizedProps = { children: "Ada" }` → 对象 ✅
- HostText fiber（"Ada"）：`memoizedProps = "Ada"` → 字符串 ❌，WeakMap 不可用

如果我们在 h1 的 props 上存了标签，但在 commit 遍历时只检查 HostText fiber，就会找不到标签。如果在 HostText 的 memoizedProps 上存，WeakMap 会直接报错或静默失败。

更复杂的是多表达式：`<div>{a}{b}</div>` 编译为 `jsx("div", { children: ["Ada", 7] })`。React 创建两个 HostText fiber。如何区分哪个标签属于哪个文本节点？

#### 第一性证明

**命题**：标签的存储位置必须满足：(1) 键是对象（WeakMap 要求），(2) 在 commit 期可读，(3) 对于多表达式能区分。

**证明**：

- 约束 (1) 排除原始值 → 不能以 HostText 的 memoizedProps 为 key
- 约束 (2) 要求键在 commit 期仍存活 → WeakMap 是天然选择（GC 自动清理）
- 约束 (3) 要求标签的结构与 React children 结构对齐

**唯一解**：标签存储在**父 HostComponent fiber 的 props 对象**上，以**数组形式按 children 位置索引**对齐。HostText fiber 不独立存标签，而是在 commit 时从父 fiber 的标签数组中按子索引读取。

#### 具体实现

**编译期**：Babel 将 JSX 表达式包装，收集每个子表达式的标签，按位置存入数组。

```jsx
// 源码
<div>{a}{b}</div>
<div>{a}<span>{c}</span>{b}</div>

// 编译后
__runtime.jsx("div", { children: [a, b] }, [a$, b$])
__runtime.jsx("div", { children: [a, __runtime.jsx("span", { children: [c] }, [c$]), b] }, [a$, null, b$])
//                                                                                  ^^^^ 嵌套元素位置填 null，它有自己的标签
```

`**__runtime.jsx**` 的实现：

```ts
const stampMap = new WeakMap<object, JSXStamps>();  // props -> stamps（含 attrStamps + childStamps，见 §3.1）

function jsx(type: any, props: object, stamps: JSXStamps): ReactElement {
  // P0-1/P0-2：把带标签的 prop 写入 props 的 SM 影子并打标，子组件 props.name 走慢道恢复
  let anyTainted = false;
  for (const [k, s] of Object.entries(stamps.attrStamps)) {
    if (s !== 0n) { slowSetProp(props, k, s); anyTainted = true; }
  }
  if (stamps.childUnion !== 0n) { slowSetProp(props, 'children', stamps.childUnion); anyTainted = true; }
  if (anyTainted) ensureFlag(props);
  stampMap.set(props, stamps);  // childStamps 用于 commit 期多文本精确对齐
  return nativeJsx(type, props);
}
```

**Commit 期遍历**：从父 fiber 读取标签数组，按子 fiber 索引对齐。

```ts
function walkCommittedFibers(fiber: Fiber) {
  // 遍历 fiber 树
  if (isHostComponent(fiber) && fiber.stateNode) {
    const stamps = stampMap.get(fiber.memoizedProps);
    if (stamps) {
      // 遍历子 fiber，按索引对齐
      let child = fiber.child;
      let i = 0;
      while (child) {
        if (isHostText(child) && stamps[i] && !isStampEmpty(stamps[i])) {
          onDomWrite(child.stateNode, stamps[i]);
        }
        child = child.sibling;
        i++;
      }
    }
  }
  // 递归子节点
  let child = fiber.child;
  while (child) {
    walkCommittedFibers(child);
    child = child.sibling;
  }
}
```

**多表达式文本的对齐**：React 的 fiber 子节点顺序与 props.children 中 truthy 值的顺序一致。条件渲染 `{cond && <span/>}` 在 cond 为 false 时产生 false，React 不为其创建 fiber，所以 fiber 子节点会"跳过"这个位置。但我们的 stamps 数组是按 props.children 位置索引的，需要同步跳过 falsy 值。

```ts
function walkChildFibers(fiber: HostComponentFiber, stamps: Stamp[]) {
  const children = fiber.memoizedProps.children;
  const childArr = Array.isArray(children) ? children : [children];
  let child = fiber.child;
  let pi = 0;  // props.children 索引
  while (child) {
    // ← P1-10 修正：必须按 React 语义跳过，不是 JS falsy！
    //   React 只跳过 null/undefined/boolean；0、""、NaN 会渲染成 HostText fiber。
    //   若按 JS falsy 跳过，{count && <b/>} 在 count=0 时索引立即错位。
    while (pi < childArr.length && isReactNonRenderingChild(childArr[pi])) pi++;
    if (pi < childArr.length && stamps[pi] && !isStampEmpty(stamps[pi])) {
      if (isHostText(child)) {
        onDomWrite(child.stateNode, stamps[pi]);
      }
    }
    child = child.sibling;
    pi++;
  }
}

// React 语义：只有 null/undefined/boolean 不创建 fiber；0/""/NaN 会渲染成文本
function isReactNonRenderingChild(v: any): boolean {
  return v === null || v === undefined || typeof v === 'boolean';
}
```

**自证正确性**：

- 始终以 props 对象为 WeakMap key → 永远不会遇到原始类型 → P1 消除
- 标签数组按 props.children 位置索引 → 多表达式对齐精确
- 嵌套 JSX 元素在父级标签数组中为 null → 各自独立存标签，无重叠
- 条件渲染的 falsy 跳过使 fiber 子节点与 props 子节点对齐 → 索引正确



#### Fragment 与嵌套数组的展开对齐

**问题**：React Fragment (`<>...</>`) 和嵌套数组会对 fiber 树进行**子节点展开**（flattening），导致父级 fiber 的子节点与 props.children 结构不一致。

```jsx
<div>
  <Header />
  <>
    <Text>{a}</Text>
    <Text>{b}</Text>
  </>
  <Footer />
</div>
```

- JSX 层：div 的 props.children = `[HeaderElement, FragmentElement, FooterElement]`（长度 3）
- Fiber 层：Fragment 的 children 被展开到父级，div 的子 fiber 为 `[Header, Text(a), Text(b), Footer]`（长度 4）

如果按索引对齐，stamps 数组 `[null, null, null]`（3 个条目）与 4 个子 fiber 错位，P1 的解法在此场景下失效。

**解法**：在 commit 遍历时，**展开 JSX 元素树中的 Fragment 和嵌套数组节点**，使其与 fiber 树的扁平结构对齐。

```ts
function walkChildFibers(fiber: HostComponentFiber, stamps: Stamp[]) {
  const children = fiber.memoizedProps.children;
  const childArr = Array.isArray(children) ? children : [children];

  // 展开 JSX 树中的 Fragment 和嵌套数组，与 fiber 树对齐
  const { flatElements, flatStamps } = flattenJSXWithStamps(childArr, stamps);

  let child = fiber.child;
  let i = 0;
  while (child && i < flatElements.length) {
    while (i < flatElements.length && isReactNonRenderingChild(flatElements[i])) i++;
    if (i >= flatElements.length) break;

    if (isHostText(child) && flatStamps[i] && !isStampEmpty(flatStamps[i])) {
      onDomWrite(child.stateNode, flatStamps[i]);
    }
    child = child.sibling;
    i++;
  }
}

function flattenJSXWithStamps(
  elements: any[],
  stamps: Stamp[]
): { flatElements: any[]; flatStamps: Stamp[] } {
  const flatElements: any[] = [];
  const flatStamps: Stamp[] = [];

  for (let i = 0; i < elements.length; i++) {
    const el = elements[i];

    if (Array.isArray(el)) {
      // 嵌套数组展开：{items.map(i => <Item/>)} 产生数组
      // 每个元素使用自己的 stamps（来自各自的 __runtime.jsx 调用）
      for (const item of el) {
        flatElements.push(item);
        flatStamps.push(emptyStamp());  // 元素有自己的 stamps，此处填 null
      }
    } else if (isReactFragment(el)) {
      // Fragment 展开：递归展开 Fragment 的 children
      const fragChildren = Array.isArray(el.props.children)
        ? el.props.children
        : [el.props.children];
      const fragStamps = stampMap.get(el.props) ?? [];
      const { flatElements: fe, flatStamps: fs } =
        flattenJSXWithStamps(fragChildren, fragStamps);
      flatElements.push(...fe);
      flatStamps.push(...fs);
    } else {
      flatElements.push(el);
      flatStamps.push(stamps[i] ?? emptyStamp());
    }
  }

  return { flatElements, flatStamps };
}
```

**自证正确性**：

- JSX 元素树展开后与 fiber 树的扁平结构一致 → 对齐正确
- Fragment 自身的 stamps 来自其 `__runtime.jsx(Fragment, ...)` 调用 → 递归展开不丢失
- 嵌套数组（`.map` 返回值）的子元素各有自己的 JSX 调用 → 各自独立存标签
- 展开逻辑在 commit 期执行，不影响 render 期性能



### 2.2 Paradox 2 解法：词法 ctx 捕获为主，动态栈为辅



#### 问题重述（第二轮深审：词法/动态混用导致 render props / 事件处理器系统性失效）

原方案 §2.2 用 `currentCtx()`（动态栈顶）作为主通道，假设"回调执行时其所属组件的 ctx 仍在栈顶"。这个假设在以下场景系统性破裂--共同点是**回调在定义组件的 ctx 窗口之外执行**：

```jsx
// 场景 1：render props（react-table / Formik / downshift / rc-* 全家桶）
<Table renderRow={(row) => <div>{row.name}</div>} />
//   时序：Page render -> enterComponent push -> 创建 <Table>（renderRow 仅被定义，未调用）
//        -> return -> exitComponent pop，栈空 -> React 渲染 Table（库代码不 push ctx）
//        -> Table 调用 renderRow -> 此刻 contextStack 空 -> 插桩走全局回退 / currentCtx() 崩溃

// 场景 2：事件处理器里 setState（§2.4 已暴露但未根治）
onClick={() => setName(user.name)}
//   点击时栈空，属性读写词法 ctx，wrappedSetter 读 currentCtx() 拿不到 -> 写进去的标签读不出来
```



#### 第一性证明

**命题**：taint 上下文是**词法属性**（这段代码属于哪个组件？），不是**动态属性**（现在谁在跑？）。

**证明**：

- 组件 A 内定义的箭头函数（render props / 事件处理器），无论被谁、在何时调用，其代码**词法上属于 A** -> 应读写 A 的 ctx
- `currentCtx()` 取栈顶，反映的是"当前正在执行的组件"--但 render props 被库调用时，栈顶是库或空，不是定义者 A
- 词法 `__ctx` 是组件函数体内的常量，被嵌套箭头函数闭包捕获 -> 无论何时调用，读写的都是定义者 A 的 ctx -> 与词法归属一致

**解**：**词法捕获为主通道**，`currentCtx()` 降级为兜底。`__ctx` 本就是组件函数体内的词法常量，所有词法位于组件体内的插桩（含嵌套箭头、render props 回调、事件处理器）直接闭包捕获它。

#### 具体实现

```ts
// ctx 对象经闭包存活，pop 后仍可被回调引用
interface RenderContext {
  instance: object;            // P1-11：useRef 持久化容器
  hookCounter: number;         // P1-11：运行时 hook 计数器
  lastReturnTaint: bigint;     // 函数返回值标签通道
  argTaints: bigint[] | null;  // P0-3：函数参数通道
  pcStack: bigint[];           // 条件栈
  returnTaint: bigint;
}

const contextStack: RenderContext[] = [];

export function enterComponent(instance: object): RenderContext {
  const ctx: RenderContext = { instance, hookCounter: 0, lastReturnTaint: 0n, argTaints: null, pcStack: [], returnTaint: 0n };
  contextStack.push(ctx);
  return ctx;
}

export function exitComponent(ctx: RenderContext) {
  contextStack.pop();
  // ctx 对象作为闭包变量继续存活，被 render props / 事件处理器后续引用
  const parent = contextStack[contextStack.length - 1];
  if (parent) parent.lastReturnTaint = ctx.returnTaint;
}

// 动态 currentCtx() 降级为兜底：只服务于无法词法确定归属的场景（helper 函数、P0-3 参数通道）
export function currentCtx(): RenderContext | undefined {
  return contextStack[contextStack.length - 1];
}
```

**词法捕获的组件编译**（render props / 事件处理器闭包捕获 `__ctx`）：

```js
function Page() {
  const __ref = useRef({}); const __ctx = __runtime.enterComponent(__ref.current);
  try {
    return __runtime.jsx(Table, {
      renderRow: (row) => {
        // ← 词法捕获外层 __ctx：即使此刻栈空，ctx 经闭包存活
        const _n = row.name;
        const _n$ = (row != null && (row[TAINT_FLAG] || frozenTainted.has(row)))
          ? __runtime.slowGetProp(row, 'name', __ctx) : 0n;
        if (__ctx) __ctx.lastReturnTaint = _n$;        // 写词法 ctx，不写 global
        return __runtime.jsx("div", { children: _n }, { attrStamps:{}, childStamps:[_n$] }, null, __ctx);
        //                                                               key=null    ↑ ctx 显式传入
      }
    }, {}, null, __ctx);
  } finally { __runtime.exitComponent(__ctx); }
}
```

**属性读 / 条件流**：词法位于组件体内 -> 直接用闭包的 `__ctx`，不调 `currentCtx()`。

**函数返回值通道**：

- **词法可确定归属**（组件体内调用 helper）：调用点用闭包 `__ctx`，helper 入口也用词法 ctx（Babel 传参）-> 一致
- **无法词法确定**（helper 被多组件调用，或库内部调用）：`helper` 用 `currentCtx()` 取栈顶兜底 + 全局回退

**wrappedSetter 统一**（场景 2 根治）：setter 在组件体内创建，闭包捕获 `__ctx` -> 读写两侧都是同一个词法 ctx，不再"写词法读动态"。

#### 自证正确性

- ctx 对象被 pop 后**作为闭包变量继续存活**，JS 单线程保证任意时刻只有一个执行流在写它 -> 无串味
- 同一组件的多个实例各有各的 `__ctx`（每次 render 新建）-> 实例间无串味
- render props 被库在任何时机调用，写侧读侧都是同一个词法 ctx -> 一致
- wrappedSetter 闭包捕获 `__ctx` -> 事件处理器读写两侧统一
- 动态 `currentCtx()` 仅兜底无法词法确定的场景（helper、参数通道）
- 此修正同时吸收第一轮 #20：`__runtime.jsx` 的 ctx 由调用点显式传入，词法外场景传 null 走 `P = 0n` 兜底，不再 `currentCtx()!` 崩溃



### 2.3 Paradox 3 解法：Symbol 标记快慢道 + Nullish 守卫### 2.3 Paradox 3 解法：Symbol 标记快慢道 + Nullish 守卫



#### 问题重述

Babel 插桩将所有属性访问 `obj.name` 替换为 `readProp$(obj, 'name')`，导致：

- V8 的 Inline Cache 失效：每个属性读变成函数调用 + WeakMap 哈希查找
- 95%+ 的对象从未接触 API 数据（本地状态、配置、样式等），却支付了完整插桩开销
- 预期开销：Jalangi 的 26-96×，甚至 Linvail 的 ~5000×
- **额外致命问题**：当 `data.user` 为 `null`/`undefined` 时（可选链 `data.user?.name`、数据未加载），`_u[TAINT_FLAG]` 会直接抛出 `TypeError: Cannot read properties of null (reading 'Symbol(...)')`。业务代码中 `null`/`undefined` 是常态（数据未加载、接口返回空），插桩代码必须对其免疫。



#### 第一性证明

**命题**：必须区分"可能携带 API 标签的对象"和"确定不携带标签的对象"，对前者走慢道（WeakMap 查找），对后者走原生快道。

**证明**：

- 对象是否有标签，在 `stampOrigin` 时已知（打标时就知道）
- 如果可以在对象上做一个**不可枚举、不影响 JS 语义**的标记，就可以在属性读时快速判断
- 标记必须满足：(1) 不污染 `Object.keys`/`for...in`，(2) 不影响 `JSON.stringify`，(3) 不影响 React reconciliation，(4) 不与用户代码冲突

**唯一解**：使用 `Symbol` 作为标记键。Symbol 天然不可枚举、不参与序列化、不与字符串键冲突。

#### 具体实现

```ts
// 全局污染标记 Symbol
const TAINT_FLAG = Symbol.for('__taint_stamp_flag__');
// ← P1-9：冻结对象（Apollo/RTK）打不上 TAINT_FLAG，单独注册
const frozenTainted = new WeakSet<object>();

// stampOrigin 的完整实现见 §1.1（后序并集 + visited 循环保护 + 路径分段 key）。
// 此处只展示 P1-9 的 isExtensible 守卫逻辑（§2.3 讨论冻结对象的核心）：
//   if (Object.isExtensible(obj)) { obj[TAINT_FLAG] = true; }  // extensible：打快道标记
//   else { frozenTainted.add(obj); }                            // 冻结：进 WeakSet，读取侧双判
// SM 是 WeakMap 不需要改对象，影子始终能建；TAINT_FLAG 打不上时退 frozenTainted。
```

**Babel 插桩的属性读**（快慢道内联 + Nullish 守卫 + 冻结对象双判）：

```js
// 编译前
const name = data.user.name;

// 编译后
const _tmp = data.user;                           // 原生属性读
const _tmp$ = (_tmp != null && (_tmp[TAINT_FLAG] || frozenTainted.has(_tmp)))  // ← Nullish 守卫：null/undefined 直接走快道
  ? __runtime.slowGetProp(_tmp, 'user', __ctx)    // 慢道：WeakMap 查找
  : 0n;                                           // 快道：直接返回 ∅

const _name = _tmp.name;                          // 原生属性读
const _name$ = (_name != null && typeof _name === 'object' && (_name[TAINT_FLAG] || frozenTainted.has(_name)))
  ? __runtime.slowGetProp(_name, 'name', __ctx)   // 嵌套属性读（如果需要）
  : 0n;
```

**Nullish 守卫规则**：所有 `obj[TAINT_FLAG]` 检查前必须加 `obj != null` 守卫。`!= null` 同时检查 `null` 和 `undefined`，且比 `!== null && !== undefined` 更紧凑。可选链 `data.user?.name` 的编译同理——`?.` 前的对象为 null/undefined 时直接跳过慢道。

**冻结对象双判**（P1-9）：`obj[TAINT_FLAG] || frozenTainted.has(obj)`。对 extensible 非污染对象，`obj[TAINT_FLAG]` 为 undefined（falsy）-> 短路求值到 `frozenTainted.has(obj)` -> false -> 快道。`frozenTainted` 只含被 `stampOrigin` 接触过的冻结对象（极少数），WeakSet 查询对非污染 extensible 对象几乎总是 miss。性能可接受；若需极致性能可配 `ignoreFrozen:true` 跳过冻结对象追踪（接受 Apollo/RTK 数据召回损失）。

**为什么** `obj[TAINT_FLAG]` **不破坏 V8 优化**：

1. **非污染对象**：从未被 `stampOrigin` 接触 → 隐藏类中不包含 `TAINT_FLAG` 槽 → `obj[TAINT_FLAG]` 返回 `undefined` → V8 将其优化为单态 IC（所有非污染对象共享同一隐藏类）
2. **污染对象**：来自 `stampOrigin` 递归遍历 → 每个对象被添加 `TAINT_FLAG` 槽 → 隐藏类发生变化 → 但**污染对象之间**隐藏类可能不同（取决于原始对象结构）→ 对污染对象的 `TAINT_FLAG` 读取可能是多态的
3. **但污染对象占比 <5%** → 绝大多数访问走快道 → 总体开销从 26-96× 压到 2-5×

**自证正确性**：

- `Symbol.for('...')` 是全局注册的，跨模块共享，不产生多副本
- Symbol 键不参与 `Object.keys`/`for...in`/`JSON.stringify` → 不影响 JS 语义
- React 的 reconciliation 基于 props 的浅比较 → 额外 Symbol 属性不影响比较结果
- `Object.freeze` **会**阻止添加 Symbol 属性（P1-9 修正原错误声明）-> stampOrigin 必须先 `Object.isExtensible` 守卫，冻结对象退化为 `frozenTainted` WeakSet + 读取侧双判；不修则 Apollo dev deep-freeze / RTK Immer autofreeze 的数据一打标就抛 TypeError
- 非污染对象始终保持原生属性访问 → V8 IC 正常工作



### 2.4 Paradox 4：React Hooks 状态黑洞（新增）



#### 问题重述

以下代码在首次渲染后，`name` 的标签会**彻底丢失**：

```js
const [name, setName] = useState(data.user.name);
```

**原因**：React 将 `useState` 的初始值存入 Fiber 内部的 Hook 链表（`fiber.memoizedState`）。当组件重渲染时，`useState` 直接从 Hook 链表中取出该值返回——**不走属性读、不走函数返回值通道**。我们的插桩代码在 Hook 链表这条路上完全不可见。

对于 Redux (`useSelector`)、Zustand、`useActionState`、`useReducer` 等所有状态管理 Hook，同样的问题都存在。

**影响面**：致命。任何有状态的 React 组件在第二次渲染时，API 数据的标签全部断流。这不是召回率的问题——这是**核心流断裂**，直接导致系统不可用。

#### 第一性证明

**命题**：Hook 状态的标签必须在 Hook 状态持久化时同步持久化，在 Hook 状态读取时同步恢复。

**证明**：

- React Hook 的状态存储是**框架内部机制**（Fiber 链表），我们的插桩对其不可见
- 标签随组件函数执行而存在，但 Hook 状态的生命周期**跨越多次渲染**
- 因此必须有**与 Hook 状态同生命周期的标签存储**

**解**：利用 `useRef` 创建跨渲染的持久化容器（挂到 RenderContext 上），以**运行时自增的 hookIndex** 为键存储标签。Babel 在组件函数顶部注入 `useRef`，然后拦截每个 Hook 调用来持久化/恢复标签。**不依赖** `React.__SECRET_INTERNALS`，完全通过公开 API 实现。

**P1-11 关键修正**：instance 挂在 RenderContext（`ctx.instance`）而非独立 useRef 变量--这样 **custom hook**（普通函数，Babel 不会注入 `__instance`）通过 `currentCtx()` 取到调用者组件的 instance 和自增 hookIndex，与 React 自身的 hook 链表同序。hookIndex 是**每次渲染清零的运行时计数器** `ctx.hookCounter++`，不是编译期静态编号（编译器看不到 call chain 里的 hook 序列）。

**P1-12 关键修正**：Hook 实参的标签由 Babel **显式传参**（`useState(arg, arg$)`），不依赖 `lastReturnTaint` 隐式捕获--否则 `useState(a + b)` 会丢 `a$`，事件处理器里 `setCount(5)` 会残留旧标签导致假阳性。

#### 具体实现

**Babel 注入**：在每个组件函数顶部插入：

```js
const __ref = useRef({});
const __ctx = __runtime.enterComponent(__ref.current);  // instance 挂到 ctx 上
```

**全局 Hook 标签存储**：

```ts
// instance -> hookIndex -> Stamp（跨渲染持久化）
const hookStampMap = new WeakMap<object, Stamp[]>();

// 全局回退通道：事件处理器/异步回调中可能没有渲染上下文
let __lastReturnTaintGlobal = 0n;
```

`**useState` 包装**（P0-5：只包装不求值，让 React 算 resolved；P1-12：arg$ 显式传参）：

```ts
function useState<T>(
  [value, setValue]: [T, (v: T | ((prev: T) => T)) => void],
  argTaint: bigint,        // ← P1-12：实参标签显式传入，不靠 lastReturnTaint
  ctx: RenderContext
): [T, (v: T | ((prev: T) => T)) => void] {
  const instance = ctx.instance;
  const hookIndex = ctx.hookCounter++;   // ← P1-11：运行时自增，custom hook 同序

  let stamps = hookStampMap.get(instance);
  if (!stamps) { stamps = []; hookStampMap.set(instance, stamps); }

  // 首次渲染：用显式传入的 argTaint 持久化初始值标签
  if (stamps[hookIndex] === undefined) {
    stamps[hookIndex] = { D: argTaint, P: 0n };
  }

  // 将持久化的标签写入上下文，使下游代码可以读到
  ctx.lastReturnTaint = stamps[hookIndex].D;

  // ← P0-5 修正：只包装，不求值！让 React 用队列最新 prev 算 resolved。
  //   原方案提前用闭包旧 value 求 updater，连续 setCount(c=>c+1) 结果错误--改错了用户程序。
  const wrappedSetter = (nv: T | ((prev: T) => T)) => {
    setValue((prev) => {
      const resolved = typeof nv === 'function' ? (nv as (p: T) => T)(prev) : nv;
      // 标签 = 旧标签 ∪ 新值标签；新值标签从当前上下文/全局回退取
      const cur = contextStack[contextStack.length - 1];
      const newTaint = cur?.lastReturnTaint ?? __lastReturnTaintGlobal;
      stamps![hookIndex] = { D: (stamps![hookIndex]?.D ?? 0n) | newTaint, P: 0n };
      return resolved;
    });
  };

  return [value, wrappedSetter];
}
```

**全局回退通道**：事件处理器和异步回调中可能没有渲染上下文，属性读插桩时需同时写入两个通道：

```js
// 属性读插桩（同时写入上下文和全局回退）
const _val = obj.name;
const _taint = (obj != null && (obj[TAINT_FLAG] || frozenTainted.has(obj)))
  ? slowGetProp(obj, 'name', __ctx) : 0n;
if (__ctx) __ctx.lastReturnTaint = _taint;
else __lastReturnTaintGlobal = _taint;  // 回退：事件处理器中无上下文
```

**Babel 编译示例**（P1-12：arg$ 显式传参）：

```js
// 编译前
function Profile({ data }) {
  const [name, setName] = useState(data.user.name);
  return <h1>{name}</h1>;
}

// 编译后
function Profile({ data }) {
  const __ref = useRef({});
  const __ctx = __runtime.enterComponent(__ref.current);  // instance 挂 ctx
  try {
    // data.user.name -> 属性读插桩
    const _u = data.user;
    const _u$ = (_u != null && (_u[TAINT_FLAG] || frozenTainted.has(_u))) ? slowGetProp(_u, 'user', __ctx) : 0n;
    const _init = _u.name;
    const _init$ = (_u != null && (_u[TAINT_FLAG] || frozenTainted.has(_u))) ? slowGetProp(_u, 'name', __ctx) : 0n;

    const [name, setName] = __runtime.useState(
      React.useState(_init),                   // 调用原始 Hook
      _init$,                                   // ← P1-12：实参标签显式传入
      __ctx                                     // ctx（含 instance + hookCounter）
    );

    return __runtime.jsx("h1", { children: name }, [name$]);
  } finally {
    __runtime.exitComponent(__ctx);
  }
}
```

**custom hook 兼容**（P1-11）：`useUserData()` 内部调 `useState`--它是普通函数，Babel 不注入 `__ref`/`__ctx`，但内部 Hook 包装通过 `currentCtx()` 取到**调用者组件**的 `ctx.instance` 和 `ctx.hookCounter`，与 React 自身 hook 链表完全同序。无需编译器分析 call chain。

`**useReducer`、`useRef`、`useContext`、`useCallback**`：用同样模式包装。

- `useReducer`：dispatch 包装类似 setter，reducer 的返回值标签从 `lastReturnTaint` 取
- `useRef`：初始值标签在首次渲染时持久化到 `hookStampMap`
- `useContext`：返回原始值时从 Provider value 槽恢复标签（Provider value 已是污染对象）
- `useCallback`：同 useMemo，缓存 `[fn, fn$]`，命中时回填 `lastReturnTaint`
**自证正确性**：
- `useRef({}).current` 在组件整个生命周期中保持同一引用 → 天然是标签的持久化 Key
- 不依赖 `React.__SECRET_INTERNALS` → 兼容所有 React 16.8+ 版本
- 首次渲染时从 `__ctx.lastReturnTaint` 捕获初始值标签 → 正确
- 重渲染时从 `hookStampMap` 恢复标签 → 标签不丢失
- setter 包装捕获新值标签并持久化 → 状态更新后标签正确
- 全局回退通道处理事件处理器和异步回调 → 覆盖完整
- `useReducer` 同理包装 dispatch 即可

---



## 第三章：核心数据结构



### 3.1 全局类型定义

```ts
// ===== 字段 ID 与位图 =====
// 每个 API 字段分配一个全局唯一的整数 ID（1, 2, 3, ...）
// 标签集合 = bigint 位图，第 i 位为 1 表示字段 i 在集合中
// 并集 = a | b，O(1) 字级运算

const fieldRegistry = new Map<string, number>();  // "GET /users/:id.user.name" → 42
let nextFieldId = 1;

function getOrCreateFieldId(path: string): number {
  let id = fieldRegistry.get(path);
  if (!id) { id = nextFieldId++; fieldRegistry.set(path, id); }
  return id;
}

function bit(id: number): bigint { return 1n << BigInt(id); }
function union(a: bigint, b: bigint): bigint { return a | b; }
function has(a: bigint, id: number): boolean { return (a & bit(id)) !== 0n; }
function isEmpty(a: bigint): boolean { return a === 0n; }

// ===== Stamp = 一个表达式的完整标签 =====
interface Stamp {
  D: bigint;   // 数据标签（值从哪来）
  P: bigint;   // 控制标签（被哪些条件罩着）
}

function emptyStamp(): Stamp { return { D: 0n, P: 0n }; }
function isStampEmpty(s: Stamp): boolean { return s.D === 0n && s.P === 0n; }

// ===== 影子内存（SM） =====
interface Shadow {
  fields: Map<string | symbol, bigint>;  // 每个属性槽的标签
}

const SM = new WeakMap<object, Shadow>();

function ensureShadow(obj: object): Shadow {
  let sh = SM.get(obj);
  if (!sh) { sh = { fields: new Map() }; SM.set(obj, sh); }
  return sh;
}

// ===== 污染标记 =====
const TAINT_FLAG = Symbol.for('__taint_stamp_flag__');
const frozenTainted = new WeakSet<object>();  // ← P1-9：冻结对象（Apollo/RTK）打不上 TAINT_FLAG

// ensureFlag：给对象打 TAINT_FLAG，冻结则进 frozenTainted（属性写/对象字面量/props 打标共用）
function ensureFlag(obj: object) {
  if (Object.isExtensible(obj)) {
    try { obj[TAINT_FLAG] = true; } catch {}
  } else {
    frozenTainted.add(obj);
  }
}

// ===== React 过缝 =====
// ← P0-1/P0-2：stamps 扩展为 { attrStamps, childStamps }，属性边 + props 打标
interface JSXStamps {
  attrStamps: Record<string, Stamp>;    // prop 名 -> Stamp{D,P}（含 src/href/value 等）
  childStamps: (Stamp | null)[];       // 第二轮：完整 Stamp{D,P}，在表达式求值点捕获（文本级控制边）
  childUnion: bigint;                    // children 并集（穿过中间组件时用）
}
const stampMap = new WeakMap<object, JSXStamps>();     // props -> stamps
const hookStampMap = new WeakMap<object, Stamp[]>();   // instance -> hookIndex -> Stamp（跨渲染持久化）

// ===== 上下文栈（P2 解法） =====
interface RenderContext {
  instance: object;        // ← P1-11：useRef 持久化容器，custom hook 共享
  hookCounter: number;     // ← P1-11：每次渲染清零的运行时 hook 计数器
  lastReturnTaint: bigint;
  argTaints: bigint[] | null;  // ← P0-3：函数参数通道（调用点写、入口读后置 null）
  pcStack: bigint[];
  returnTaint: bigint;
}

const contextStack: RenderContext[] = [];

// ===== 全局回退通道（事件处理器/异步回调中无上下文时使用） =====
let __lastReturnTaintGlobal = 0n;

// ===== 边注册表（P0-7/#18：边的生命周期，节点卸载/变更后清旧边） =====
const edgeRegistry = new WeakMap<Node, Set<string>>();  // domNode -> edge key 集合

// ===== 值指纹索引（§1.7 不透明函数恢复 + §10 DOM 反查共用）=====
// stampOrigin 时同步构建：原始值 -> 字段位图。对象属性值递归入索引。
const valueIndex = new Map<string | number | boolean, bigint>();

function indexValue(v: unknown, fieldId: number) {
  if (v === null || v === undefined) return;
  if (typeof v === 'object') return;  // 对象按 SM 走，不入值索引
  const prev = valueIndex.get(v as any) ?? 0n;
  valueIndex.set(v as any, prev | bit(fieldId));
}

// 三段式恢复：身份匹配 -> 值指纹∩输入 -> CUT
function recoverOpaqueReturn(result: any, args: any[], argTaints: bigint[]): bigint {
  for (let i = 0; i < args.length; i++) {
    if (args[i] === result) return argTaints[i] ?? 0n;  // ① 身份匹配：精确
  }
  const inputUnion = argTaints.reduce((a, b) => a | b, 0n);
  if (inputUnion === 0n) return 0n;
  if (typeof result === 'object' || result === null) return 0n;  // 对象返回值靠子属性匹配
  const fp = valueIndex.get(result) ?? 0n;  // ② 值指纹
  return fp & inputUnion;  // 与输入并集求交，过滤碰撞
}
```



### 3.2 数据流总览

```
                           fieldRegistry
                           (string → int)
                                │
                                ▼
┌─────────────┐   stampOrigin   ┌──────────────┐
│ API 响应对象  │ ──────────────→ │ SM (WeakMap)  │
│ (JSON.parse) │  逐字段打标     │ object → Shadow│
└─────────────┘  标记 TAINT_FLAG │  fields: Map   │
                                └──────┬───────┘
                                       │
         ┌─────────────────────────────┤
         │ Babel 插桩（快慢道）          │
         ▼                             ▼
   非污染对象                    污染对象
   obj[TAINT_FLAG]              obj[TAINT_FLAG]
   → undefined                  → true
   → 跳过 SM 查找               → SM.get(obj).fields.get(k)
   → 标签 = 0n                  → 标签 = 字段位图
         │                             │
         └─────────┬───────────────────┘
                   ▼
           JSX 表达式求值
           __runtime.jsx(type, props, stamps[])
                   │
                   ▼
           stampMap.set(props, stamps[])
                   │
         ┌─────────┤
         │ Hook 状态 │ useRef 容器 + hookStampMap
         │ 持久化   │ useState/useReducer 包装
         │         │ 跨渲染保持标签不丢失
         └─────────┤
                   ▼ (React render→commit)
           onCommitFiberRoot 增量剪枝遍历
           stampMap.get(fiber.memoizedProps)
           → Fragment 展开 + 子索引对齐
                   │
                   ▼
           onDomWrite(domNode, stamp)
           → 画 data/control 边
```

---



## 第四章：编译期插桩（Babel Plugin）



### 4.1 插桩原则

1. **值不变**：插桩代码不影响原始值的身份和类型
2. **快慢道**：所有属性读走快慢道内联，不引入函数调用开销
3. **上下文穿透**：所有标签操作通过 `__ctx` 参数，不依赖全局变量
4. **不可达即 CUT**：看不懂的语法构造（Proxy、eval、WASM）直接丢弃标签，不猜测



### 4.2 插桩清单


| AST 节点     | 插桩策略                                                                                                                                               |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| **组件函数入口** | `const __ref = useRef({}); const __ctx = enterComponent(__ref.current)`（instance 挂 ctx，P1-11）；嵌套箭头/回调闭包捕获 `__ctx`（词法 ctx 主通道）；**第三轮 P2-8 组件识别**见下表 |


**组件识别判定表**（第三轮 P2-8，Babel 注入 enterComponent 依赖此规则）：


| 函数形式                                                         | 识别         | 理由                                     |
| ------------------------------------------------------------ | ---------- | -------------------------------------- |
| `function Foo(){...}` / `const Foo = () => {...}`（大写，返回 JSX） | ✅ 注入       | 标准组件                                   |
| `forwardRef((props, ref) => {...})`                          | ✅ 注入**内层** | 内层才是渲染体；不识别则 hooks 包装（P4）错位破坏 hooks 顺序 |
| `memo(fn)` / `memo(forwardRef(...))`                         | ✅ 注入**内层** | 同上，剥到渲染函数                              |
| `export default function(){...}`（匿名默认导出）                     | ✅ 注入       | 默认导出常是组件                               |
| 大写命名但返回 null 的普通函数                                           | ✅ 注入（无害）   | 多一对 push/pop，无副作用                      |
| 小写命名返回 JSX 的 helper                                          | ❌ 不注入（无害）  | 按词法 ctx 修正，它闭包捕获外层 __ctx，本就是期望行为       |
| 事件处理器 / render props 回调                                      | ❌ 不注入      | 闭包捕获外层 __ctx（第二轮词法修正）                  |


**原则**：forwardRef/memo **必须**识别（否则 hooks 顺序破坏）；其余"宁可错杀（多注入）不可漏判"对 forwardRef/memo 之外的函数成立。
| **变量声明** `const x = e` | 额外声明 `const x$ = e$`（影子变量） |
| **变量赋值/复合赋值/++--** `x = e` / `x += e` / `x++` | **新增**（第二轮 P1）：左值 Identifier 同步影子 `x$ = e$` / `x$ \|= e$` / `++` 标签不变；否则重赋值后 `x$` 陈旧 |
| **属性读** `a.b` / `a[k]` | 内联快慢道 + Nullish 守卫 + 冻结双判：先读值，再 `(a != null && (a[TAINT_FLAG] \|\| frozenTainted.has(a))) ? slowGet(a,'b',__ctx) : 0n`；计算键 `a[k]` 时 `D \|= k$`（第二轮 P2-5）；写 `__ctx.lastReturnTaint` |
| **属性写** `a.b = e` | **替换语义**（第三轮 P0-4 再修正）：无条件 `slowSet(a,'b',e$)`（e$ 为 0n 删槽）；`if (e$ !== 0n) ensureFlag(a)` | mutate API 对象/表单重置后旧标签失效，不假阳性 |
| `**delete a.b**` | **新增**（第二轮 P1）：`if (a[TAINT_FLAG] \|\| frozenTainted.has(a)) slowDelete(a,'b')` 清影子槽，否则后续读 undefined 仍带旧标签 -> 假阳性 |
| **对象字面量** `{k: e}` | **新增**（P0-4）：带非零影子的属性逐一 `slowSet`，任一非零即给新对象打 TAINT_FLAG |
| **展开** `{...a}` / `[...a]` | **新增**（P0-4）：逐槽拷贝源影子；`{...a, k:v}` 源键继承 + `k` 槽写 `v$` |
| **算术/拼接** | 结果标签 = 操作数标签的并集 |
| **比较/布尔化**（`===`/`<`/`!`） | result.D = 0n, result.P ∪= 操作数 D ∪ P |
| `**&&` / `\|\|`（值语义）** | **新增**（第二轮 P1）：`D = a$ ∪ b$`，`P \|= a$`；不是布尔化，结果是操作数本身 |
| **方法调用** `x.m(...args)` | **新增**（第二轮 P0-9）：纯方法白名单（String/Number/Date/Math 变换类，§1.8）命中则 `result$ = recv$ ∪ (∪ args$)`；非纯走 §1.7 三段式恢复 |
| **if/else/三元/switch** | pcPush/pcPop 包装，通过词法 `__ctx` 操作；labeled break/continue 需跨层 pop（配穷举单测） |
| **while/for** | 每轮 pcPush/pcPop |
| **for-of / for-in** | **新增**（第二轮 P1）：循环变量逐轮从集合槽位初始化 `u$ = SM(coll).fields.get(String(i)) ?? ∅`（for-of）/ `SM(obj).fields.get(k) ?? ∅`（for-in） |
| **函数声明/表达式** | 入口取词法 `__ctx`（Babel 传参）或 `currentCtx()` 兜底；return 前写 `__ctx.lastReturnTaint` |
| **函数调用** `f(a, b)` | **参数通道 + 不透明恢复**（P0-3/§1.7）：调用前 `__ctx.argTaints = [a$, b$]`；f 已插桩则入口读、return 写；f 不透明则 `recoverOpaqueReturn`（身份 -> 值指纹∩输入 -> CUT） |
| **Hook 调用** `useState(x)` | 包装为 `__runtime.useState(React.useState(x), x$, __ctx)`（P1-11/P1-12） |
| **JSX 元素** `<T key={k} p={e}>{c}</T>` | **扩展**（P0-1/P0-2 + 第二轮）：`jsx(type, props, key, { attrStamps, childStamps }, __ctx)` -- **key 必须透传**（automatic runtime 第三参是 key，吞了会破坏列表 reconciliation）；childStamps 元素类型为完整 `Stamp{D,P}` 在**表达式求值点捕获**（§5.5） |
| **不透明构造** | Proxy/Reflect/eval/WASM：函数调用走 §1.7；非调用表达式 -> 0n（CUT），值指纹层兜底 |

### 4.3 编译示例

```jsx
// ===== 源码 =====
function Profile({ data }) {
  const name = data.user.name;
  const label = "LV." + data.user.level;
  if (data.user.isVip) {
    return <div><h1>{name}</h1><em>VIP</em></div>;
  }
  return <div><h1>{name}</h1></div>;
}

// ===== 编译后（示意，实际更复杂） =====
// ===== 编译后（示意，实际更复杂） =====
function Profile({ data }) {
  const __ref = useRef({});
  const __ctx = __runtime.enterComponent(__ref.current);  // instance 挂 ctx（P1-11）
  try {
    // data.user.name -- 快慢道 + Nullish 守卫 + 冻结双判（P1-9）
    const _u = data.user;
    const _u$ = (_u != null && (_u[TAINT_FLAG] || frozenTainted.has(_u))) ? __runtime.slowGetProp(_u, 'user', __ctx) : 0n;
    const name = _u.name;
    const name$ = (_u != null && (_u[TAINT_FLAG] || frozenTainted.has(_u))) ? __runtime.slowGetProp(_u, 'name', __ctx) : 0n;

    // data.user.level
    const _u2 = data.user;
    const _u2$ = (_u2 != null && (_u2[TAINT_FLAG] || frozenTainted.has(_u2))) ? __runtime.slowGetProp(_u2, 'user', __ctx) : 0n;
    const level = _u2.level;
    const level$ = (_u2 != null && (_u2[TAINT_FLAG] || frozenTainted.has(_u2))) ? __runtime.slowGetProp(_u2, 'level', __ctx) : 0n;

    const label = "LV." + level;
    const label$ = level$;  // 字面量 "LV." 的标签 = 0n, 并集 = level$

    // data.user.isVip
    const _u3 = data.user;
    const _u3$ = (_u3 != null && (_u3[TAINT_FLAG] || frozenTainted.has(_u3))) ? __runtime.slowGetProp(_u3, 'user', __ctx) : 0n;
    const isVip = _u3.isVip;
    const __isVip$ = (_u3 != null && (_u3[TAINT_FLAG] || frozenTainted.has(_u3))) ? __runtime.slowGetProp(_u3, 'isVip', __ctx) : 0n;
    const __c = __isVip$;  // 条件的标签

    if (isVip) {
      __runtime.pcPush(__ctx, __c);
      try {
        __ctx.returnTaint = 0n;
        // ← P0-1/P0-2 + 第二轮：jsx(type, props, key, stamps, ctx)；childStamps 是 Stamp{D,P} 求值点捕获
        const _nameStamp = __runtime.captureChildStamp(name$, __ctx);  // D=name$, P=pcUnion(__ctx)
        return __runtime.jsx("div", {
          children: [
            __runtime.jsx("h1", { children: name }, null, { attrStamps: {}, childStamps: [_nameStamp] }, __ctx),
            __runtime.jsx("em", { children: "VIP" }, null, { attrStamps: {}, childStamps: [{ D: 0n, P: __c }] }, __ctx)
            //                                                          ^^ 控制边（push 窗口内捕获）
          ]
        }, null, { attrStamps: {}, childStamps: [null, null] }, __ctx);
      } finally {
        __runtime.pcPop(__ctx);
      }
    }

    __ctx.returnTaint = 0n;
    const _nameStamp2 = __runtime.captureChildStamp(name$, __ctx);
    return __runtime.jsx("div", {
      children: [__runtime.jsx("h1", { children: name }, null, { attrStamps: {}, childStamps: [_nameStamp2] }, __ctx)]
    }, null, { attrStamps: {}, childStamps: [null] }, __ctx);
  } finally {
    __runtime.exitComponent(__ctx);
  }
}```

---

## 第五章：运行时（Runtime）

### 5.1 源打标（origin.ts）

```ts
// ← P1-14 修正：补全 XHR 系（axios 走 XHR）、res.text()、res.clone()、流式消费

// ===== 1. fetch：拦截 .json() / .text()，并 patch clone 出的 Response =====
const _fetch = window.fetch;
window.fetch = async function (input: RequestInfo, init?: RequestInit) {
  const res = await _fetch.call(window, input, init);
  const sourceId = resolveSourceId(input);
  patchResponse(res, sourceId);
  // ← P1-14：res.clone() 产生的新 Response 不带补丁，必须一并 patch
  const _clone = res.clone.bind(res);
  res.clone = function () { const c = _clone(); patchResponse(c, sourceId); return c; };
  return res;
};

function patchResponse(res: Response, sourceId: string) {
  const _json = res.json.bind(res);
  res.json = async function () { const d = await _json(); stampOrigin(d, sourceId); return d; };
  const _text = res.text.bind(res);
  res.text = async function () { const t = await _text(); return t; };  // text 返回原始字符串，按值指纹层兜底
  // ← P1-14：流式消费 res.body.getReader()
  const _body = Object.getOwnPropertyDescriptor(res, 'body');
  if (_body && res.body) {
    const reader = res.body.getReader();
    // 流式数据：collect chunks -> 合并后 stampOrigin（或增量打标）
    // 简化：留给值指纹层处理，污点层对流式承认盲区
  }
}

// ===== 2. XMLHttpRequest（axios / GraphQL-over-XHR 大量使用）=====
const _open = XMLHttpRequest.prototype.open;
const _send = XMLHttpRequest.prototype.send;
XMLHttpRequest.prototype.open = function (method, url, ...rest) {
  this.__sourceId = resolveSourceId({ method, url });
  return _open.call(this, method, url, ...rest);
};
XMLHttpRequest.prototype.send = function (body) {
  this.addEventListener('load', () => {
    try {
      const ct = this.getResponseHeader('content-type') || '';
      if (ct.includes('json')) {
        const data = JSON.parse(this.responseText);
        stampOrigin(data, this.__sourceId);
      }
    } catch { /* 非 JSON 或解析失败，忽略 */ }
  });
  return _send.call(this, body);
};

// ===== 3. JSON.parse（网络数据靠边界打标；带 reviver 时先过原生再 stampOrigin）=====
const _parse = JSON.parse;
JSON.parse = function (text: string, reviver?: (k: any, v: any) => any) {
  const data = reviver ? _parse.call(this, text, reviver) : _parse.call(this, text);
  // 仅对疑似网络数据打标（调用栈启发式含 fetch/XHR 处理器，或显式标记）
  if (looksLikeNetworkData()) stampOrigin(data, currentSourceId());
  return data;
};

// 递归逐字段打标：后序并集 + visited 循环保护 + 路径分段 key（完整实现见 §1.1）
//   - 槽位 = 自身 id ∪ 子树并集（后序冒泡），整对象流出时边指向叶子字段
//   - visited WeakSet 防循环引用（手动接入的 store/ORM 实体可能有环）
//   - 路径 key = JSON.stringify([sourceId, ...segments])，字段名含 "." 不歧义
//   - isExtensible 守卫（P1-9）+ length 槽（P1-13）+ 数组通配 []（#17）
function stampOrigin(obj: object, sourceId: string, path: string[] = [], visited = new WeakSet()): bigint {
  if (obj == null || typeof obj !== 'object' || visited.has(obj)) return 0n;
  visited.add(obj);
  ensureFlag(obj);
  const sh = ensureShadow(obj);
  const isArr = Array.isArray(obj);
  for (const [k, v] of Object.entries(obj)) {
    const seg = isArr ? '[]' : k;
    const id = getOrCreateFieldId(JSON.stringify([sourceId, ...path, seg]));
    let slot = union(sh.fields.get(k) ?? 0n, bit(id));
    if (v && typeof v === 'object') slot |= stampOrigin(v, sourceId, [...path, seg], visited);
    sh.fields.set(k, slot);
    indexValue(v, id);
  }
  if (isArr) {
    let lu = 0n; for (const t of sh.fields.values()) lu |= t;
    sh.fields.set('length', lu);
  }
  let all = 0n; for (const v of sh.fields.values()) all |= v;
  return all;
}
```

### 5.2 上下文栈（context.ts）

```ts
const contextStack: RenderContext[] = [];

export function enterComponent(instance: object): RenderContext {
  // ← P1-11：instance 来自 useRef，挂到 ctx 上供 custom hook 共享；hookCounter 每次渲染清零
  const ctx: RenderContext = {
    instance,
    hookCounter: 0,
    lastReturnTaint: 0n,
    argTaints: null,          // ← P0-3：函数参数通道
    pcStack: [],
    returnTaint: 0n,
  };
  contextStack.push(ctx);
  return ctx;
}

export function exitComponent(ctx: RenderContext) {
  contextStack.pop();
  const parent = contextStack[contextStack.length - 1];
  if (parent) parent.lastReturnTaint = ctx.returnTaint;
}

export function currentCtx(): RenderContext | undefined {
  return contextStack[contextStack.length - 1];
}

export function pcPush(ctx: RenderContext, taint: bigint) {
  if (taint !== 0n) ctx.pcStack.push(taint);
}

export function pcPop(ctx: RenderContext) {
  ctx.pcStack.pop();
}

export function pcUnion(ctx: RenderContext): bigint {
  return ctx.pcStack.reduce((a, b) => a | b, 0n);
}
```

### 5.3 影子内存读写（shadow.ts）

```ts
export function slowGetProp(obj: object, key: string | symbol, ctx: RenderContext): bigint {
  const sh = SM.get(obj);
  if (!sh) return 0n;
  return sh.fields.get(key) ?? 0n;
}

export function slowSetProp(obj: object, key: string | symbol, taint: bigint) {
  // ← 第三轮 P0-4 再修正：替换语义。taint 为 0n 时删除槽位，不是写 0n。
  //   槽位内容始终 = 最后一次写入的值的标签；mutate/重置后旧标签失效，不假阳性。
  const sh = SM.get(obj);
  if (taint === 0n) {
    if (sh) sh.fields.delete(key);   // 删槽：read 时 get(key) ?? 0n -> 0n
    return;
  }
  ensureShadow(obj).fields.set(key, taint);
}

// slowDelete：delete a.b 的清槽入口（与 slowSetProp 替换语义同一语义）
export function slowDeleteProp(obj: object, key: string | symbol) {
  const sh = SM.get(obj);
  if (sh) sh.fields.delete(key);
}
```

### 5.4 原生方法替换（native-patch.ts）

```ts
// ← 第二轮 P2-7：补丁幂等 marker，HMR 重复执行 runtime 模块时不二次包装
function patchOnce(proto: any, name: string, impl: Function) {
  const orig = proto[name];
  if ((orig as any).__taint_patched__) return;
  const wrapped = impl.bind(null, orig);
  (wrapped as any).__taint_patched__ = true;
  proto[name] = wrapped;
}
```



// Array.prototype.map -- 逐元素传播标签（变异/集合类原生走 patch，变换类走 §1.8 codegen）
patchOnce(Array.prototype, 'map', function (_orig, cb, thisArg) {
  const out = new Array(this.length);
  const ctx = currentCtx();
  let anyTainted = false;  // P0-6：追踪是否有任一元素带标签
  for (let i = 0; i < this.length; i++) {
    if (!(i in this)) continue;  // 保持稀疏数组语义
    if (ctx) ctx.lastReturnTaint = 0n;
    out[i] = cb.call(thisArg, this[i], i, this);
    if (ctx && ctx.lastReturnTaint !== 0n) {
      ensureFlag(out);  // P0-6：给 out 打 TAINT_FLAG，否则后续 out[i] 读走快道返回 0n
      slowSetProp(out, String(i), ctx.lastReturnTaint);
      anyTainted = true;
    }
  }
  if (ctx) ctx.lastReturnTaint = anyTainted ? (SM.get(out)?.fields.get('whole') ?? 0n) : 0n;
  return out;
});

// String/Number/Date/Math 的纯变换方法（toUpperCase/toFixed/trim/slice/...）
//   **不 patch**：由 §1.8 codegen 规则处理（result$ = recv$ ∪ args$）。
//   patch 只用于"变异方法"（push/splice/sort/reverse）和"逐元素精确"需求（map/filter/reduce）。
//   纯方法走 codegen 更轻、无需 patch 时机担忧。

// 变异方法示例：sort 的控制依赖（comparator 的 P 并入数组 whole.P）
patchOnce(Array.prototype, 'sort', function (_orig, comparator) {
  const ctx = currentCtx();
  if (ctx) ctx.lastReturnTaint = 0n;
  const result = comparator ? _orig.call(this, (a, b) => {
    if (ctx) ctx.lastReturnTaint = 0n;
    const r = comparator(a, b);
    // comparator 返回值的 P 并入数组 whole（排序顺序依赖 comparator）
    return r;
  }) : _orig.call(this);
  return result;
});

// P0-6 验收铁律：正例 users.map(u => u.name) 渲染  必须出边；
//   反例 [1,2,3].map(x => x*2)（无 API 数据）不能误出边。
// 同理 patch：.filter, .reduce, .slice, .flat, .flatMap, .concat, .find
//   每个都必须在写入任一槽位前 ensureFlag(out)，否则传播成果静默归零。
// Object.assign, Object.fromEntries, structuredClone, JSON.parse 同理。
// 每个配 spec 级单测（稀疏数组、thisArg、species 等）

```

### 5.5 JSX 过缝（react-bridge.ts）

```ts
// ← P0-1/P0-2 + 第二轮：stamps 扩展为 { attrStamps, childStamps }。
//   childStamps 元素类型是完整 Stamp {D, P}，在表达式求值点捕获（见下），不是 bigint。
//   props 成为一等污点公民：带标签 prop 写入 props 的 SM 影子，子组件走慢道恢复（P0-2）。

export function jsx(
  type: any,
  props: object,
  key: any,                    // ← 第二轮 P2-1：key 必须透传，不能吞（破坏列表 reconciliation）
  stamps: JSXStamps,
  ctx: RenderContext | null    // ← 词法 ctx 显式传入（第二轮：不调 currentCtx()）
): ReactElement {
  let anyTainted = false;
  for (const [k, s] of Object.entries(stamps.attrStamps)) {
    if (s && s.D !== 0n) { slowSetProp(props, k, s.D); anyTainted = true; }
  }
  if (stamps.childUnion !== 0n) { slowSetProp(props, 'children', stamps.childUnion); anyTainted = true; }
  if (anyTainted) ensureFlag(props);
  // ← 第二轮 P2-8：stamps 全空时跳过 WeakMap 写，省每次渲染数千次写入
  if (anyTainted || stamps.childStamps.some(s => s && (s.D !== 0n || s.P !== 0n))) {
    stampMap.set(props, stamps);
  }
  return nativeJsx(type, props, key);  // key 转发给原生 jsx
}

// ← 第三轮 P2-7：dev 模式 JSX 编译目标是 jsxDEV(type, props, key, isStaticChildren, source, self)。
//   自定义 __runtime.jsxDEV 必须原样透传前六参，否则：
//   - source（__source 文件名/行号）丢 -> React key/act 警告丢失位置信息
//   - self/owner 丢 -> "component was created by X" 警告退化
//   实现：jsxDEV(type, props, key, isStaticChildren, source, self, stamps, ctx) 透传前六参给 native jsxDEV。
export function jsxDEV(type, props, key, isStaticChildren, source, self, stamps, ctx) {
  // 同 jsx() 的 props 打标 + stampMap 写入逻辑
  stampPropsAndMap(props, stamps);
  return nativeJsxDEV(type, props, key, isStaticChildren, source, self);
}

// ← 第二轮修正：childStamps 是 Stamp{D,P}，在表达式求值点捕获，jsx() 只透传不归一化。
//   原方案在 jsx() 时刻取 pcUnion(currentCtx()!) -> 文本级控制边丢失：
//   {isVip && name} 的 pcPush/pop 发生在子表达式求值期间，jsx() 在其之后调用 -> pcStack 已空。
//   改为求值点捕获：Babel 在求值 {expr} 时立即 { D: expr$, P: pcUnion(__ctx) }，存入 childStamps。
function captureChildStamp(exprTaint: bigint, ctx: RenderContext | null): Stamp {
  return { D: exprTaint, P: ctx ? pcUnion(ctx) : 0n };  // 无 ctx（词法外）时 P=0n 兜底
}

// normalizeStamp 废弃：不再在 jsx() 时刻归一化。childStamps 已是完整 Stamp。
// 原 normalizeStamp 的 undefined 崩溃（只判 null/0n，undefined 落入 {D:undefined}）随之消除。

export function getStamps(props: object): JSXStamps | undefined {
  return stampMap.get(props);
}
```

**文本级控制边正确性**（第二轮）：`{isVip && name}` 编译为：

```js
// isVip && name 求值时：pcPush(isVip$) -> 求值 name -> captureChildStamp(name$, __ctx) 此时 P 含 isVip -> pcPop
const _v = isVip && name;
const _stamp = __runtime.captureChildStamp(_v$ /*=name$*/, __ctx);  // P 此时含 isVip（push 窗口内）
__runtime.jsx("div", { children: _v }, null, { attrStamps:{}, childStamps:[_stamp] }, __ctx);
```

name 出数据边（D=name$），isVip 出控制边（P 含 isVip$），**不**出 isVip 数据边（D 不含 isVip$）。元素级 `{isVip && <em/>}` 同理正确（em 的 jsx 在 push 窗口内）。

### 5.6 Commit 收口--增量剪枝遍历（sink.ts）

```ts
// ← P0-8 修正：DevTools hook 由 React DevTools 扩展安装，React 本身不装。
//   没装扩展的环境里原方案静默全灭。必须在 React 加载之前自注入最小 hook 实现，
//   让 React 主动来注册（参照 react-devtools-inline 做法）。
export function installCommitHook() {
  const hook = ensureDevToolsHook();  // 自注入，不依赖扩展
  const orig = hook.onCommitFiberRoot;
  hook.onCommitFiberRoot = (rendererID: number, root: any) => {
    orig?.(rendererID, root);
    walkCommittedFibersIncremental(root.current);
  };
}

// 自注入最小 hook（若已存在则复用，处理多 rendererID 与版本差异）
function ensureDevToolsHook() {
  const w = window as any;
  if (!w.__REACT_DEVTOOLS_GLOBAL_HOOK__) {
    w.__REACT_DEVTOOLS_GLOBAL_HOOK__ = {
      supportsFiber: true,
      inject: () => 1,
      onCommitFiberRoot: () => {},
      onCommitFiberUnmount: () => {},
      onScheduleFiberRoot: () => {},
      renderers: new Map(),
    };
  }
  return w.__REACT_DEVTOOLS_GLOBAL_HOOK__;
}

// ===== 增量剪枝遍历 =====
// ← P0-7 修正：lanes 在 commit 后已重置（createWorkInProgress 时清为 NoLanes），
//   current 树上的 lanes 描述"未来排队的工作"不是"本次变更"。
//   用 alternate 身份比较做增量键：jsx() 每次渲染新建 props 对象，
//   bailout 子树 props 身份不变 -> 跳过；重渲染子树 props 是新对象 -> 处理。
function walkCommittedFibersIncremental(fiber: any) {
  if (!hasUpdates(fiber)) return;  // 剪枝

  // 2. 处理当前 HostComponent 节点
  if (isHostComponent(fiber) && fiber.stateNode) {
    processHostComponent(fiber);
  }

  // 3. 递归子节点
  let child = fiber.child;
  while (child) {
    walkCommittedFibersIncremental(child);
    child = child.sibling;
  }
}

// ← P0-7：用 props 身份比较，不用 lanes
function hasUpdates(fiber: any): boolean {
  if (fiber.alternate === null) return true;  // 新挂载
  // 重渲染过的 fiber props 是新对象；bailout 子树 props 身份不变
  return fiber.alternate.memoizedProps !== fiber.memoizedProps
      || fiber.alternate.memoizedState !== fiber.memoizedState;
}

// ← P0-1/P0-7：处理 HostComponent -- 属性边 + 文本子节点边 + 边生命周期
function processHostComponent(fiber: any) {
  const domNode = fiber.stateNode;
  const props = fiber.memoizedProps;
  const stamps = stampMap.get(props);
  const sh = SM.get(props);  // props 已是污染对象（jsx 打标时建）

  // ← P0-7/#18：先清该节点旧边，再记新边（节点身份不变但内容/属性变了）
  clearEdges(domNode);

  // ← P0-1：属性边 -- 对元素本身按 attrStamps 逐属性记边（src/href/value/className/disabled）
  if (stamps && stamps.attrStamps) {
    for (const [attr, st] of Object.entries(stamps.attrStamps)) {
      if (st && !isStampEmpty(st)) {
        onDomWrite(domNode, st, attr);  // attr 名作为边元数据，st 已是完整 Stamp{D,P}
      }
    }
  }
  // props 上其他带标签的槽（如穿过中间组件时 children 并集等）也记边
  if (sh) {
    for (const [k, taint] of sh.fields) {
      if (k === 'children' || taint === 0n) continue;
      if (!stamps?.attrStamps?.[k]) onDomWrite(domNode, { D: taint, P: 0n }, k);
    }
  }

  // 文本子节点边（精确按位置对齐）
  if (stamps && stamps.childStamps && stamps.childStamps.length > 0) {
    walkChildFibersWithStamps(fiber, stamps.childStamps);
  }
}

// ===== Fragment 展开对齐 =====
function walkChildFibersWithStamps(fiber: HostComponentFiber, childStamps: (Stamp | null)[]) {
  const children = fiber.memoizedProps.children;
  const childArr = Array.isArray(children) ? children : [children];
  const { flatElements, flatStamps } = flattenJSXWithStamps(childArr, childStamps);

  let child = fiber.child;
  let i = 0;
  while (child && i < flatElements.length) {
    while (i < flatElements.length && isReactNonRenderingChild(flatElements[i])) i++;
    if (i >= flatElements.length) break;
    if (isHostText(child) && flatStamps[i] && !isStampEmpty(flatStamps[i])) {
      onDomWrite(child.stateNode, flatStamps[i]);
    }
    child = child.sibling;
    i++;
  }
}
```



function flattenJSXWithStamps(
  elements: any[],
  stamps: (Stamp | null)[]
): { flatElements: any[]; flatStamps: Stamp[] } {
  const flatElements: any[] = [];
  const flatStamps: Stamp[] = [];
  for (let i = 0; i < elements.length; i++) {
    const el = elements[i];
    if (Array.isArray(el)) {
      for (const item of el) {
        flatElements.push(item);
        flatStamps.push(emptyStamp());  // 元素有自己的 stamps
      }
    } else if (isReactFragment(el)) {
      const fragChildren = Array.isArray(el.props.children) ? el.props.children : [el.props.children];
      const fragStamps = stampMap.get(el.props)?.childStamps ?? [];
      const { flatElements: fe, flatStamps: fs } = flattenJSXWithStamps(fragChildren, fragStamps);
      flatElements.push(...fe);
      flatStamps.push(...fs);
    } else {
      flatElements.push(el);
        flatStamps.push(stamps[i] ?? emptyStamp());  // 已是 Stamp，无需归一化
    }
  }
  return { flatElements, flatStamps };
}

function onDomWrite(domNode: Node, stamp: Stamp, attrName?: string) {
  if (isStampEmpty(stamp)) return;
  for (const fieldId of expandBits(stamp.D)) recordEdge(fieldId, domNode, 'data', attrName);
  for (const fieldId of expandBits(stamp.P)) recordEdge(fieldId, domNode, 'control', attrName);
}

// ← P0-7/#18：边的生命周期
function recordEdge(fieldId: number, domNode: Node, type: 'data' | 'control', attrName?: string) {
  const key = `${fieldId}:${type}:${attrName ?? ''}`;
  let edges = edgeRegistry.get(domNode);
  if (!edges) { edges = new Set(); edgeRegistry.set(domNode, edges); }
  if (edges.has(key)) return;
  edges.add(key);
  graph.addEdge(fieldId, domNode, type, attrName);
}
function clearEdges(domNode: Node) {
  const edges = edgeRegistry.get(domNode);
  if (edges) { for (const key of edges) graph.removeEdge(key, domNode); edges.clear(); }
}

```

---

## 第六章：跟着一个值走完全程

以 `GET /users/42` 返回 `{ user: { name: "Ada", level: 7, isVip: true } }` 为例，追踪 `user.name` 的完整生命周期。

```

═══════════════════════════════════════════════════════════════════
阶段 1：海关盖戳
═══════════════════════════════════════════════════════════════════

fetch("/users/42")
  → 被 patch 的 fetch 拦截
  → res.json() 被 patch
  → stampOrigin(data, "GET /users/:id")

```
 data 对象被标记 TAINT_FLAG = true
 fieldRegistry（路径分段 key，第三轮）: ["GET /users/:id","user","name"] -> id=1
 SM.get(data).fields（后序并集，第三轮）:
   "user" -> bit(user自身) | bit(1) | bit(2) | bit(3)   ← 自身 id ∪ 子树叶子并集
                整对象流出时（JSON.stringify(user)）边指向全部叶子字段
 
 data.user 对象被标记 TAINT_FLAG = true
 SM.get(data.user).fields（后序并集）:
   "name"  -> bit(1)                       (user.name 叶子)
   "level" -> bit(2)                       (user.level 叶子)
   "isVip" -> bit(3)                       (user.isVip 叶子)
```

═══════════════════════════════════════════════════════════════════
阶段 2：代码执行（Babel 插桩 + 快慢道）
═══════════════════════════════════════════════════════════════════

function Profile({ data }) {
  const __ref = useRef({}); const __ctx = __runtime.enterComponent(__ref.current);  // push 上下文

  // const name = data.user.name;
  const _u = data.user;
  //    data[TAINT_FLAG] → true → 走慢道
  //    slowGetProp(data, 'user', __ctx) → SM(data).fields.get('user') → bit(1)|bit(2)|bit(3)
  const _u$ = bit(1) | bit(2) | bit(3);

  const name = _u.name;
  //    _u[TAINT_FLAG] → true → 走慢道
  //    slowGetProp(_u, 'name', __ctx) → SM(data.user).fields.get('name') → bit(1)
  const name$ = bit(1);

  // 类似地，level$ = bit(2), isVip$ = bit(3)

═══════════════════════════════════════════════════════════════════
阶段 3：JSX 表达式求值
═══════════════════════════════════════════════════════════════════

  return {name} // {name} 的标签 = name$ = bit(1) LV.{level} // {level} 的标签 = level$ = bit(2) {isVip && *VIP*} // 条件栈 push isVip = bit(3);

  // 编译后（jsx(type, props, key, stamps, ctx)；childStamps 是 Stamp{D,P} 求值点捕获）：
  __runtime.jsx("div", {
    children: [
      __runtime.jsx("h1", { children: name }, null, { attrStamps:{}, childStamps:[captureChildStamp(bit(1),__ctx)] }, __ctx),
      //                                                            ^^^^ {D: bit(1), P: 0}
      __runtime.jsx("span", { children: "LV." + level }, null, { attrStamps:{}, childStamps:[captureChildStamp(bit(2),__ctx)] }, __ctx),
      //                                                                 ^^^^ {D: bit(2), P: 0}
      isVip
        ? (() => {
            __runtime.pcPush(__ctx, bit(3));
            try {
              return __runtime.jsx("em", { children: "VIP" }, null,
                { attrStamps:{}, childStamps:[captureChildStamp(0n,__ctx)] }, __ctx);
              //                                          ^^^^ {D: 0, P: bit(3)}（push 窗口内捕获）
            } finally { __runtime.pcPop(__ctx); }
          })()
        : false,
      // 注意：false 在 React 中被跳过，不创建 fiber
    ]
  }, null, { attrStamps:{}, childStamps:[null, null, null] }, __ctx);  // 父级 stamps 为 null

  // stampMap:
  //   h1.props  → [{D: bit(1), P: 0}]
  //   span.props → [{D: bit(2), P: 0}]
  //   em.props  → [{D: 0, P: bit(3)}]

═══════════════════════════════════════════════════════════════════
阶段 4：React commit 收口
═══════════════════════════════════════════════════════════════════

onCommitFiberRoot 回调触发 → walkCommittedFibers:

  div fiber (HostComponent)
    memoizedProps = { children: [h1Element, spanElement, emElement] }
    stamps = stampMap.get(div.memoizedProps) = [null, null, null]
    → 都是 null，不记边

```
子 fiber h1 (HostComponent)
  memoizedProps = { children: "Ada" }
  stamps = stampMap.get(h1.memoizedProps) = [{D: bit(1), P: 0}]
  → 子 fiber HostText("Ada") 在索引 0
  → stamps[0] = {D: bit(1), P: 0}
  → onDomWrite(h1文本节点, {D: bit(1), P: 0})
  → 画边: "GET /users/:id.user.name" ──data──▶ h1文本节点

子 fiber span (HostComponent)
  memoizedProps = { children: "LV.7" }
  stamps = stampMap.get(span.memoizedProps) = [{D: bit(2), P: 0}]
  → onDomWrite(span文本节点, {D: bit(2), P: 0})
  → 画边: "GET /users/:id.user.level" ──data──▶ span文本节点

子 fiber em (HostComponent)
  memoizedProps = { children: "VIP" }
  stamps = stampMap.get(em.memoizedProps) = [{D: 0, P: bit(3)}]
  → onDomWrite(em文本节点, {D: 0, P: bit(3)})
  → 画边: "GET /users/:id.user.isVip" ──control──▶ em文本节点
```

═══════════════════════════════════════════════════════════════════
最终绑定图 G:
  user.name  ──data──▶  文本节点 "Ada"
  user.level ──data──▶  文本节点 "LV.7"
  user.isVip ──control──▶ 文本节点 "VIP"
═══════════════════════════════════════════════════════════════════

```

---

## 第七章：性能模型

### 7.1 开销分析（逐层）


| 层级               | 操作                                                          | 非污染对象开销                           | 污染对象开销                 |
| ---------------- | ----------------------------------------------------------- | --------------------------------- | ---------------------- |
| 属性读              | `obj.name` + `obj[TAINT_FLAG] || frozenTainted.has(obj)` 双判 | ~1.2×（Symbol 读 + 偶尔 WeakSet miss） | + WeakMap 查找           |
| 属性写              | `if (e$ !== 0n) ensureFlag + slowSet`                       | ~1.0×（e$ 为 0n 跳过）                 | + WeakMap 写入           |
| 对象字面量            | 逐属性检查 + ensureFlag                                          | ~1.1×（新增，P0-4）                    | + WeakMap 写入           |
| 算术/拼接            | `a$ | b$`（bigint |）                                         | ~1.0×（0n | 0n = 0n）               | ~1.0×（bigint | 是 O(1)） |
| 函数调用             | 清/读 `__ctx.lastReturnTaint` + argTaints 写入                  | ~1.1×（新增参数通道）                     | ~1.1×                  |
| 条件栈              | push/pop 数组 + try/finally 包裹                                | ~1.1×（try/finally 全量包裹，#25）       | ~1.1×                  |
| 纯方法调用 `x.m(...)` | codegen `result$ = recv$ ∪ args$`（§1.8，不 patch 原生）          | ~1.0×（recv$ 为 0n 时短路）             | + bigint 并集            |
| JSX              | `stampMap.set` + props 打标                                   | ~1.1×（stamps 全空时仍建空对象）            | + WeakMap 写入           |
| Commit           | `stampMap.get` + alternate 身份增量遍历                           | ~1.0×（剪枝跳过未变更子树）                  | + WeakMap 读取           |
| stampOrigin      | 递归遍历响应                                                      | --                                | 瞬时阻塞（大响应，#25）          |


### 7.2 总体开销估算（修正 #25，偏诚实）

- **非污染对象路径**（95%+ 的代码）：~2-4× 开销（Symbol 双判 + 影子变量赋值 + try/finally 包裹 + 对象字面量插桩）
- **污染对象路径**（<5% 的代码）：+ WeakMap 查找开销
- **Commit 遍历**：~O(变更节点数) 而非 O(全部节点数)，打字输入等高频微更新仅处理变更的少量节点
- **大响应打标**：stampOrigin 递归有瞬时阻塞，大列表/深嵌套需分片或异步化
- **综合**：预计 ~5-10× 总体开销（原估 3-8× 偏乐观，try/finally + 字面量插桩未计入），仍远低于 Jalangi 的 26-96×

> **go/no-go**：P0 阶段必须在真实中型 app 上实测"Vite 冷启动 + 交互帧率"，不能只看微基准。

### 7.3 关键优化

1. **bigint 位图**：标签并集 = `a | b`，O(1) 字级运算，无需创建新 Set 对象
2. **双空短路**：`isStampEmpty(stamp)` 在 commit 时过滤掉 99% 的 DOM 写（框架内部写）
3. **Symbol 快慢道**：95%+ 的属性读不走 WeakMap（frozenTainted 双判对非污染对象几乎总 miss）
4. **增量 Commit 剪枝**：用 alternate props 身份比较跳过未变更子树（不用 lanes，P0-7），打字输入等高频更新仅处理 O(10) 个节点
5. **WeakMap GC**：组件卸载时 props 对象被 GC -> stampMap/edgeRegistry 条目自动消失 -> 零内存管理负担
6. **dev-only**：所有插桩代码不进生产产物
7. **通配路径**（#17）：数组元素用 `items[].name` 共享 ID，避免千条列表膨胀 fieldRegistry

---

## 第八章：边界条件对策

### 8.1 React 特有


| 边界                                     | 风险                   | 对策                                                                                                                     |
| -------------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| **dev 冻结 element/props**               | 致命                   | stampMap 是 WeakMap，不修改 element，完全不被冻结影响                                                                                |
| **HostText 原始 memoizedProps**          | 致命（P1）               | 标签存父 HostComponent 的 props，commit 时按子索引对齐                                                                              |
| **React Hooks 状态黑洞**                   | 致命（P0-5/P1-11/P1-12） | useRef 持久化（挂 ctx.instance）+ hookCounter 自增 + arg$ 显式传参；setter 只包装不求值（P0-5）；custom hook 经 currentCtx 共享 instance（P1-11） |
| **属性边整条通道缺失**                          | 致命（P0-1）             | stamps 扩展 attrStamps；commit 期对元素本身按属性记边（src/href/value/className/disabled）                                             |
| **props 传递断流**                         | 致命（P0-2）             | jsx() 把带标签 prop 写入 props 的 SM 影子并打 TAINT_FLAG，子组件 props.name 走慢道自动恢复                                                   |
| **函数参数通道缺失**                           | 致命（P0-3）             | ctx.argTaints 通道：调用点写、入口读后置 null；原始值过函数边界不死                                                                            |
| **属性写条件写反 + 对象字面量未插桩**                 | 致命（P0-4）             | 属性写按值驱动 `if (e$ !== 0n)`；对象字面量/展开逐槽 slowSet + ensureFlag                                                               |
| **DevTools hook 不存在**                  | 致命（P0-8）             | React 加载前自注入最小 hook（ensureDevToolsHook），不依赖浏览器扩展                                                                       |
| **lanes 增量剪枝前提不成立**                    | 致命（P0-7）             | 用 alternate props 身份比较代替 lanes（commit 后 lanes 已重置）；补边生命周期（先清旧边再记新边）                                                    |
| **useState setter 改语义**                | 致命（P0-5）             | 只包装不求值，让 React 用队列最新 prev 算 resolved；标签 = 旧 ∪ 新                                                                        |
| **数组补丁不打标**                            | 致命（P0-6）             | 所有数组包装写入槽位前 `ensureFlag(out)`，否则传播成果静默归零                                                                               |
| **Object.freeze 阻止 Symbol**            | 高（P1-9）              | stampOrigin 用 isExtensible 守卫；冻结对象进 frozenTainted WeakSet；读取侧双判 `obj[TAINT_FLAG] || frozenTainted.has(obj)`            |
| **falsy 跳过语义错**                        | 高（P1-10）             | 按 React 语义：仅 null/undefined/boolean 跳过；0/""/NaN 渲染成 HostText（`{count && <b/>}` count=0 渲染 "0"）                         |
| **custom hook 击穿 P4**                  | 高（P1-11）             | instance 挂 RenderContext，hookCounter 运行时自增；custom hook 经 currentCtx 取调用者 ctx                                           |
| **Fragment 展开索引错位**                    | 高（P1）                | 遍历时展开 JSX 树中的 Fragment 和嵌套数组，与 fiber 树对齐                                                                               |
| **组件返回原始值**                            | 高（P1-15）             | 父是 FunctionComponent 非 HostComponent，对齐失效 -> 承认盲区，值指纹层兜底                                                               |
| **数组 length 未打标**                      | 中高（P1-13）            | stampOrigin 遍历数组时给 length 槽写入全部元素字段的并集                                                                                 |
| **数据源覆盖不足**                            | 中高（P1-14）            | 补 XMLHttpRequest（axios）/ GraphQL-over-XHR；patch res.clone()/.text()/.body.getReader()                                  |
| **cloneElement 丢标签**                   | 高                    | wrap cloneElement：旧 props 的 stamp 复制到新 props                                                                           |
| **SSR/hydration 初始屏**                  | 中高                   | hydrate 完成后的首次 commit 做"虚拟 commit"遍历补记                                                                                 |
| **Nullish 对象触达**                       | 中高                   | 所有 `obj[TAINT_FLAG]` 前加 `obj != null` 守卫                                                                               |
| **StrictMode 双渲染**                     | 低                    | stamp 幂等（bigint | 运算）；每次 render 入口新建上下文                                                                                |
| **Concurrent 中断**                      | 低                    | 重做时 element 重建、stamp 重盖；上下文栈 LIFO 保证一致                                                                                 |
| **Suspense**                           | 中                    | try/finally 确保上下文弹出；fallback 渲染使用独立上下文；resolve 后重渲新建上下文                                                                |
| **useMemo/useCallback 丢标签**            | 中                    | wrap 缓存 `[value, value$]` / `[fn, fn$]`，命中时回填 `lastReturnTaint`                                                        |
| **useContext 原始值**                     | 中                    | Provider value 已是污染对象，consumer 读走慢道恢复                                                                                  |
| **事件处理器中创建 JSX / render props**        | 高（第二轮）               | **词法 ctx 捕获**为主通道：`__ctx` 经闭包存活，回调在定义组件 ctx 窗口外执行时仍正确；`currentCtx()` 降级为兜底；jsx 的 ctx 显式传入，词法外传 null 走 P=0n             |
| **原生方法变换黑洞**                           | 致命（第二轮 P0-9）         | `toFixed/toUpperCase/trim/slice/join` 等纯方法 codegen 并集 `result$ = recv$ ∪ args$`（§1.8）；非纯走 §1.7。否则文本上屏前最后一公里断流          |
| **文本级控制边丢失**                           | 高（第二轮）               | childStamps 改为完整 `Stamp{D,P}` 在**表达式求值点捕获**（push 窗口内），jsx() 只透传不归一化；`{isVip && name}` 出 name 数据边 + isVip 控制边           |
| **jsx 签名吞 key**                        | 高（第二轮 P2-1，正确性）      | `jsx(type, props, key, stamps, ctx)` -- key 必须透传给原生 jsx，否则列表 reconciliation 身份错乱；需插件自实现 JSX 转换                         |
| **useSelector / useSyncExternalStore** | 中（第二轮 P2-2）          | 显式 wrap；selector 渲染期外调用（订阅检查）不串味，配单测                                                                                   |
| **React.Children.map**                 | 中（第二轮 P2-3）          | 同 cloneElement 一并 wrap，复制 stamps 到克隆 element                                                                           |
| **React 19 `use()` hook**              | 中（第二轮 P2-4）          | 读取 promise/context，同 useContext 处理                                                                                     |
| **stampMap 全空也 set**                   | 中（第二轮 P2-8）          | stamps 全零时跳过 WeakMap 写，省每次渲染数千次写入                                                                                      |
| **多表达式文本**                             | 中（P1）                | 标签数组按 props.children 位置索引，React 语义 falsy 跳过对齐                                                                          |
| **React Compiler**                     | 低                    | 跳过 render = 无 commit 变化 = 无需新边（sound）                                                                                  |
| **React ≤16 classic runtime**          | 低（#22）               | 并行 patch `React.createElement`（比 jsx-runtime 更简单）                                                                      |
| **第三方组件库内部断流**                         | 结构性（#23）             | antd 等用自己 jsx-runtime，props 打标救不了 -> 值指纹层兜底                                                                            |
| **fiber.tag 跨版本脆弱**                    | 中（#19）               | HostText=6/HostComponent=5 是内部值，需版本探测表 + 启动自检                                                                          |


### 8.2 引擎内核


| 边界                           | 对策                                                                                                                                                                                                                                                                 |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **async/await**（第三轮具体化）      | 三个补丁点：① async 函数 `return expr` 且 expr$≠0n 时 `SM.set(返回promise, expr$)`（出口插桩 `.then` 打标）；② `v = await p` -> `v$ = SM.get(p) ?? 0n`；③ patch `Promise.prototype.then/catch/finally`：回调参数影子从 `SM.get(promise)` 注入（同 P0-3 参数通道）；`Promise.all/race/allSettled` 结果数组逐槽或并集 |
| **生成器**                      | P2 处理，先 CUT                                                                                                                                                                                                                                                        |
| **异常**                       | try/finally 保证上下文弹出；catch 后清一次 `lastReturnTaint`                                                                                                                                                                                                                   |
| `**f(g(x))` 嵌套**             | 每个调用点 codegen：先清 `__ctx.lastReturnTaint`，后读；嵌套调用每个独立清/读循环                                                                                                                                                                                                          |
| **immer/Proxy**              | 已消解（§1.7）：RTK `produce` 的 draft 是 Proxy 看不懂，但 produce 返回的最终对象属性值 = 原 API 值 -> 三段式恢复的值指纹匹配自动恢复，无需 wrap produce、无需追踪 draft↔base                                                                                                                                      |
| **getter/eval/new Function** | 函数调用走 §1.7 三段式恢复；非调用表达式 CUT。DevTools UI 显示置信度（taint/value-match/断流），避免把盲区当 bug                                                                                                                                                                                     |


| **变量赋值/复合赋值/++--** | 第二轮 P1：左值 Identifier 同步影子 `x$ = e$` / `x$ \|= e$` / `++` 标签不变；否则重赋值后陈旧断流 |
| **for-of / for-in 循环变量** | 第二轮 P1：逐轮从集合槽位初始化 `u$ = SM(coll).fields.get(String(i)) ?? ∅` |
| `**delete a.b**` | 第二轮 P1：清影子槽，否则后续读 undefined 仍带旧标签 -> 假阳性 |
| `**&&` / `\|\|` 值语义** | 第二轮 P1：`D = a$ ∪ b$`，`P \|= a$`；不是布尔化（结果是操作数本身） |
| **计算属性键 `a[user.id]`** | 第二轮 P2-5：键选择依赖字段 -> `D \|= key$`（保守并集，一行 codegen） |
| **JSON.parse 直调**（localStorage 水合） | 第二轮 P2-6：标 `unknown://json.<path>` 源，DevTools 标注"来源不明"；引导用户接 stampOrigin |

### 8.3 原生替换


| 边界                 | 对策                                                                           |
| ------------------ | ---------------------------------------------------------------------------- |
| **补丁时机**           | runtime 必须第一行 import（Vite 入口注入）                                              |
| **spec 一致性**       | 每个包装函数配 Test262 风格单测（稀疏数组、thisArg、species）                                   |
| `**.sort` 控制依赖**   | comparator 返回的 P 并入数组 whole.P                                                |
| `**.filter` 控制边**  | callback 的 P 并入每个保留元素                                                        |
| **Map/Set**        | 先 CUT，按需 patch                                                               |
| **数组下标膨胀 fieldId** | 中（#17）：千条列表 = 千个 ID，registry 膨胀。改通配路径 `items[].name` 共享 ID（配置 arrayWildcard） |


| **补丁幂等 marker** | 第二轮 P2-7：HMR 重复执行 runtime 不二次包装，`if (orig.__taint_patched__) return` |

### 8.4 工具链


| 边界             | 对策                                                                                                                                                                                 |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Vite 预打包**   | 仅插桩 app 代码（Babel `enforce: pre`）；三方库不插桩（§1.7 兜底），无需 exclude。**第三轮备选**：若需对个别库精确插桩，用 `include: /node_modules\/(lodash-es|immer|zod)/` 直接转预打包产物，比 exclude（数百模块请求、冷启动恶化）更优；P0 各花半天实测对比 |
| **HMR**        | 补丁幂等；SM/stampMap 是 WeakMap 无需清空                                                                                                                                                    |
| **source map** | Babel 保留 inputSourceMap                                                                                                                                                            |
| **性能预算偏乐观**    | 中（#25）：3-8× 未计 try/finally 全量包裹、对象字面量插桩、大响应 stampOrigin 瞬时阻塞。P0 阶段在真实中型 app 实测"Vite 冷启动 + 交互帧率"作为 go/no-go                                                                         |


---

## 第九章：落地路线

### 验收铁律

每个传播函数、每条机制 = **正例能记边 + 反例不记边**，双单测进 CI。
**交叉验证**：每条污点边都应能在值指纹层（P-1）找到值层面佐证；两边不一致即暴露插桩 bug--这是验收铁律最便宜的实现方式。

### P-1：值指纹层（先做，一周内出可演示绑定图）

**目标**：零插桩，拦截 fetch/XHR 建"值->字段路径"索引；commit/DOM 扫描时按文本/属性值反查。产出 60% 演示价值 + 后续所有阶段的**回归真值集**。

**双重价值**：

1. 穿透第三方库与一切插桩盲区（P1-15 组件返回原始值、#23 antd 内部断流的唯一现实解）
2. 给污点引擎当对照真值集，是最便宜的"正例记边 + 反例不记边"验收


| 模块           | 内容                   | 验收                             |
| ------------ | -------------------- | ------------------------------ |
| fetch/XHR 拦截 | 建值->字段路径索引（含 length） | 值索引覆盖嵌套/数组                     |
| fiber 扫描     | 全量遍历 fiber，文本/属性值反查  | 能产出候选绑定图（有碰撞假阳性，如 7/true/重名文案） |


**注**：值指纹单独不够格当终态（碰撞假阳性、无控制边、值变换启发式），但它是 A 的必经基础设施（fetch/XHR 拦截 + fiber 遍历），零浪费。

### P0：引擎内核（不绑框架）

**目标**：vanilla JS 上验证全部核心机制。


| 模块                 | 内容                                                                                                                    | 验收                                                         |
| ------------------ | --------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| fieldRegistry      | 字段 ID 分配（含数组通配路径 #17）                                                                                                 | 同一字段重复分配返回相同 ID                                            |
| SM + 快慢道           | WeakMap + TAINT_FLAG + frozenTainted 双判                                                                               | 非污染对象走快道；冻结对象（P1-9）不崩且能查                                   |
| stampOrigin        | 递归逐字段打标 + isExtensible 守卫 + length 打标                                                                                 | 嵌套/数组每个槽正确；冻结对象不抛；length 有标签                               |
| valueIndex + 三段式恢复 | stampOrigin 同步建值指纹索引；recoverOpaqueReturn（身份->值指纹∩输入->CUT）                                                             | `_.get(user,"name")` 出边；`_.get(obj,"greeting")` 不假阳性（§1.7） |
| 上下文栈               | enterComponent(instance)/exitComponent/currentCtx + argTaints 通道                                                      | 嵌套正确；异常不泄漏；函数参数标签传递（P0-3）                                  |
| Babel 插件           | 属性读/写/运算/函数调用/条件栈/对象字面量/展开 + **第二轮**：变量赋值/for-of-in/delete/&&||值语义/方法调用纯方法并集（含 Nullish 守卫 + 词法 ctx）                   | 编译产物正确；null/undefined 不抛；属性写按值驱动（P0-4）                     |
| 原生替换 + 纯方法 codegen | Array.map/filter/reduce/slice/flat/concat + ensureFlag(out)；**String/Number/Date/Math 纯方法走 §1.8 codegen 并集**（不 patch） | 逐元素传播正确（P0-6）；`price.toFixed(2)` 出边（P0-9）；反例不假阳性           |
| DOM sink           | onDomWrite + 边生命周期（clearEdges/recordEdge）                                                                             | 双空短路；边只增不减修复（P0-7）                                         |


**关键实测（go/no-go）**：Vite 冷启动 + 真实中型 app 交互帧率（#25）。

**第二轮追加验收**：

- `items.map(i => i.price.toFixed(2))` -> `<span>` 出 price 数据边（P0-9 + P0-6）
- `let x; x = user.name;` -> 出边（变量赋值）
- `for (const u of users) return <li>{u.name}</li>` -> 出边（for-of）
- `delete user.name` 后渲染 -> **不**出边（delete 反例）
- render props（react-table 最小复现）-> 出边且不崩（词法 ctx）
- `{isVip && name}` -> name 数据边 + isVip 控制边，且**不**出 isVip 数据边（&& 双通道反例）

### P1：React 数据边

**第三轮追加验收**：

- `{loading ? <Spinner/> : <Content/>}` -> Content 节点有 loading 控制边（否支对称）
- `if (err) return <Error/>; return <Main/>` -> Main 有 err 控制边（早退区域 P）
- `data.user.name = "Bob"` 后渲染 -> **不**出 user.name 数据边（属性写替换语义，旧标签失效）
- 含循环引用对象 stampOrigin -> 不崩，各槽位标签正确
- `{ "user.name": "Ada" }` 与 `{ user: { name: "Bob" } }` -> 两个字段 fieldId 不冲突
- `<pre>{JSON.stringify(user)}</pre>` -> 文本边指向 user.name/level/isVip 全部叶子（后序并集）
- 点 DOM 节点 -> O(1) 反查字段（边双向索引）

**目标**：真实 React 组件的数据边正确。


| 模块                  | 内容                                                                    | 验收                                              |
| ------------------- | --------------------------------------------------------------------- | ----------------------------------------------- |
| JSX 过缝              | jsx() 收集 attrStamps + childStamps，props 打标（P0-1/P0-2）                 | 属性边正确；props 传递不断流                               |
| commit 遍历           | 自注入 hook（P0-8）+ alternate 身份增量剪枝（P0-7）+ Fragment 展开                   | 文本/属性边正确；未变更子树不遍历；无 hook 也能工作                   |
| Hook 状态持久化          | ctx.instance + hookCounter + arg$ 显式传参 + setter 只包装（P0-5/P1-11/P1-12） | 重渲染标签不丢；连续 setCount(c=>c+1) 语义正确；custom hook 兼容 |
| cloneElement        | wrap 复制标签                                                             | Radix/shadcn 组件标签不丢失                            |
| SSR hydration       | 虚拟 commit 补记                                                          | Next.js 初始屏有边                                   |
| useMemo/useCallback | wrap 缓存标签                                                             | 缓存命中时标签不丢失                                      |
| 数据源                 | 补 XHR/axios/res.clone/.text（P1-14）                                    | 非 fetch 项目有边                                    |


### P2：控制边

**目标**：控制边精确。


| 模块               | 内容                            | 验收            |
| ---------------- | ----------------------------- | ------------- |
| 条件栈              | if/else/三元/&&/||/switch/循环    | 结构化流控制边正确     |
| 双轨分离             | D 永不升自 P                      | 控制标签不污染数据标签   |
| sort/filter P 通道 | comparator/predicate 的 P 正确传播 | 排序/过滤产生的控制边正确 |
| length 控制边       | items.length === 0 && （P1-13） | 空态组件有控制边      |


### P3：生态 + UI

**目标**：召回 ~90%+，DevTools 可视化。


| 模块          | 内容                                                                      |
| ----------- | ----------------------------------------------------------------------- |
| 不透明函数三段式恢复  | §1.7：身份匹配 -> 值指纹∩输入 -> CUT；覆盖 lodash/immer/zod/react-query 等一切三方库，无需白名单 |
| 值指纹索引       | stampOrigin 时建 valueIndex（值->字段位图）；供三段式恢复 + DOM 端反查共用                   |
| 未拦截源        | WebSocket/SSE/IndexedDB -> 暴露 stampOrigin 手动接                           |
| DevTools UI | 点 DOM 节点看绑定字段；展示边置信度（taint / value-match / both）与断流点                    |
| 召回实测        | 真实 app 测召回率，与值指纹层交叉验证，标出盲区（变换值如 toUpperCase 靠腿二 patch）                  |


---

## 第十章：值指纹层--第一性机制（双重角色）

> 值指纹不再只是"兜底层"，而是第一性机制，承担双重角色：
> ① **不透明函数返回恢复**（§1.7 三段式的第②步）：任意三方库/原生/WASM 调用后，按返回值查索引恢复标签--**这取代了原方案的三方库白名单（腿三）**。
> ② **DOM 端反查 + 交叉验证**：穿透第三方组件库内部、组件返回原始值、immer/Proxy 等污点引擎结构性盲区，并给污点边当对照真值集。

### 10.1 为什么值指纹是第一性机制

撤掉白名单后，污点引擎对不透明函数的覆盖**完全依赖值指纹恢复**（§1.7）。这让值指纹从"可选兜底"升级为"召回主干的一部分"。它同时覆盖污点引擎的三个结构性盲区：


| 盲区                   | 原因                                                                                                | 值指纹如何救                  |
| -------------------- | ------------------------------------------------------------------------------------------------- | ----------------------- |
| **第三方组件库内部**（#23）    | `<Button>{user.name}</Button>`：antd 用自己的 jsx-runtime 渲染 children，props 打标也救不了（antd 代码未插桩，属性读不走慢道） | 值指纹只看 DOM 文本值，不关心中间经过谁  |
| **组件返回原始值**（P1-15）   | `const Price = ({v}) => fmt(v)` 返回字符串，父是 FunctionComponent 非 HostComponent，对齐失效                   | 值指纹按文本值反查，不看 fiber 类型   |
| **immer/Proxy**（#16） | RTK `produce` 的 draft 是 Proxy，CUT 后 store 更新路径断流                                                  | 值指纹看最终 store 值，不看 draft |


### 10.2 值指纹层原理

```

fetch/XHR 拦截 -> 递归遍历响应 -> 建 "值 -> 字段路径" 索引
                                       │
commit/DOM 扫描 -> 读取文本/属性值 -> 反查索引 -> 候选边

```

- **零插桩**：不碰 app 代码、不碰库代码，只拦数据源 + 扫 DOM
- **开销极低**：~1.05×（仅拦截 + 一次 DOM 扫描）
- **天然穿透**：任何中间层（库、Proxy、函数边界）都无所谓，只看值

### 10.3 值指纹的局限（为什么不能单独当终态）


| 局限         | 说明                                           |
| ---------- | -------------------------------------------- |
| **碰撞假阳性**  | `7`、`true`、重名文案（多个字段值都是 "Active"）会匹配到多个字段路径  |
| **无控制边**   | 值指纹只看"值流进去了"，看不到"渲染被谁决定"（isVip 控制 `<em>` 出现） |
| **值变换启发式** | `"LV.7"` 是 `level` 变换来的，需要子串/模板启发式匹配，不精确     |


**结论**：B 单独不够格，但它的两个价值不可替代--穿透盲区 + 当 A 的对照真值集。

### 10.4 混合架构：A + B 双轨

```

```
        ┌─────────── 值指纹层（B，P-1 先做）───────────┐
        │  fetch/XHR 拦截 -> 值->路径索引（含 length）  │
        │  fiber 扫描 -> 文本/属性值反查 -> 候选边      │
        └──────────────┬───────────────────────────────┘
                       │ 交叉验证 / 盲区兜底
```

┌────────── 污点引擎（A，本方案修复 P0 后）──────────────┐
│  stampOrigin + 快慢道 + ctx 栈 + jsx 过缝 + hook 持久化 │
└──────────────────────────────────────────────────────┘
   边存储：fieldId × DOM节点 × {data|control} × 置信度
            置信度 = taint（A 独有）/ value-match（B 独有）/ both（双证）

```

**置信度三级**：

- `taint`：A 独有产出（精确，但可能有盲区漏报）
- `value-match`：B 独有产出（穿透盲区，但可能碰撞假阳性）
- `both`：A 和 B 都指向同一字段（最高置信度）

**交叉验证**：A 的每条边都应在 B 找到值层面佐证；A 有而 B 没有 -> 可能是控制边（B 看不到，正常）或 A 误报；B 有而 A 没有 -> 暴露 A 的插桩盲区（第三方库/immer），用 B 兜底。

### 10.5 落地顺序（重排）

```

P-1（新增，先做 B）：值指纹层。一周出可演示绑定图 + 永久回归真值集。
                    直接验证 fetch/XHR 拦截 + fiber 遍历两大基础设施（A 的必经之路，零浪费）。
P0（引擎内核）：原内容 + P0-3 参数通道 + P0-4 属性写/对象字面量修复。
P1（React 数据边）：原内容 + P0-1 属性边 + P0-2 props 打标 + P0-5/6/7/8 + P1-10/11/12。
P2（控制边）：原内容 + P1-13 length。
P3（生态）：XHR 系拦截提前到此开头；immer produce 包装；DevTools UI 展示边来源与断流点。

```

**核心思路**：先用一周值指纹层拿到 60% 演示价值与永久性回归真值集，再让污点引擎在**可验证的轨道**上生长--每加一个机制，都能用 B 对照验收。
**核心思路**：先用一周值指纹层拿到 60% 演示价值与永久性回归真值集，再让污点引擎在**可验证的轨道**上生长--每加一个机制，都能用 B 对照验收。

---

## 第十一章：图语义与不支持清单（第三轮新增）

> "图"本身是产品。用户的核心交互是"点 DOM 节点 -> 反查字段"，图的数据结构必须服务这个交互。

### 11.1 边双向索引（点选反查）

`recordEdge(fieldId, domNode, type)` 原只有 fieldId->node 单向。产品的核心交互是**点 DOM 节点 -> 反查字段**，需要 node->fields 反向索引。第一轮 #18 的边清理也依赖反向索引（按节点清边）。

```ts
// 正向：fieldId -> Set<{domNode, type, attr}>（字段查 DOM，画高亮路径）
const forwardIndex = new Map<number, Set<Edge>>();
// 反向：domNode -> Map<fieldId, type>（DOM 查字段，点选交互）-- 第三轮新增
const reverseIndex = new WeakMap<Node, Map<number, 'data' | 'control'>>();
```



function recordEdge(fieldId, domNode, type, attrName?) {
  // ... 正向写入 forwardIndex
  // 反向写入（点选场景 O(1) 查）
  let rev = reverseIndex.get(domNode);
  if (!rev) { rev = new Map(); reverseIndex.set(domNode, rev); }
  rev.set(fieldId, type);
}

function clearEdges(domNode) {
  // 反向索引同步清理（按节点清边，第一轮 #18）
  reverseIndex.delete(domNode);
  // ... 正向清理
}

// 点选交互：用户点 DOM 节点 -> O(1) 反查字段
function lookupFieldsByNode(domNode: Node) {
  return reverseIndex.get(domNode) ?? new Map();
}

```

### 11.2 时间维度（边的覆盖 vs 叠加）

tab 切换后同一 DOM 位置先后被 `feedA.title` 和 `feedB.title` 驱动。叠加则图越来越脏（#18），覆盖则丢失"可由多字段驱动"的真实信息。

**决策**：边上带 `lastSeenCommit`，UI 默认只显示当前 commit 的边，可切历史视图。

```ts
interface Edge { fieldId: number; domNode: Node; type: 'data'|'control'; attrName?: string; lastSeenCommit: number; }
// 每次 commit 自增 commitCounter；recordEdge 时更新 lastSeenCommit
// UI 默认过滤 lastSeenCommit === currentCommit；历史视图展示所有
```

### 11.3 字段级 vs 记录级血缘

`resolveSourceId` 把 `/users/1`、`/users/2` 归一为 `GET /users/:id` -> 两个用户的 name 共享 fieldId（位图不爆炸，正确决策）。但用户指着列表某行问，只能答"来自 user.name"，答不了"来自**第 2 条记录**的 user.name"。

**决策**：fieldId 保持字段级（位图不爆炸）；边上**可选携带** `entityKey`（数组路径 index 或 id 字段值），UI 高亮同记录的其他边--低成本、体验跃升。

```ts
interface Edge { /* ... */ entityKey?: string | number; }  // 如 "items[2]" 或 id 值
// stampOrigin 时若对象有 id 字段，记 entityKey = id 值；数组元素记 index
```

### 11.4 不支持清单（明确边界，避免过度恐慌）


| 场景                                 | 状态       | 原因 / 替代                                                                        |
| ---------------------------------- | -------- | ------------------------------------------------------------------------------ |
| **Apollo Client normalized cache** | **不支持**  | 库内把对象拆散重组（未插桩、原始值断流）+ dev deep-freeze（P1-9）。UI 检测 Apollo 时提示切换或手动接 stampOrigin |
| **Proxy 深链（非 immer）**              | 不支持      | trap 不透明，CUT；值指纹层兜底                                                            |
| **组件返回原始值**（P1-15）                 | 降级       | 父是 FunctionComponent 非 HostComponent，对齐失效；值指纹层兜底                               |
| **WASM / eval / new Function**     | 不支持      | 无法 instrument；值指纹层兜底                                                           |
| **dayjs/moment** `.format()`       | P3 补     | 输入时间戳、输出格式化串，值指纹不匹配；P3 专门 wrap 构造器                                             |
| **zustand / Redux（非 immer 路径）**    | **基本可用** | 对象引用传递天然免疫--传播靠引用+SM，不经过库代码。此洞察避免过度恐慌                                          |
| **Map/Set 作为数据载体**                 | 不支持      | JSON 不产生，CUT                                                                   |


### 11.5 调试体验（dev 工具自身可用性）

- **sourcemap** 保留行号映射，避免 step-into 全是插桩代码
- `**instrument.ignore**` 配置（如 `['**/vendor/**']`）跳过指定路径
- **shadow 变量命名约定** `__$`（不可枚举感），便于调试器折叠
- **非生产放宽**：原"dev-only"放宽为"非生产"--staging/QA 给 PM 用，3-8× 开销对非开发机可接受，插桩通过 env 开关进 staging build

---

## 附录 A：评审修复后的关键差异


| 维度              | 评审前                                                      | 评审后                                                | 修复编号     |
| --------------- | -------------------------------------------------------- | -------------------------------------------------- | -------- |
| 文本节点标签          | 依赖 HostText 的 memoizedProps（原始值）                         | 存父 HostComponent 的 props，按子索引对齐                    | P1       |
| 属性边             | **整条通道缺失**，只画文本边                                         | attrStamps + commit 期对元素本身按属性记边                    | P0-1     |
| props 传递        | **断流**，子组件 props.name 走快道返回 0n                           | jsx() 把带标签 prop 写入 props SM 影子并打标，子组件走慢道恢复         | P0-2     |
| 函数参数            | **通道缺失**，原始值过函数边界即死                                      | ctx.argTaints 通道：调用点写、入口读后置 null                   | P0-3     |
| 属性写条件           | 看目标对象 `a[TAINT_FLAG]`（写反）-> 第一轮按值驱动 `if (e$ !== 0n)`     | **替换语义**：无条件写槽，e$ 为 0n 删槽（第三轮再修正）；mutate/重置后旧标签失效  | P0-4（三轮） |
| useState setter | 提前用闭包旧值求 updater（改错程序）                                   | 只包装不求值，让 React 用队列最新 prev 算 resolved               | P0-5     |
| 数组补丁            | 只写影子不打标，传播成果静默归零                                         | 写槽前 ensureFlag(out)                                | P0-6     |
| Commit 增量       | 用 lanes（commit 后已重置，更新类全剪枝）                              | 用 alternate props 身份比较 + 边生命周期                     | P0-7     |
| DevTools hook   | 不存在时静默全灭                                                 | React 加载前自注入最小 hook                                | P0-8     |
| Object.freeze   | 声称"不影响 Symbol"（错误）                                       | isExtensible 守卫 + frozenTainted WeakSet + 读取侧双判    | P1-9     |
| falsy 跳过        | 按 JS falsy（0/"" 会错位）                                     | 按 React 语义：仅 null/undefined/boolean 跳过             | P1-10    |
| custom hook     | 独立 useRef 变量，custom hook 击穿                              | instance 挂 RenderContext + hookCounter 运行时自增       | P1-11    |
| Hook 实参         | 靠 lastReturnTaint 隐式捕获（丢 a$）                             | arg$ 显式传参                                          | P1-12    |
| 数组 length       | 未打标，空态判断无控制边                                             | stampOrigin 给 length 槽写元素字段并集                      | P1-13    |
| 数据源             | 仅 fetch                                                  | 补 XHR/axios/res.clone/.text/流式                     | P1-14    |
| 渲染状态            | 全局 `__lastReturnTaint` + 全局 `pcStack`                    | 作用域上下文栈 `__ctx`（含 instance/hookCounter/argTaints）  | P2       |
| 属性读开销           | 所有属性读走 `readProp$(obj, key)` 函数调用                        | 快慢道内联：`obj[TAINT_FLAG]                             |          |
| 三方库覆盖           | 白名单插桩（腿三）：枚举 lodash/immer/zod/react-query + Vite exclude | **撤掉白名单**：不透明函数三段式恢复（身份->值指纹∩输入->CUT），对任意库/WASM 成立 | §1.7     |
| 架构形态            | 纯污点单轨                                                    | 污点引擎（A）+ 值指纹层（B）混合；值指纹双重角色（不透明恢复 + DOM 反查/交叉验证）    | §1.7/§10 |


| 原生方法变换（toFixed/toUpperCase/...） | CUT（断流最后一公里） | 纯方法 codegen 并集 `result$ = recv$ ∪ args$`（§1.8，纯度分类非白名单） | 第二轮 P0-9 |
| 上下文模型 | 动态 `currentCtx()` 为主 | **词法 ctx 捕获为主**，动态降级兜底（render props / 事件处理器） | 第二轮 |
| 插桩清单 | 缺 6 行 | 补变量赋值/for-of-in/delete/&&值语义/方法调用/计算键 | 第二轮 |
| childStamps | bigint，jsx 时刻归一化（文本控制边丢） | 完整 `Stamp{D,P}` 求值点捕获（§5.5） | 第二轮 |
| jsx 签名 | 吞 key（破坏 reconciliation） | `jsx(type, props, key, stamps, ctx)` 透传 key | 第二轮 P2-1 |

## 附录 B：术语对照

| 槽位语义 | 仅自身 id（§5.1 实现）vs 子树并集（§6 示例）矛盾 | **后序并集**：槽位 = 自身 id ∪ 子树叶子并集（§1.1 重写） | 第三轮 |
| 控制边对称性 | 只在真支 push，否支/早退之后漏画 | 双支对称 push + 早退区域 P（§1.4） | 第三轮 |
| 属性写语义 | 条件写入 `if (e$ !== 0n)`（mutate 后旧标签残留） | **替换语义**：无条件写槽，e$ 为 0n 删槽（§1.3/§5.3） | 第三轮 P0-4 |
| 循环引用 | stampOrigin 无保护，手动接入 store 栈溢出 | visited WeakSet（§1.1） | 第三轮 |
| 字段路径 key | 字符串拼接，含 `.` 的字段名歧义 | 分段数组 `JSON.stringify([sourceId, ...seg])`（§1.1） | 第三轮 |
| jsxDEV 参数 | 只转发三参，丢 source/self | 原样透传前六参（§5.5） | 第三轮 P2-7 |
| 图索引 | 单向 fieldId->node，点选反查 O(n) | 双向索引 + node->fields 反向（§11.1） | 第三轮 |
| 边时间维度 | 只增不减（叠加脏）或覆盖（丢信息） | lastSeenCommit，UI 默认当前 commit（§11.2） | 第三轮 |
| 记录级血缘 | fieldId 字段级，答不了"第几条记录" | fieldId 保持字段级 + 边携带 entityKey（§11.3） | 第三轮 |
| 不支持清单 | 未明确 | Apollo/Proxy 深链/WASM/组件返回原始值 明确降级（§11.4） | 第三轮 |


| 术语                    | 含义                                                                    |
| --------------------- | --------------------------------------------------------------------- |
| TAINT_FLAG            | 对象污染的 Symbol 标记，决定走快道还是慢道                                             |
| frozenTainted         | 冻结对象（Apollo/RTK）的 WeakSet 注册表，TAINT_FLAG 打不上时的兜底                      |
| SM                    | Shadow Memory，全局 `WeakMap<object, Shadow>`，存每个对象的属性槽标签                |
| ensureFlag            | 给对象打 TAINT_FLAG，冻结则进 frozenTainted（属性写/字面量/props 共用）                  |
| stampMap              | `WeakMap<props, JSXStamps>`，React 过缝的标签存储（含 attrStamps + childStamps） |
| JSXStamps             | `{ attrStamps, childStamps, childUnion }`，属性边 + 文本边 + children 并集     |
| hookStampMap          | `WeakMap<instance, Stamp[]>`，Hook 状态标签跨渲染持久化                          |
| 上下文栈                  | 每个组件函数的独立渲染上下文（含 instance/hookCounter/argTaints/pcStack）              |
| argTaints             | 函数参数通道，调用点写实参标签、入口读后置 null                                            |
| 快慢道                   | 非污染对象走原生属性读（快道），污染对象走 WeakMap（慢道）                                     |
| fieldRegistry         | 字段路径 -> 整数 ID 的全局映射                                                   |
| edgeRegistry          | `WeakMap<DOMNode, Set<edgeKey>>`，边的生命周期管理（先清旧边再记新边）                   |
| valueIndex            | 值指纹索引 Map<原始值, 字段位图>，stampOrigin 时构建；双重角色：不透明函数恢复（§1.7）+ DOM 反查（§10）  |
| recoverOpaqueReturn   | 不透明函数三段式恢复：身份匹配 -> 值指纹∩输入并集 -> CUT；取代三方库白名单                           |
| 置信度                   | taint（A 独有）/ value-match（B 独有）/ both（双证）                              |
| 纯方法并集（§1.8）           | String/Number/Date/Math 变换类方法：`result$ = recv$ ∪ args$`，按纯度分类（非库白名单）  |
| 词法 ctx                | 组件函数体内的 `__ctx` 常量，经闭包捕获；render props/事件处理器在 ctx 窗口外执行时仍正确（主通道）       |
| captureChildStamp     | 在 JSX 表达式求值点捕获完整 `Stamp{D,P}`（push 窗口内），保文本级控制边                       |
| patchOnce             | 原生补丁幂等 marker，HMR 不二次包装                                               |
| CUT                   | 看不懂的代码处丢弃标签，宁可漏报不可错报                                                  |
| 替换语义（§5.3）            | 属性写无条件 slowSetProp，e$ 为 0n 删槽；槽位 = 最后写入值的标签                           |
| 控制边对称（§1.4）           | 双支 push + 早退区域 P：否支/早退之后的代码也由 cond 控制                                 |
| reverseIndex（§11.1）   | node->fields 反向索引，点选 O(1) 反查                                          |
| lastSeenCommit（§11.2） | 边的时间戳，UI 默认只显示当前 commit 的边                                            |
| entityKey（§11.3）      | 边携带的记录标识（数组 index / id 值），实现记录级血缘                                     |



# 评审报告：边界探索、可行性裁决与替代方案

> 评审对象：《终极技术方案-第一性原理重构.md》（下称"原方案"）
> 评审方法：逐章推演数据流，对每个"自证正确性"的命题做对抗性验证，并按真实 React/V8/生态行为核对前提。
> 评审日期：2026-07-30

---

## 〇、总裁决

**架构方向：可行，且基本正确。** 槽位影子内存 + 编译期传播 + Symbol 快慢道 + WeakMap 过缝，这是 dev 模式下 JS 污点追踪的合理形态，比 Proxy 膜（Linvail ~5000×）和全量函数化（Jalangi 26-96×）都务实。

**但当前版本不能开工。** 文档多处"自证正确性"的前提与 React/V8 的真实行为不符，且有 **8 个 P0 级漏洞**——每一个都足以让核心数据流断裂或改变用户程序语义。此外，**属性边（src/href/value）整条通道缺失**、**函数参数通道缺失**，这两个不是 bug 而是机制空白。

修复全部 P0 后，本方案可以支撑"一等公民代码"的高精度绑定图；对**第三方组件库内部**和 **immer/Proxy** 存在结构性盲区，建议用"值指纹匹配"做兜底与交叉验证（见第四章）。

---

## 一、P0 致命问题（不修则系统不可用）

### P0-1 属性边整条通道缺失

原方案的 commit 遍历只处理 `isHostText(child)`——**只画文本节点的边**。但"这是哪个接口字段弄出来的"的答案有相当大比例在属性上：

```jsx
<img src={user.avatar} />           // 头像
<a href={detail.url}>...</a>        // 链接
<input value={form.name} />         // 受控输入
<div className={status.cls} />      // 状态样式
<button disabled={!user.canEdit}>   // 权限
```

Babel 的 JSX 插桩只收集了 children 表达式的 stamps，commit 期也从不调用 `onDomWrite(fiber.stateNode /* 元素本身 */, attrStamp)`。

**修复**：扩展 stamps 结构为 `{ attrs: Record<string, Stamp>, children: Stamp[] }`：
- 编译期：JSXAttribute 的每个表达式同样求影子值，传入 `__runtime.jsx(type, props, { attrStamps, childStamps })`
- commit 期：对 HostComponent 自身的 `stateNode`（元素节点）按 attrStamps 逐属性记边，目标节点 = 元素本身，边上可标注属性名（`src`、`href`），这对手指点选后的 UI 呈现反而是加分项

### P0-2 props 传递断流：最常见的 React 模式直接失效

```jsx
<Child name={user.name} />
// Child 内部：function Child({ name }) { return <h1>{name}</h1> }
```

`name` 是原始字符串。原方案的 stamps 数组**只覆盖 children 位置**，普通 prop（`name=...`）的标签没有任何存储位置。Child 里对 `props.name` 的属性读走快慢道——props 对象没有 TAINT_FLAG，快道返回 0n → **断流**。这是 React 应用中最普遍的数据传递方式，此洞不补，图基本是空的。

**修复（推荐，一石二鸟）**：让 props 成为一等污点公民——`__runtime.jsx` 内对每个带标签的 prop（含 children）执行：

```ts
function jsx(type, props, stamps) {
  for (const [k, s] of Object.entries(stamps.attrStamps ?? {})) {
    if (s !== 0n) { slowSetProp(props, k, s); props[TAINT_FLAG] = true; }
  }
  if (stamps.childUnion !== 0n) { slowSetProp(props, 'children', stamps.childUnion); props[TAINT_FLAG] = true; }
  stampMap.set(props, stamps.childStamps);  // 保留数组用于多文本精确定位
  return nativeJsx(type, props);
}
```

此后 Child 内 `props.name` / 解构 `{ name }` 走**现成的慢道**自动恢复标签，无需新机制。children 的槽位用并集（牺牲 `<div>{a}{b}</div>` 穿过中间组件后的单文本精度，由 stamps 数组在不过中间组件时保精度）。同时解决 `<Wrapper>{name}</Wrapper>` 穿 children 传递的断流。

### P0-3 函数参数通道缺失：原始值过函数边界即死

```js
const label = format(user.name);   // format(n) { return n.toUpperCase() }
```

原方案全文的跨函数机制只有 `lastReturnTaint`（**返回**通道）。实参→形参的标签如何传递，**插桩清单（§4.2）中没有任何条目**。`n` 是原始字符串，形参影子 `n$` 无从初始化 → `toUpperCase` 是原生不透明调用 → 返回标签 0n → 断流。对象实参因自带 SM 侥幸存活，**字符串/数字实参——恰恰是渲染文本的主力——全部死于函数边界**。

**修复**：ctx 增加参数通道，调用点与函数入口配对插桩：

```js
// 调用点：先求值各实参（影子存临时变量），再一次性写通道
__ctx.argTaints = [_a$, _b$];
const r = f(a, b);
const r$ = __ctx.lastReturnTaint;

// 函数入口
function f(n, m) {
  const [n$, m$] = __ctx?.argTaints ?? [];
  __ctx && (__ctx.argTaints = null);  // 立即消费，防串味
  ...
}
```

注意 `f(g(x), h(y))`：实参求值本身含调用，须先把 g/h 的返回影子存入临时变量，最后统一写 `argTaints`，避免嵌套调用互相覆盖。

### P0-4 属性写条件写反了 + 对象字面量未插桩

§4.2：`属性写 a.b = e → if(a != null && a[TAINT_FLAG]) slowSet(a,'b',e$)`。条件判断的是**目标对象**是否污染——但真正的逃逸方向是**值**：

```js
const vm = {};               // 全新字面量，无 TAINT_FLAG
vm.name = user.name;         // e$ = bit(1)，但 a[TAINT_FLAG] 为 undefined → 不写影子
render(vm.name);             // 快道 → 标签丢失
```

污点一旦写入任何**新建/本地对象**（视图模型、DTO、表单状态——前端最高频模式之一）即永久丢失。同理，对象字面量 `{ label: user.name }` 和展开 `{ ...user }` **根本不在插桩清单里**。

**修复**：
- 属性写条件改为 `if (e$ !== 0n) { ensureFlag(a); slowSetProp(a, 'b', e$); }`（按值的标签驱动，并顺手给目标对象打 TAINT_FLAG）
- ObjectExpression 插桩：字面量中带非零影子的属性逐一 slowSet，且有任何非零即给新对象打标
- 展开：编译为运行时辅助 `__runtime.spread(t$ ∪ 源槽并集)` 或逐槽拷贝

### P0-5 useState setter 包装改变程序语义

§2.4 的 wrappedSetter：

```ts
const resolved = typeof newValue === 'function' ? newValue(value) : newValue;
setValue(resolved);
```

两个语义破坏：
1. **updater 被提前用闭包旧值 `value` 求值**，而 React 语义是用**队列中最新的 prev**。连续两次 `setCount(c => c + 1)` 在本方案下结果错误——这不再是标签问题，是**把用户程序改错了**。
2. updater 在用户代码里允许不纯（dev/StrictMode 下 React 还会双调用），此处又多调用一次。

**修复**：只包装不求值，让 React 算 resolved：

```ts
const wrappedSetter = (nv) => setValue((prev) => {
  const resolved = typeof nv === 'function' ? nv(prev) : nv;
  const prevTaint = stamps[hookIndex]?.D ?? 0n;
  stamps[hookIndex] = { D: prevTaint | currentArgTaint(), P: 0n };
  return resolved;
});
```

### P0-6 数组补丁只写影子、不打标，结果标签全丢

§5.4 的 map 补丁：`slowSetProp(out, String(i), ...)` 建立了 `out` 的 Shadow，**但从未 `out[TAINT_FLAG] = true`**。后续任何 `out[i]` 读先走快道检查 `out[TAINT_FLAG]` → undefined → 快道 → 0n。**map/filter/slice/concat 的全部传播成果静默归零**，且不抛任何错——最难排查的一类 bug。

**修复**：所有数组补丁在写入任一槽位前 `out[TAINT_FLAG] = true`。补一条验收：正例 `users.map(u => u.name)` 渲染 `<li>` 必须出边。

### P0-7 增量剪枝的前提不成立：lanes 在 commit 后已重置

§5.6 用 `fiber.lanes !== 0 || fiber.childLanes !== 0` 判断"本次 commit 变更的子树"。但 React 在 `createWorkInProgress` 时就把 `lanes/childLanes` 清为 NoLanes，**commit 完成后 current 树上的 lanes 描述的是"未来排队的工作"，不是"本次的变更"**。后果：更新类 commit 全部剪枝跳过 → **边只在首次挂载时记录，之后永不更新**——比全量遍历更糟，是功能错误。`fiber.alternate === null` 也不是可靠的"新挂载"信号。

**修复**：用 alternate 身份比较做增量键（本方案天然适配，因为 jsx() 每次渲染都新建 props 对象）：

```ts
function hasUpdates(fiber) {
  // 本次渲染过的 fiber 才有新 props 对象；bailout 的子树 props 身份不变
  return fiber.alternate === null || fiber.alternate.memoizedProps !== fiber.memoizedProps;
}
```

同时必须补**边的生命周期**：原方案边只增不减，节点卸载/字段变更后旧边残留 → 图越来越脏。方案：按 DOM 节点存 `WeakRef` + 每次重walk时"先清该子树旧边再记新边"，或 FinalizationRegistry 回收。

### P0-8 DevTools hook 不存在时静默全灭

§5.6：`const hook = window.__REACT_DEVTOOLS_GLOBAL_HOOK__; if (!hook) return;`——这个 hook **由 React DevTools 浏览器扩展安装，React 本身不装**。没装扩展的用户环境里，整个 commit 收口静默失效，零报错零边。

**修复**：参照 react-devtools-inline 的做法，在 React 加载**之前**自注入一个最小 hook 实现（`inject`/`onCommitFiberRoot`/`onScheduleFiberRoot`），让 React 主动来注册。同时处理多 rendererID 与版本差异。

---

## 二、P1 高危问题

### P1-9 "Object.freeze 不影响 Symbol 属性"是错的

§2.3 自证第 4 条声称 freeze 兼容。**错误**：`Object.freeze` 阻止添加**任何**新属性（含 Symbol 键），ES module 恒严格模式下 `frozenObj[TAINT_FLAG] = true` 直接抛 `TypeError`。现实中 **Apollo Client（dev 模式 deep-freeze 结果）**、**Redux Toolkit（Immer autofreeze）** 的数据一打标就崩。

**修复**：`stampOrigin` 全程 `Object.isExtensible` 守卫 + try/catch；不可扩展对象退化为"只建 SM 影子、不打对象标记"（SM 是 WeakMap 不需要改对象），代价是这些对象永远走不到快道开关——需要读取侧改为 `SM.has(obj) || obj[TAINT_FLAG]` 双判，或接受这部分对象全慢道。

### P1-10 falsy 跳过语义与 React 不一致

§2.1 对齐算法依赖 `isFalsyChild`，但 React 的渲染语义是：仅 `null/undefined/boolean` 不渲染；**`0`、`""`、`NaN` 会渲染成文本 fiber**。经典坑 `{count && <b/>}` 在 count=0 时渲染文本 "0"。若 `isFalsyChild` 按 JS falsy 实现，索引立即错位。必须严格按 React 语义实现，并配 `{0}`、`{""}`、`{cond && ...}` 三组单测。

### P1-11 custom hooks 击穿 P4 解法

`useUserData()` 内部调 `useState(data.name)`：custom hook 是普通函数，Babel 不会给它注入 `__instance`，且"静态 hookIndex"在跨 custom-hook 边界时根本无法编号（编译器看不到 call chain 里的 hook 序列）。

**修复**：instance 从独立 useRef 改为挂在 RenderContext 上（`ctx.instance = useRef({}).current`），hookIndex 改为**每次渲染清零的运行时计数器** `ctx.hookCounter++`——custom hook 通过 `currentCtx()` 取 instance 和自增索引，与 React 自身的 hook 链表同序。

### P1-12 hook 参数标签靠 lastReturnTaint 捕获不可靠

`useState(a + b)`：最后一次属性读是 b，通道里只剩 `b$`，`a$` 丢失。事件处理器里 `setCount(5)`：字面量无属性读，通道残留上一次读到的旧标签 → **假阳性边**。

**修复**：hook 参数的影子由 Babel **显式传参**：`__runtime.useState(React.useState(arg), arg$, ctx)`，不依赖通道隐式传递。通道只服务于"返回值"这一它本职的场景。

### P1-13 数组 length 未打标，普遍存在的空态判断无控制边

`{items.length === 0 && <Empty/>}` —— `length` 不在 SM 的 index 槽里，条件标签 0n，空态组件的控制边全丢。这是列表页标配写法。

**修复**：`stampOrigin` 遍历数组时给 `length` 槽写入"全部元素字段的并集"。

### P1-14 数据源覆盖不足

- **axios（XHR）/ GraphQL-over-XHR** 完全没提——大量真实项目不走 fetch；需同步 patch `XMLHttpRequest`（responseText/response 的 getter）
- `res.clone().json()`：clone 产生的新 Response 不带补丁
- `res.text()`、`res.body.getReader()` 流式消费未覆盖
- P3 提了 WebSocket/SSE/IndexedDB，但 XHR 系的遗漏更致命（覆盖率高得多）

### P1-15 组件直接返回原始值：对齐机制结构性失效

`const Price = ({v}) => fmt(v)` —— 函数组件返回字符串，React 创建 HostText fiber，但其**父 fiber 是 FunctionComponent 而非 HostComponent**，P1 的"父 props + 子索引对齐"无处挂接，标签丢失。这类格式化小组件在真实代码里非常多。

**修复**：渲染期拿不到 fiber（不用 internals 的前提下），承认盲区，用第四章的值指纹匹配兜底；或在文档中显式降级为已知限制。

---

## 三、P2 中危与工程风险

| # | 问题 | 说明 / 对策 |
|---|------|------------|
| 16 | immer/Proxy CUT 的代价被低估 | Redux Toolkit 的 `produce(draft => { draft.x = api.y })` 中 draft 是 Proxy，CUT 后**整个 RTK 生态的 store 更新路径断流**。immer 在 optimizeDeps 白名单里也没用——插桩看不懂 Proxy。可行对策：wrap `produce`，记录 draft↔base 映射，finalize 时把 draft 影子并回 base |
| 17 | stampOrigin 按数组下标分配 fieldId | 1000 条列表 = 1000 个字段 ID，registry 膨胀、位图变长。改用通配路径 `items[].name`，同名字段共享 ID（牺牲单条精度换可扩展性，建议做成配置） |
| 18 | 边只增不减 | 见 P0-7，节点卸载后旧边残留 |
| 19 | fiber.tag 等内部常量跨版本脆弱 | HostText=6/HostComponent=5 是内部值，需 React 版本探测表 + 启动自检 |
| 20 | `normalizeStamp` 里 `currentCtx()!` 会在渲染期外崩溃 | `toast.success(<div>{msg}</div>)` 这类**事件处理器中创建 JSX** 的场景无 ctx。需兜底（pcStack 全局回退或 P=0n） |
| 21 | useContext 原始值 / useCallback 命中 | useMemo 文档已提；useCallback 同理需缓存标签；useContext 返回原始值时需从 Provider value 槽恢复 |
| 22 | 经典运行时兼容 | React ≤16 / `runtime: 'classic'` 的项目走 `React.createElement`，需并行 patch createElement（反而更简单） |
| 23 | 第三方组件库内部断流（结构性） | `<Button>{user.name}</Button>`：antd 内部用自己的 jsx-runtime 渲染 `{children}`，P0-2 的 props 打标也救不了（antd 代码未插桩，属性读不走慢道）。**纯污点方案无解**，值指纹兜底（见下） |
| 24 | getter/eval/new Function | CUT 合理，但 DevTools UI 应能显示"此处断流"，否则用户会把盲区当 bug |
| 25 | 性能预算偏乐观 | 3-8× 的估算未计入：try/finally 全量包裹、对象字面量插桩（P0-4 修复后新增）、大响应 stampOrigin 递归的瞬时阻塞。建议 P0 阶段先在真实中型 app 上实测，把"Vite 冷启动 + 交互帧率"作为 go/no-go |

---

## 四、替代方案与更优架构

### 方案对比

| 方案 | 原理 | 开销 | 数据边 | 控制边 | 值变换（"LV."+level） | 第三方库 | 工程侵入 |
|------|------|------|--------|--------|----------------------|----------|----------|
| **A. 本方案（编译期污点）** | 全量插桩 + 影子内存 | 3-8× | ✅ 精确 | ✅ 唯一能做 | ✅ | ❌ 库内断流 | 高（Babel+补丁+hook） |
| **B. 值指纹匹配（零插桩）** | 拦截 fetch/XHR 建 `值→字段路径` 索引；commit/DOM 扫描时按文本/属性值反查 | ~1.05× | ⚠️ 有碰撞假阳性（`7`、`true`、重名文案） | ❌ | ⚠️ 子串/模板启发式 | ✅ 天然穿透 | 极低 |
| **C. jsx 层浅匹配** | 只 wrap jsx/createElement，对每个 prop 值查值索引 | ~1.1× | ⚠️ 同 B | ❌ | ⚠️ | ⚠️ 半穿透 | 中 |
| **D. Proxy 膜（Linvail）** | 响应对象全程包 Proxy | ~5000× | ✅ | ✅ | ✅ | ❌ 身份语义破坏（`===`、key、freeze 全崩） | 低但不可用 |

原方案否决 D 是对的（身份语义破坏是正确性问题，不只是性能）。B/C 单独都不够格当终态，但 B 有两个不可替代的价值：

1. **穿透第三方库与一切插桩盲区**（P1-15、#23 的唯一现实解）
2. **给 A 当对照真值集**：A 的每条边都应能在 B 里找到佐证（值层面），两边不一致即暴露插桩 bug——这正是"验收铁律：正例记边 + 反例不记边"最便宜的实现方式

### 推荐：混合架构 + 重排落地顺序

```
            ┌─────────── 值指纹层（B，1 周）───────────┐
            │  fetch/XHR 拦截 → 值→路径索引（含 length） │
            │  fiber 全量扫描 → 文本/属性值反查 → 候选边  │
            └──────────────┬───────────────────────────┘
                           │ 交叉验证 / 盲区兜底
┌────────── 污点引擎（A，本方案修复 P0 后）──────────┐
│  stampOrigin + 快慢道 + ctx 栈 + jsx 过缝 + hook 持久化 │
└──────────────────────────────────────────────────┘
        边存储：fieldId × DOM节点 × {data|control} × 置信度(taint|value-match|both)
```

- **P-1（新增，先做 B）**：值指纹层。一周内可出能演示的绑定图，同时产出后续所有阶段的回归基准。这也直接验证了 fetch/XHR 拦截与 fiber 遍历两大基础设施——它们是 A 的必经之路，零浪费。
- **P0（引擎内核）**：原内容 + 本报告 P0-3（参数通道）、P0-4（属性写/对象字面量）修复。**go/no-go 指标不变**（Vite 冷启动）。
- **P1（React 数据边）**：原内容 + P0-1（属性边）、P0-2（props 打标）、P0-5/6/7/8 修复 + P1-10/11/12。
- **P2（控制边）**：原内容 + P1-13（length）。
- **P3（生态）**：XHR 系拦截提前到此阶段开头；immer produce 包装；DevTools UI 展示"边来源"（taint / value-match / both）与"断流点"。

---

## 五、结论

1. **方向成立**：槽位影子 + 编译期传播 + 快慢道，是当前约束（C1-C6）下的合理最优架构，三大悖论的解题思路（位置级护照、上下文栈、Symbol 开关）本身都正确。
2. **当前版本不可实施**：8 个 P0 中任何一个都会让系统在真实应用上静默失效或改错用户程序；多处"自证正确性"建立了错误的自信心（freeze、lanes、属性写条件），评审价值正在于此。
3. **修完后仍有结构性边界**：第三方组件库内部、immer/Proxy、组件返回原始值——这些不是实现质量问题是架构边界，应坦然接受并由值指纹层兜底。
4. **更优方案不是换掉 A，而是给 A 配一个 B**：先用一周值指纹层拿到 60% 的演示价值与永久性的回归真值集，再让污点引擎在可验证的轨道上生长。

# 评审报告（第六轮）：修订核验与残留问题

> 评审对象：《API-DOM绑定图-深入浅出.md》(21:49 修订版)、《Web数据血缘-最优设计与规范提案.md》(21:53 修订版)
> 性质：①核验第四、五轮共 20 项结论的吸收情况；②对修订引入的**新机制**（controlIndex 桥、传播侧交集约束、条件槽位侧车）做对抗性审查——修订本身是新的攻击面。
> 评审日期：2026-07-30

---

## 〇、总裁决

**两份文档已收敛到可开工状态。** 第四轮 9 项、第五轮 11 项修正全部吸收，且吸收方式不是打补丁而是重构了论述（碰撞从"唯一代价"改写成"查询侧诚实+传播侧设防"的双面性；控制边从"小例外"改写成"值追踪最不适合的场景"——这两处改写是判断力的体现）。

但新机制带来了**两个 P1 和一批 P2**：controlIndex 桥的覆盖范围被过度宣称（组件边界断裂）、"交集约束"与文档自己的诚实原则自相矛盾。无新增 P0。

---

## 一、吸收核验（全部 ✅）

### 深入浅出 ← 第四轮

| 第四轮结论 | 吸收位置 | 状态 |
|------------|----------|------|
| P0-A 传播侧碰撞中毒（三条设防） | §四 改写为"查询侧诚实,传播侧必须设防" | ✅ |
| P0-B 控制边布尔失效、侧车必选 | §四"控制边不是小例外"+ §八 条件槽位侧车 | ✅ |
| P0-C 值映射生命周期（双容器+代际） | §五"值映射要有生命周期" | ✅ |
| P1-D 类型归一化桥 | §五 + §九 normalizeKeys | ✅ |
| P1-E 相邻文本拼接查询 | §九 tryConcatSiblings | ✅ |
| P1-F hydration 全 DOM 扫描 | §九 | ✅ |
| P1-G DOM 拦截规格（CharacterData 主通道/Node.prototype/property 表/MutationObserver） | §九 规格表 | ✅ |
| P1-H JSON.parse 全局重盖 | §五"一条规则救五条通道" | ✅ |
| P2-I 验收铁律按档位重述 | §十三 | ✅ |

### 规范提案 ← 第五轮

| 第五轮结论 | 吸收位置 | 状态 |
|------------|----------|------|
| 废弃《终极技术方案》标注 | 文首 | ✅ |
| 控制边机制重写（controlIndex 桥 + L0 无控制边） | §3.6 / §4.3b | ✅ |
| sourceId canonical=原始 URL、模板化降注解 | §4.1/§4.2 | ✅ |
| capability/opt-in 模型 | §4.4"必读,安全命门" | ✅ |
| 删 TC39、删"零开销" | §5.1 三处现实性修正 | ✅ |
| 一致性套件与实现同生 | §5.1/§4.6/§七 | ✅ |
| exact 命名、lastSeenWrite、normative/informative 拆分 | §4.1/§4.5/§4.6 | ✅ |
| subscribe 节流、节点回收、shadow DOM/iframe 边界 | §4.4 | ✅ |
| 调研补遗（安全污点谱系/Whyline/Locker） | §1.3 | ✅ |
| 生命周期入 conformance | §4.6/§4.7 | ✅ |
| 置信度增 collision 档 | §4.5 | ✅ |

---

## 二、残留 P1：新机制的新问题

### P1-1 controlIndex 桥在组件边界处断裂——"L1 闭环"是过度宣称

§八/§4.3b 称 controlIndex + 侧车"让控制边在 L1 闭环(带近似)"。推演覆盖边界：

```jsx
{showDetail && <DetailPanel data={d}/>}
```

`&&` 求值点只知道创建了 `<DetailPanel>` element——**DetailPanel 的函数体此刻还没执行**（reconciliation 时才渲染），它内部要写的文本值（d.title、d.items[0].name）在求值点根本不存在，**无法登记进 controlIndex**。commit 期这些文本写入 → 数据边有，showDetail 的控制边**丢失**。

精确边界：
- ✅ **内联深层 JSX**：`{c && <div><h1>{name}</h1></div>}`——子树表达式求值覆盖了 `{name}`，"Ada" 在求值点可知 → 登记成功；
- ❌ **组件边界**：`{c && <Panel/>}`——Panel 内部一切输出拿不到 c 的控制边。

而"条件包裹卡片/面板/弹窗"恰恰是控制边最高频的真实场景（`{loading && <Skeleton/>}`、`{hasError && <ErrorBoundary UI/>}`）。这不是近似降级，是**整类召回缺失**。

修法二选一（文档必须选边并写明）：
1. **诚实降级（推荐）**：写明"L1 控制边覆盖内联子树；跨组件控制边属 L2"——与 L2 全量位置精化的分工自然衔接；
2. **扩展侧车（贵）**：条件点把 props 对象登记进 `controlIndex`（对象身份 key），然后对"属性读发生在 controlIndex 在册对象上"做传播（`d.title` → 把 controlIndex[d] 转登到 "Title" 值上）——但这要求**全部属性读**带一次 WeakMap 检查，等于把旧架构的逐读税请回来了，与"只插桩条件表达式(~5%)"的开销承诺冲突。若选这条，开销章节要同步改。

### P1-2 "传播侧交集约束"与文档自己的诚实原则矛盾，且"阻止"过度宣称

§四/§3.7 第三条设防：`输出已在册 → 输出护照 = (∪输入) ∩ 已有; 交集为空 → CUT`。两个漏洞：

**漏洞 1：交集为空 ≠ 输出是巧合碰撞。** 反例：`price(7, {price}) * qty(2, {qty}) = 14`，而 `14` 先前已因 `shippingCost=14` 在册。交集 `{price,qty} ∩ {shipping} = ∅` → CUT → 14 保持 `{shipping}`。随后应用渲染总额 "14" → 查出 `{shipping}` → **真实流被静默丢弃，假边反而保留**。按文档自己"碰撞报'可能 x 或 y'是诚实的"的原则，这里应该**并集并标 collision**（`{shipping, price, qty}` 碰撞集），而不是 CUT——CUT 是不诚实的静默。

**漏洞 2："阻止无关字段沿变换链混入"过度宣称。** 交集约束只在**输出值已在册**时生效；`config.maxRetries * 1000 = 7000`（7000 不在册）照常写入并集——中熵数字（7、42、200）不在默认黑名单里，级联只是被**减缓**不是被阻止。措辞改"减缓"，并明确：数字边的终极答案仍是"数字降格"的精度定位，不是这三条设防。

---

## 三、残留 P2：规格缝隙

| # | 问题 | 位置 | 修法 |
|---|------|------|------|
| 1 | **深入浅出 §九 pseudocode 逻辑错误**：`if (!stamp) { 查 controlIndex(value); return; }`——controlIndex 只在数据边 miss 时查。但 `{isVip && <em>{user.name}</em>}` 写 "Ada" 时 valueIndex **命中** → 提前 return → 控制边永远丢 | 深入浅出 §九 | controlIndex 必须**总是**查（WDPP §4.3b 的措辞"除查…也查"是对的，把代码对齐） |
| 2 | **侧车的 SM 从哪来没写**：条件槽位侧车要 `SM(data.user).fields.get('isVip')`，但 §五盖戳只写 valueIndex，**没有写 SM**——旧架构的影子内存半回归了，盖戳节一个字没提 | 深入浅出 §五/§八 | 写明：L1 启用时 stampOrigin **双写** valueIndex + SM（槽位影子的构建成本回来了，开销章节同步） |
| 3 | **controlIndex 无生命周期**：valueIndex 有代际，controlIndex 没有——陈旧控制条目（上一次的 "VIP"→{isVip}）跨 session 命中 | §五/§八 | controlIndex 并入代际机制 |
| 4 | **tryConcatSiblings 缺两条规则**：拼接命中后边挂哪个节点（"LV." 还是 "7"）？两个无关相邻文本碰巧拼成在册值（"hello"+"world"）→ 假边 | §九 | 规则：仅当各节点单独查询 miss 且拼接命中时启用，边挂拼接串中**最后一个**节点，置信度标 `value-match` |
| 5 | **GraphQL 是 sourceId 黑洞**：`POST /graphql` 所有操作共享一个 sourceId（§4.2 还丢了 query/body）——GraphQL 应用的来源归因全灭 | WDPP §4.2 | Producer 规范加 body 检查规则：GraphQL 端点提取 `operationName` → `POST /graphql::<operationName>`；"query 丢弃"改可配置（`/search?q=` 语义化 query 不该丢） |
| 6 | **大响应盖戳阻塞**：10MB JSON 同步递归打标阻塞主线程（go/no-go 只测了交互帧率） | 两文档落地章 | 分片/`scheduler.yield` 打标；go/no-go 加"大列表接口的打标耗时" |
| 7 | **编辑性矛盾**：一句话总结仍写"没有过缝"，但 §八 已承认控制边过缝回归（靠 controlIndex 桥渡过） | 深入浅出 末尾 | 改"数据边没有过缝；控制边经 controlIndex 桥渡过" |

---

## 四、最终判断

1. **可以开工**。六轮评审至此：旧架构 9 个 P0 死于架构转向；新架构 3 个 P0 已全部落进文档；规范层的机制错误（appendChild 控制边）已重写。残留是 2 个 P1（组件边界控制边、交集规则矛盾）+ 7 个 P2——都是"落地时做对"级别，不是"方向重审"级别。
2. **落地顺序建议不变**：L0（一周内可演示）→ L1 数据边 → 控制边两件套。P1-1 建议在 P1 阶段按"诚实降级"处理（写清边界），把跨组件控制边明确让给 L2——这比提前支付逐读税更符合整个架构"低税优先"的立身之本。
3. **评审闭环建议**：两份文档的修订质量说明评审-修订回路是有效的。下一步最高价值动作不再是第七轮纸面评审，而是 **L0 参考实现 + 一致性套件前 20 个 golden case**——让下一轮换一种评审对象（代码，而不是文档）。

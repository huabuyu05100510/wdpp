# 评审报告（第五轮）：《Web数据血缘-最优设计与规范提案》深审

> 评审对象：《Web数据血缘-最优设计与规范提案.md》（WDPP 提案）
> 性质：值追踪地基（《深入浅出》）+ 标准化层（WDPP 协议、CDP 域、W3C/TC39 路径）
> 评审分两条线：A. 地基修正吸收情况（对照第四轮）；B. 规范层自身的新问题。
> 评审日期：2026-07-30

---

## 〇、总裁决

**一句话：规范的野心配得上这个空白，但文档把一栋地基有 3 个未修 P0 的楼直接盖到了第二层，而且第二层自己有一个机制级漏洞——控制边的"过缝"问题回来了，文档却宣称它不存在。**

- **A 线（地基）**：第四轮评审的 3 个 P0 + 5 个 P1 **全部未吸收**（见下表）。§3.4 还在骄傲地说"累加的"，§3.7 还是只有查询侧诚实，§4.3 的 Sink 表还是规格错误版。
- **B 线（规范层）**：§4.3 "节点插入(appendChild) → 控制边" **机制上不成立**——appendChild 发生在 commit 期，条件栈在 render 期早已弹空。数据边靠"值熬过缝"躲掉的问题，控制边（存在性关系，无值可搭车）躲不掉。这是本文最大的新发现。
- **配套关系混乱**：文首说"实现细节见《终极技术方案》"——那是**按位置**的旧架构，与本文**按值**地基互相矛盾，必须标注废弃或重写。

### A 线：第四轮修正吸收情况（逐条核对）

| 第四轮结论 | 本文状态 | 证据 |
|------------|----------|------|
| P0-A 碰撞的传播侧中毒（假阳性级联） | ❌ 未吸收 | §3.7 仍只有"查询侧诚实"；无低熵黑名单、无护照集熔断、无传播侧交集约束 |
| P0-B 控制边布尔构造性失效，"位置精化可选"须反转 | ❌ 未吸收 | §3.6 仍"条件值仍在索引"（查 passport(true)）；§3.7 仍"L2 可选" |
| P0-C 值映射无 GC/生命周期 | ❌ 未吸收 | §3.4 仍"早就在索引里(累加)"——这是 bug 当 feature |
| P1-D 类型归一化桥（DOM 侧永远是字符串） | ❌ 未吸收 | §4.1 `value: Primitive`、§4.3 Sink 直接查值——7 vs "7" 的桥一个字没有 |
| P1-E 相邻文本拼接查询 | ❌ 未吸收 | 无 |
| P1-F SSR 首屏全 DOM 扫描 | ⚠️ 半吸收 | SSR 列为 producer（盖戳），但 hydration 不写 DOM → 首屏零边，无扫描机制 |
| P1-G DOM 拦截规格（Node.prototype/property 表/MutationObserver） | ❌ 未吸收 | §4.3 还是同一张错表 |
| P1-H JSON.parse 全局重盖规则 | ❌ 未吸收 | §3.4 仍说"存储类不用专门拦"——缓存水合退化为全响应并集 |

**结论：本文不能直接作为"最优设计"发布——先把第四轮的 9 项修正落进地基层（L0/L1 的协议描述里），否则规范化的是一栋危楼。**

---

## 一、B 线 P0：控制边的过缝问题回归——§4.3 机制不成立

### 问题

§3.6 声称控制边"不涉及过缝/持久化难题"。§4.3 给出机制："节点插入(appendChild 等) → 控制边(存在性)"。推演：

```jsx
{isVip && <em>VIP</em>}
```

1. **render 期**（用户代码，L1 插桩）：求值 isVip → pcPush → 创建 element → pcPop。**此刻没有任何 DOM 节点存在。**
2. **commit 期**（React 内部，无插桩）：`appendChild(emNode)` 触发拦截。**此刻 pcStack 早已弹空**——拦截器拿不到 isVip。

数据边能躲过缝，是因为**值本身熬过了缝**（render 期的 "Ada" 和 commit 期写入的是同一个字符串，查值即得）。控制边是**存在性关系**——"VIP" 这个字面量的护照是空的，没有任何值可以搭 condition 的便车。**过缝问题对控制边原样回归，而本架构恰恰以"消解过缝"为立身之本。**

附带推论：**L0（零插桩）没有条件栈，控制边能力为零**——§4.6 的 L0 一致性表没写明这一点，按现文 L0 实现的 appendChild 拦截只能画出"所有节点都有控制边∅"的废话。

### 修复：controlIndex 桥（唯一不依赖框架的机制）

L1 的 `&&`/三元/if 插桩在**求值点**同时知道两件事：条件的护照、被条件控制的 JSX 子树的文本/属性值。在求值点把它们登记进一本**按值的控制索引**：

```ts
// 编译期：{isVip && <em>VIP</em>}
// → 求值时: if (isVip) controlIndex.add("VIP", isVipPassport)
// DOM 写 "VIP" 时: 查 valueIndex(数据边) + controlIndex(控制边)
```

必须诚实标注的两条降级：
1. **字面量碰撞**："VIP"、"加载中"、"暂无数据" 这类字面量在多处出现、各自由不同条件控制 → controlIndex 按值合并 → 噪声。这类边的置信度应标 `approx`；**唯一解仍是按位置**（L2），所以"条件槽位侧车"（第四轮 P0-B 方案）要从"可选"提为 **L1 的组成部分**——它管"条件护照是什么"，controlIndex 管"护照到哪个节点"，两者配套控制边才闭环。
2. **同值异处**："Ada" 一处在条件下渲染、一处无条件 → 无条件处被误画控制边。may-分析语义下可接受，标 `value-match`。

协议层修订：§4.3 删掉"appendChild → 控制边"一行（它是错的），改为"控制边由 L1 条件插桩经 controlIndex 按值归因"；§4.6 L0 行明确"L0 = 仅数据边"。

---

## 二、B 线 P1：sourceId 路由模板化是互操作命门，spec 不能留白

§4.2 给了规则雏形："数字段→`:id`、UUID→`:uuid`"。作为实现启发式可以，**作为协议不行**：

- slug（`/posts/hello-world`）、日期（`/logs/2026-07-30`）、复合键（`/repos/a:b`）、短 hash——归一化规则每个实现各猜各的；
- SourceRef 是协议的**主键**：两个工具对同一接口产出不同 sourceId，互操作当场死亡（A 工具的 `GET /users/:id` 对不上 B 工具的 `GET /users/42`）。

**规范工艺上的正确做法**：canonical form 用**原始 URL**（无模板化，各实现必一致），路由模板降级为**可选注解字段**（`routeTemplate?: string`，生成规则 informative 而非 normative）。字段级归并由消费端自行聚合。

## 三、B 线 P1：安全/隐私面——这既是现在的问题，也是标准化最大的墙

`window.__wdpp__` 把**全量 API 字段名、来源端点、页面值与字段的对应关系**暴露给页面上的**任何脚本**（第三方 SDK、浏览器扩展、注入的广告脚本）。这是一份现成的数据外泄地图：往耳只给值，这还给"值从哪个接口哪张表来"。

- **现在（工具形态）**：必须 opt-in——只在 dev/显式开启时挂载，文档写死；
- **标准化后**：协议必须定义 **capability 模型**（如 `<meta name="wdpp" content="on">` 或 DevTools 开启时才暴露），否则浏览器厂商第一轮安全评审就会毙掉；
- **§5.1 阶段 3"浏览器原生追踪,零开销"技术上天真**：V8 的原始值是无盒立即数，值级 provenance 只能靠侧表，内存/GC 代价非零；且值级追踪有侧信道含义（Spectre 之后浏览器对"跨执行上下文的信息流可见性"极度敏感——这正是安全污点追踪从未进浏览器的原因，见 §五调研补遗）。可信的表述是"**DevTools 模式下按需开启、低开销**"，类比 sampling profiler，而不是"零开销"。

## 四、B 线 P1：标准化路径的现实性修正

§5.1 三阶段的骨架（参考实现 → 工具采纳 → 平台原生）是对的，但三处要修：

1. **WICG 缺席**。Web 标准的现实路径是 WICG 孵化 →（有实现兴趣后）W3C 工作组。直接写"W3C/TC39 标准化"会暴露路径不熟。**TC39 应基本放弃**：JS 语言级 provenance 原语 ≡ 语言级污点，安全社区讨论多年从未推进，且本提案的价值根本不需要语言改动（引擎外全可实现）。把 TC39 从路线图删掉是加分不是减分。
2. **一致性测试套件要提前**。§七把它放在 P4（最后）——但 L0/L1/L2 的**唯一意义**就是"不同实现产出可对比的结果"，没有套件这三个级别只是营销词。套件应该和参考实现**同生**（P0/P1），每个协议条款配 golden case——这也正好和"验收铁律：正例记边+反例不记边"合流。
3. **成功标准是多实现**。阶段 2 的目标不该只是"DevTools 扩展消费 API"，而是**催生第二个独立实现**（哪怕是 Vue 社区的）。单实现的"协议"是 API 文档，不是标准——Source Maps 成为标准是因为 Chrome/FF/工具链各自实现后对齐，而非先有完美 spec。

## 五、B 线 P2：规范工艺细节与调研补遗

### 5.1 规范工艺

| # | 问题 | 修法 |
|---|------|------|
| 1 | 置信度命名 `taint`：L0/L1 根本没有污点追踪，"身份匹配/唯一值指纹"叫 taint 名不符实，且给安全审读错误信号 | 改 `exact` |
| 2 | `lastSeenCommit`："commit" 是 React 术语，出现在"框架无关"协议里 | 改单调写序号 `lastSeenWrite: number` |
| 3 | §4.6 一致性表把"召回 ~60%/~90%"写成**承诺** | 召回依赖 app 数据形状，不能进 normative 文本；拆成"机制一致性(normative) + 参考召回率(informative 注记)" |
| 4 | §4.1 `ProvenanceRecord.value` 对控制边无意义（存在性无值） | value 标 optional，或控制边单独 record 类型 |
| 5 | `subscribe()` 无背压：高频 DOM 写（打字、列表滚动）事件洪水 | 协议注明批量/节流语义（如 microtask 合批） |
| 6 | `lookup(node)` 的 node 生命周期：节点移除后记录残留 | 与第四轮 MutationObserver 清理合流，协议写明"实现必须回收 detached 节点的记录" |
| 7 | shadow DOM：TreeWalker 扫描/MutationObserver 默认不进 shadow root | 协议注明 pierce 语义（或显式声明不支持 open shadow root 以内） |
| 8 | 跨 iframe：SOP 下父页查不到子帧节点；每 realm 独立 `__wdpp__` | 协议写明 realm 边界 |

### 5.2 调研补遗（§1.2 的空白论证要补两段历史，否则评委会替你想起来）

1. **安全污点谱系就是同一项技术**：DOM-XSS 动态污点（Kudzu 等学术研究、DOMinator 类工具、Contrast/Checkmarx IAST——文中已列后两者但未点破同构）做的就是"source 字段 → DOM sink"。**它没有标准化——这才是提案最该分析的案例**：开销宣称不实、精度（碰撞/过染）不敢诚实、只有安全合规拉动没有开发者日常拉动。本提案的差异点（dev UX 拉动、置信度诚实、分层采纳）应该对着这个失败案例写，而不是假装谱系不存在。
2. **调试 provenance 研究**：Whyline（"why did this output happen"）、theseus（JS omniscient debugger）一脉答的也是"输出的来源"。引用它们能把"领域空白"说得更准确：**空白不在"思想"，在"可用的、低税的、dev 工具形态的实现"**——这个定位比现在"没有任何方案"的绝对化表述更经得起质询。
3. **运行环境边界**：Locker Service / Lightning Web Security 等**冻结原型链**的安全沙箱里，L0 的原型 patch 全灭（fetch/Node.prototype 不可改）——写进"做不到"清单；CSP 不影响（patch 是 JS 内行为）。

---

## 六、修订清单（按优先级）

1. **【P0】把第四轮 9 项修正落进 L0/L1 协议描述**（低熵策略/条件槽位侧车/生命周期/归一化桥/拼接查询/SSR 扫描/DOM 规格/JSON.parse 重盖）——本文前三章实质上要回炉；
2. **【P0】控制边机制重写**：删 "appendChild→控制边"，改为 controlIndex 桥 + L0 明确无控制边 + 条件槽位侧车升入 L1（本文 §一）；
3. **【P1】sourceId canonical = 原始 URL，模板化降为注解**（§二）；
4. **【P1】capability/opt-in 模型入协议 + 删除"浏览器零开销"和 TC39 路线**（§三、四）；
5. **【P1】一致性测试套件提前至 P0/P1，与参考实现同生**（§四）；
6. **【P2】规范工艺八项**（§5.1）+ **调研补两段谱系**（§5.2）；
7. **【P2】文首配套关系修正**：《终极技术方案》（按位置旧架构）标注"已废弃，由《深入浅出》地基取代"，或明确它只剩历史价值。

## 七、最终判断

- **空白判断：成立**（修正表述后：空白在"dev 工具形态的值级血缘"，思想谱系在安全污点与调试研究）；
- **地基：可行但带伤**——第四轮 9 项修正落地前，"最优设计"名不符实；
- **规范层：方向对、节奏错**——先套件与多实现，后 WICG，别碰 TC39，别承诺零开销；
- **最大单点风险仍是控制边**——它是唯一无法纯按值闭环的关系，controlIndex 桥 + 条件槽位侧车的组合是目前的最佳解，但要在协议里诚实写下它的近似本质。

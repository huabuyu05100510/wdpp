# 评审报告（第三轮）：盖戳层、控制边对称性与图语义深审

> 评审对象：《终极技术方案-第一性原理重构.md》§1.1/§5.1（stampOrigin）、§0.1（边的定义）、§6（walkthrough）、§8.2（异步）、§9（落地）
> 前置文档：第一轮（收口侧 8 P0）、第二轮（传播侧 P0-9 + 词法 ctx + 插桩清单补缺）
> 本轮视角：数据入口层 + 文档内部自洽性 + "图"本身作为产品的语义缺口。
> 评审日期：2026-07-30

---

## 〇、本轮裁决

前两轮证明"边画不上、标签活不到"。本轮发现三个新维度的问题：

1. **文档自相矛盾**：§5.1 的 stampOrigin 实现与 §6 walkthrough 对同一数据结构给出了**两种不同的标签内容**——说明文档未经过可执行验证；
2. **控制边定义不对称**：§0.1 自己定义"控制边 = 值决定渲不渲染"，但实现对 else 分支/三元否支/早退之后的代码**不画控制边**——按文档自己的定义这是召回漏洞；
3. **对第一轮修复方案的再修正**：P0-4 的属性写修复（`if (e$ !== 0n)`）不完整，直接 mutate API 对象会产生假阳性。

---

## 一、P1：§5.1 实现与 §6 示例硬矛盾——槽位是"自身 id"还是"子树并集"

§6 walkthrough 阶段 1 声称：

```
SM.get(data).fields:
  "user" → bit(1) | bit(2) | bit(3)     (user.name + user.level + user.isVip)
```

但 §5.1 的实现是：

```js
sh.fields.set(k, union(sh.fields.get(k) ?? 0n, bit(id)));  // 只并当前字段自己的 id
```

按实现，`SM(data).fields["user"]` 只有 `bit(id_"...user")` 这一个中间节点位，**没有子树并集**。两者必错其一。

这不是文档笔误问题，两种语义有实质分歧，分歧在**整对象作为值流出**时暴露：

```jsx
const { user } = data;
<pre>{JSON.stringify(user)}</pre>   // 这段文本来自哪些字段？
<Child user={user} />               // P0-2 修复后 props 的 user 槽 = ?
```

- 单点语义（实现）：JSON 文本的边指向中间节点 `…user`——图上冒出一堆非叶子节点，用户指着文本得到回答"来自 user"，信息量大减；
- 并集语义（示例）：边指向全部叶子字段——符合用户直觉，但要求 stampOrigin **后序遍历**：先递归子对象，再把子槽位并集冒泡到父槽。

**修复**（并集语义，后序聚合）：

```js
function stampOrigin(obj, sourceId, path = '', visited = new WeakSet()) {
  if (obj == null || typeof obj !== 'object' || visited.has(obj)) return 0n;
  visited.add(obj);
  if (Object.isExtensible(obj)) obj[TAINT_FLAG] = true;   // 第一轮 P1-9 的守卫并入
  const sh = ensureShadow(obj);
  for (const [k, v] of Object.entries(obj)) {
    const id = getOrCreateFieldId(joinPath(sourceId, path, k));  // 见第三节路径转义
    let slot = union(sh.fields.get(k) ?? 0n, bit(id));
    if (v && typeof v === 'object') {
      slot |= stampOrigin(v, sourceId, `${path}${k}.`, visited);  // 子树并集冒泡
      // 数组再并 length 槽（第一轮 P1-13）
    }
    sh.fields.set(k, slot);
    if (Array.isArray(obj)) sh.fields.set('length', union(sh.fields.get('length') ?? 0n, slot));
  }
  // 返回本对象全部槽位的并集，供父层冒泡
  let all = 0n;
  for (const v of sh.fields.values()) all |= v;
  return all;
}
```

注意对象引用传递（`<Child user={user}>` 后 `user.name`）在两种语义下都正确（慢道按引用逐层读槽）——所以此修复只影响整对象流出场景，与 P0-2 的 props 修复正交。

---

## 二、P1：控制边不对称——else 分支按文档自己的定义也该有边

§0.1 定义：**控制边 = 字段的值决定了"渲不渲染"**。据此：

```jsx
{loading ? <Spinner/> : <Content/>}   // Content 渲不渲染由 loading 决定 ✅ 应有控制边
{isVip && <Gold/>}                     // 已覆盖 ✅
if (isVip) return <Gold/>;
return <Normal/>;                      // Normal 恰恰因为 isVip 为假才渲染 ✅ 应有控制边
```

但 §4.3 的实现只在 if 块/三元真支内 pcPush——**否支和早退之后的代码 pcStack 是空的**。
§4.3 的编译示例里，第二个 `return <div><h1>{name}</h1></div>` 就走出了 pcPush 的作用域，`isVip` 对普通版 div 的控制边丢失。而"加载态/空态/内容态三态切换"恰恰是用户最常指着一个东西问"这是哪个字段控制的"的场景。

**修复（分两档）**：

- **廉价档（必须做）**：三元与 if-else **双支对称 push**——`cond ? A : B` 编译为 A、B 两个分支都在 `pcPush(cond$)` 窗口内求值；
- **完整档（早退模式）**：`if (cond) return/throw/break/continue` 之后、同一作用域的**剩余语句**都隐含被 cond 控制。Babel 转换策略：检测到分支以跳转语句结尾时，将后续语句包入 else 块（或维护"区域 P"，在进入作用域剩余部分时 union 进 pcStack）。`if (a) return X; if (b) return Y; return Z;` 中 Z 的 P = a$ ∪ b$。

验收反例：`{loading ? <Spinner/> : <Content/>}` → Content 节点必须有 loading 控制边；`if (err) return <Error/>; return <Main/>` → Main 必须有 err 控制边。

---

## 三、P0-4 修复的再修正：属性写必须是"替换"而非"条件写入"

第一轮 P0-4 把属性写条件从 `if (a[TAINT_FLAG])` 修正为 `if (e$ !== 0n)`。**仍然不完整**：

```js
data.user.name = "Bob";    // 用户代码直接 mutate API 对象（或表单双向绑定）
// e$ = 0n（字面量）→ 按"if (e$ !== 0n)"不写影子 → name 槽仍残留 bit(1)
render(user.name);          // 渲染 "Bob"，却画出 user.name → API 的数据边 → 假阳性
```

重置场景同样：`form.name = ''`、`state.count = 0`——旧标签必须随写入**失效**。

**再修正**：属性写插桩无条件执行 `slowSetProp(a, k, e$)`——**e$ 为 0n 时删除槽位**（替换语义），TAINT_FLAG 的打标仍按 e$ ≠ 0n 驱动。数组元素覆盖 `users[0] = localItem` 同理。这与第二轮的 `delete` 清槽是同一语义的两个入口：**槽位内容必须始终等于"最后一次写入的值的标签"**。

---

## 四、P1：stampOrigin 无循环引用保护

§9 P3 明确"WebSocket/SSE/IndexedDB → 暴露 stampOrigin 手动接"。手动接入的数据**不保证是 JSON 形状**——已水合的 store、带引用的领域对象、ORM 实体都可能有循环：

```js
stampOrigin(storeState, 'ws://...');   // 循环引用 → 无限递归 → 栈溢出，页面崩
```

修复已并入第一节的代码（`visited: WeakSet`）。验收：含循环对象打标不崩、各槽位标签正确。

---

## 五、P1：字段路径 key 歧义——合法 JSON 字段名含 `.`

```json
{ "user.name": "Ada", "user": { "name": "Bob" } }
```

`"GET /x.user.name"` 同时是这两个字段的路径字符串 → **共享 fieldId → 两个字段的边互相污染（假阳性）**。字段名还可以含 `\0` 以外的任意字符。

**修复**：fieldRegistry 的 key 不用字符串拼接，用分段数组 `JSON.stringify([sourceId, ...segments])`；UI 展示时再格式化成 `user.name`。同时把 `resolveSourceId` 的 URL 模板化规则（数字/UUID 段 → `:param`）写进文档——目前全文只有函数名没有规则。

---

## 六、P2：异步通道具体化（§8.2 只有一行，落到设计）

```js
async function load() {
  const u = await getUser();        // getUser 返回 Promise<原始值>
  setName(u);                        // u 的标签在哪？
}
```

需要三个补丁点（都比想象中便宜）：

1. **async 函数返回**：`return expr` 且 expr$ ≠ 0n 时 `SM.set(返回的promise, expr$)`——在 async 函数出口插桩（返回的 promise 对象是语言创建的，可在 `return` 处用 helper 包一层：`return __runtime.packPromise(expr, expr$)`，对 async 函数即包装其返回值 promise 化之前的值——实际做法：async 函数体重写为对返回值 `.then` 打标）；
2. **`await p`**：`v = await p` → `v$ = SM.get(p) ?? 0n`；
3. **`Promise.prototype.then/catch/finally` patch**：回调参数影子从 `SM.get(promise)` 注入（与 P0-3 参数通道同机制）；`Promise.all/race/allSettled` 结果数组逐槽位或取并集。

---

## 七、P2：jsxDEV 全参数转发——dev 下丢 source/self 会让 React 警告系统退化

Vite dev 模式 JSX 编译目标是 `jsxDEV(type, props, key, isStaticChildren, source, self)`。自定义 `__runtime.jsx` 若只转发三参：

- `source`（`__source`：文件名/行号）丢失 → React 的 key 警告、act 警告全部丢失位置信息；
- `self`/`owner` 丢失 → "component was created by X" 警告退化；
- 第二轮已指出 key 必须转发。

修复：`__runtime.jsxDEV(type, props, key, isStaticChildren, source, self, stamps)` 原样透传前六参。这是 dev 体验问题但影响每天使用此工具的开发者。

---

## 八、P2：组件识别启发式覆盖不全

Babel 注入 enterComponent 依赖"识别组件"，目前文档未列规则。必须覆盖：

- `forwardRef((props, ref) => ...)`、`memo(fn)`、`memo(forwardRef(...))`——内层函数才是渲染体；
- `export default function() {...}` 匿名默认导出；
- 大写命名但返回 null 的普通函数——误注入无害（多一对 push/pop）；
- 小写命名但返回 JSX 的 helper——**漏注入也无害**（按第二轮词法修正，它闭包捕获外层 __ctx，本就是期望行为）。

结论：识别规则"宁可错杀（多注入）不可漏判"？——不，对 forwardRef/memo 必须识别，否则 hooks 包装（P4）错位导致 hooks 顺序破坏。给一张判定表。

---

## 九、P2：图语义的三个产品级缺口

### 9.1 边存储只有单向索引，点选场景查不到

`recordEdge(fieldId, domNode, type)` 是 fieldId→node 方向。但产品的核心交互是**用户点 DOM 节点 → 反查字段**——需要 node→fields 反向索引（WeakMap<Node, Map<fieldId, type>>）。第一轮 #18 的边清理也依赖反向索引（按节点清边）。现在补比事后重构便宜得多。

### 9.2 时间维度未定义

tab 切换后同一 DOM 位置先后被 `feedA.title` 和 `feedB.title` 驱动——边是覆盖还是叠加？叠加则图越来越脏（第一轮 #18），覆盖则丢失"这个节点可以由多个字段驱动"的真实信息。建议：边上带 `lastSeenCommit`，UI 默认只显示当前 commit 的边，可切历史视图。

### 9.3 字段级 vs 记录级血缘

`resolveSourceId` 把 `/users/1`、`/users/2` 归一为 `GET /users/:id` → 两个用户的 name 共享 fieldId。用户指着列表里某一行问，只能回答"来自 user.name"，回答不了"来自**第 2 条记录**的 user.name"。这是精度设计决策而非 bug，但值得现在定：建议 fieldId 保持字段级（位图不爆炸），边上可选携带 `entityKey`（数组路径 index 或 id 字段值），UI 高亮同记录的其他边——低成本、体验跃升。

---

## 十、P2：工具链与体验补遗

| # | 问题 | 对策 |
|---|------|------|
| 1 | **Apollo Client 基本不可支持**：normalized cache 把对象拆散重组（库内、未插桩、原始值断流）+ dev deep-freeze（第一轮 P1-9） | 明确写入"不支持清单"，UI 检测 Apollo 时提示；zustand/Redux(非 immer 路径) 因**对象引用传递天然免疫**（传播靠引用+SM，不经过库代码）基本可用——这个洞察写进文档能避免过度恐慌 |
| 2 | **插桩后调试体验**：step-into 全是插桩代码，调用栈膨胀 | sourcemap 保证行号映射；提供 `instrument.ignore: ['**/vendor/**']` 配置；shadow 变量用不可枚举命名约定（`__$`）便于调试器折叠 |
| 3 | **C4 约束的场景扩展**：staging/QA 环境给 PM 用 | 3-8× 开销对非开发机可接受；插桩通过 env 开关进 staging build——文档把 dev-only 写死了，值得放宽为"非生产" |
| 4 | **§6 阶段 3 示例有语法错误**：`pcPop(__ctx), __runtime.jsx(...) : false` 三元分支粘连 | 全文代码示例应抽进 golden 测试可执行化——三个"自证正确性"出错（freeze、lanes、属性写）已经证明纸面自证不可靠 |
| 5 | **Vite optimizeDeps.exclude 的替代路线**：exclude 导致数百模块请求、冷启动恶化（文档自列为 go/no-go） | 备选：不 exclude，Babel 插件用 `include: /node_modules\/(lodash-es\|immer\|zod)/` 直接转换预打包产物——两条路线在 P0 各花半天实测对比 |

---

## 十一、三轮累计与最终开工门槛

| 轮次 | 维度 | P0 | 关键 P1 |
|------|------|-----|---------|
| 一 | React 收口 + 架构 | 8 | freeze、falsy、custom hooks、XHR 系、组件返回原始值 |
| 二 | 传播机制 | 1（P0-9 方法黑洞） | 词法 ctx、赋值/for-of/delete/&&·‖ 缺行、normalizeStamp |
| 三 | 盖戳层 + 图语义 | 0 | 槽位并集矛盾、控制边不对称、写入替换语义、循环保护、路径转义 |

**文档修订清单**（开工前必须落入原方案）：
1. §1.1/§5.1：后序并集 + visited 守卫 + isExtensible 守卫 + length 槽 + 路径分段 key（本轮一/四/五）；
2. §0.1/§4.3：控制边对称化（双支 push + 早退区域 P）（本轮二）；
3. §1.3/§4.2：写入替换语义（本轮三）+ 第二轮第六节 diff 全表；
4. §2.2：词法 ctx 为主、动态栈为辅（第二轮二）；
5. §2.4：setter 包装语义修复 + arg$ 显式传递（第一轮 P0-5、第二轮 P1-12）；
6. §5.6：alternate 身份比较剪枝 + 边双向索引 + 边清理 + 自注入 devtools hook（第一轮 P0-7/8、本轮 9.1）；
7. §6：全章按修正后语义重写，示例进 golden 测试（本轮十.4）；
8. 新增章节：不支持清单（Apollo、Proxy 深链、组件返回原始值的降级行为）+ 图语义（时间维度、记录级）（本轮九）。

**架构层面的最终判断不变**：方向可行，混合架构（污点 + 值指纹）是最优解；但原方案"每个机制都回答为什么必须是这个"的目标，要靠把上述 25+ 处修正全部消化后才能重新宣称。

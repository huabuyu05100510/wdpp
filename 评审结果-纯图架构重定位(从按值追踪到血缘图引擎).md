# 评审结果:纯图架构重定位(从按值追踪到血缘图引擎)

> 状态:**已结题**。最终推荐:**Universal Taint Union + 图存储/查询层**(runtime 图引擎,非静态图,亦非 Jalangi 风格 taint graph)。
> 本文件保存**全部讨论轨迹与认知修正**,作为后续设计与决策的存档。
> 评议纪律(来自 memory):第一性原理、通用不 by case、精确做不到就诚实降级块级并标置信度。

---

## 0. 提议与初始假设

**原提议**:把现行运行时实现从 **按值追踪** 重定位为 **血缘图引擎**(纯图架构)。

**当前实现**(评议起点):
- `impl/l0/src/value-index.js`: `Map<值, passport>` 中心
- `impl/l0/src/stamp-origin.js`: 网络拦截 + 递归盖戳
- `impl/l0/src/dom-sink.js`: DOM 写入拦截 + 值反查护照
- `impl/l0/src/component-bind.js`: 块级归因兜底
- `impl/l0/src/babel-runtime.js`: 运行时 helpers(`__recover` / `__passthrough` / `__readProp` 等)
- 已有 104 conformance 测试 + antd/rwa 真实 app 测试床(antd 100%/100%, rwa 88%/100%)

---

## 1. 评议历程(6 轮,4 次认知修正)

### 第 1 轮:v1 评议(误判为 Jalangi 风格 taint graph)

**我的解读**:运行时操作级 taint graph(每个赋值/运算/调用 = 图节点)

**判定**:❌ 不可行
- 同 DESIGN.md §14 已否决的 Jalangi 方案
- §1.4 opt-in 严重违反(库内必须插桩)
- §11.6"千行表格 2-10x 慢"场景默认发生

**文档**:`深审-纯图架构重定位(从按值追踪到血缘图引擎).md`

### 第 2 轮:v2 评议(澄清为静态图)

**用户澄清**:提议是 `DESIGN-STATIC.md` 思路,**静态图**(build-time 抽象解释),非运行时 taint graph。

**判定**:✅ 可行
- 16 条语言级规则覆盖所有 JS 构造
- 零运行时归因,库内天然免疫
- 已有完整文档(12 章 + 15 case + 5 期落地)

**问题**:不是"运行时引擎",粒度止于"组件级 / 列表项 API 级",**不做记录级 / cell did**。

**推荐**:静态图为新主方案,运行时按值为辅,双轨并行。

**文档**:`深审-纯图架构重定位(静态图,DESIGN-STATIC思路).md`

### 第 3 轮:用户挑战"对象 vs 值"(架构诊断被纠正)⚠️ **认知修正 #1**

**用户原话**:"为什么会有按值的问题呢 我们对数据追踪 追不到的某些case 进行hack 不存在对值分析的情况啊 哪些情况下需要值分析呢"

**我的修正**:
- 现行架构**不是**"按值追踪",**是**"按对象/字段追踪 + 边界 fallback"
- 真实分层:
  - Layer 1 主追踪(对象/字段):WeakMap + 非枚举属性 + SM 字段槽位 + fiberReads——**无 by case 问题**
  - Layer 2 DOM 边界兜底:值 lookup + fiber walk + 块级——**hack 出现在这里**
  - Layer 3 序列化边界兜底:`__passthrough` + byVal——**hack 出现在这里**
- byVal 碰撞等**不是 Layer 1 的本性**,是 Layer 2/3 的 fallback 过近似

### 第 4 轮:用户追问"100% 覆盖"(我混淆 coverage 与 precision)⚠️ **认知修正 #2**

**用户原话**:"为什么不能100%覆盖呢 我们不是想尽办法建立图吗 对象和值有什么区别吗 另外有哪些case是覆盖不到 或者会断路呢"

**我的修正**:
- **100% coverage 可达**(apply universal rule 后)
- **100% precision 不可达**(同值不同源,信息论上限)
- 我之前说的"信息论上限"是混淆
- 列了 8 类断点,但每类**都可用 taint union 在边界解决**——不是真断点

### 第 5 轮:用户暗示"输入参与了 → 输出就继承"⚠️ **认知修正 #3**

**用户原话**:"那么我们再会断的地方hack呢 只要输入参与了 那么输出就认为有 / 能精确就精确 不能就块级 就与输入一致"

**用户的 hack 原则**:
```
output = f(inputs) → provenance(output) = ∪ provenance(inputs)
```

**这是 taint 语义的普适规则**:
- 不分应用层:runtime / static 通用
- 不分 AST 类型:任何 input/output 关系
- 不分操作类型:函数、运算、读写、控制流都适用
- 失败模式仅在"输入不可观测"或"输出不可观察"时

**我的修正**:
- 这是 **universal taint union**,不是某个特定方案
- 当前架构已经多处应用(`__recover` / `__passthrough` / `__aggr`)
- **缺口在 babel plugin 覆盖不全**——7 个关键 AST 节点未覆盖
- **不需要"重定位架构",需要"扩展 babel plugin 到 universal coverage"**

### 第 6 轮:用户选择"按图的架构"作为最终方向

**用户原话**:"现将评审结果保存 另外详细设计按图的架构 以及应该对哪些做hack"

**解读**:用户接受"按图的架构"作为存储/查询层,**与 universal taint union 兼容**(图是表达,union 是规则)。

**最终推荐**:**Universal Taint Union(图存储) + 图查询**:
- 数据结构:图(节点 + 边)
- 核心规则:Universal Taint Union
- 入口:Producer(网络拦截 + stamp origin)
- 中继:Transform(babel + universal helpers)
- 出口:Sink(DOM 拦截 + onDomWrite)
- 查询:Query API(图遍历 + 置信度过滤)

---

## 2. 关键发现(总览)

### 2.1 术语澄清 ⚠️

| 我之前说的 | 实际是什么 |
|---|---|
| "现行是按值追踪" | 现行是**按对象/字段追踪 + 边界 fallback**。"按值"只出现在 Layer 2/3 |
| "byVal 是按值的代价" | byVal 是 Layer 3 序列化边界断后的**fallback over-approx**,不是 Layer 1 的本性 |
| "深拷贝断身份是按值的限制" | 深拷贝断身份是 WeakMap 按对象身份,**任何追踪方案都受影响**,不是按值的限制 |

### 2.2 Coverage vs Precision ⚠️

| 维度 | 可达? | 说明 |
|---|---|---|
| **Coverage** | ✅ **100% 可达** | apply universal taint union,任何参与数据流的输入,都进入输出 |
| **Precision** | ❌ **100% 不可达** | 同值不同源,信息论上限,只能 over-approx |

我之前把"信息论上限"误扩展到 coverage。**这是错的**。

### 2.3 Universal Taint Union 原则

```
output = f(inputs) → provenance(output) = ∪ provenance(inputs)
```

**应用**:
- 任何 input/output 边界(函数调用、运算、读写、控制流)
- 任何观察层(runtime / static)
- 任何操作类型

**失败模式**:
- 输入不可观测(代码不可见,如 WASM 内部)
- 输出不可观察(void / write-only)
- 此时用 over-approx 兜底:⚪ unknown 或 🟡 union

### 2.4 当前 babel plugin 缺口

**14 个现有 visitor,缺至少 7 个关键 AST 节点**:

| 节点 | 影响 | 优先级 |
|---|---|---|
| `AssignmentExpression` `x = y` | `x` 的 taint 不继承 `y` | 高 |
| `MemberExpression` 写 `obj.x = y` | **关键缺口**:field mutation 不传 taint | **最高** |
| `Destructuring` `const {a} = obj` | `a` 不继承 `obj.a` 的 taint | 高 |
| `VariableDeclarator` with member init `const x = obj.a` | `x` 不继承 `obj.a` 的 taint | 中 |
| `ThrowStatement` `throw x` | 抛出值 taint 丢失 | 中 |
| `OptionalChain` `a?.b` | 链路断 | 中 |
| `ClassProperty` `class { x = y }` | field init taint 丢失 | 低 |
| `TaggedTemplate` `` tag`${x}` `` | tag 函数收到 x 但 taint 不传 | 低 |
| `NullishCoalescing` `a ?? b` | 短路结果 taint 选错 | 中 |
| `UpdateExpression` `x++` | 自循环,影响小 | 低 |
| `DeleteExpression` `delete obj.x` | field taint 删不掉 | 中 |

### 2.5 按图 vs 按值的本质

| 维度 | 按值(值索引) | 按图(图) |
|---|---|---|
| 抽象 | value content | operation + edges |
| 索引 | Map<value, passport> | adjacency list |
| 碰撞 | ❌ 同值不同源无法区分 | ✅ 图节点独立,无碰撞 |
| 链追溯 | ❌ 需要额外维护 | ✅ 图遍历天然 |
| 复杂度 | O(1) lookup | O(degree) BFS |
| 内存 | O(unique values) | O(nodes + edges) |

**结论**:图是表达方式,值是索引方式。**两者不互斥**——值索引可作 fast-path(图的 secondary index)。

---

## 3. 最终推荐

### 3.1 核心方案:Universal Taint Union + Graph Engine

```
┌─────────────────────────────────────────────────────────────┐
│ Graph Engine (Core)                                          │
│   节点:ApiSource / Field / Operation / ControlFrame / Dom   │
│   边:produces / derived-from / writes-to / conditional-by   │
│   规则:Universal Taint Union(output = ∪ inputs)            │
└─────────────────────────────────────────────────────────────┘
            ↑                ↑              ↑
┌───────────┴──┐    ┌────────┴──────┐  ┌───┴──────────┐
│ Producer     │    │ Transform     │  │ Sink         │
│  网络拦截    │    │  babel +      │  │  DOM 写入    │
│  stampOrigin │    │  universal    │  │  onDomWrite  │
│              │    │  helpers      │    │              │
└──────────────┘    └───────────────┘  └──────────────┘
                          ↑
                ┌─────────┴─────────┐
                │ Hack Layer        │
                │  7+ AST visitor   │
                │  opaque 库白名单   │
                │  序列化边界        │
                │  async / control  │
                │  全局副作用        │
                └───────────────────┘
```

### 3.2 关键设计决策

| 决策点 | 选择 | 理由 |
|---|---|---|
| 运行时 vs 静态 | **运行时**(babel + helper) | 用户最终选择 + 已有 104 测试资产 + did 能力 |
| 存储:图 vs 值 | **图为主** + 值索引为 fast-path | 表达力 vs 性能权衡 |
| 规则:per-case vs universal | **universal taint union** | 通用不 by case(memory 铁律) |
| 边界断点处理 | **hack 而非放弃** | 用户明确"hack at broken places" |
| 置信度分级 | 🟢 / 🟡 / ⚪ / 🔵 | 既诚实又分级 |

### 3.3 不推荐的方案

| 方案 | 不推荐原因 |
|---|---|
| 单纯按值追踪 | 碰撞 + 低熵失效(虽然只是 Layer 2/3 的 fallback) |
| 单纯静态图(DESIGN-STATIC.md) | 无 did 能力,与已有 104 测试 + antd/rwa 资产脱节 |
| 单纯运行时 taint graph(Jalangi) | §14 已否决,爆炸半径 + 库内墙 |
| 重定位到完全不同的架构 | **错的命题**——架构本性是对的,缺的是 babel plugin 覆盖 |

### 3.4 行动建议

1. **补 babel plugin 7 个新 visitor**(universal taint union 应用)
2. **迁移存储从 Map<值, passport> 到 Graph**(值索引保留作 fast-path)
3. **增加 universal taint conformance 测试**(50+ case)
4. **antd/rwa 测试床验证**(看补完后的 precision/recall)
5. **可选:叠加静态图作为 audit 工具**(lint / code review / 文档生成)

---

## 4. 我的反思(评议方)

我连续两轮在用"按值 vs 按图"的二分法看问题。**真正的二分法是**:
- "当前覆盖不全" vs "universal coverage"
- "per-case hack" vs "universal rule"

**Universal Taint Union 是 universal rule**,适用 runtime 也适用 static。**不是 runtime vs static 的对立**。

我推荐"静态图"是因为没看到 universal taint union 也能达到同样效果。用户几条追问让我意识到 runtime 这条路也能通,且成本更低。

**用户的追问改变了我的诊断**:
- 第 3 轮:纠正"按值追踪"的误诊
- 第 4 轮:纠正"coverage vs precision"的混淆
- 第 5 轮:暴露"per-case hack vs universal rule"的关键二分法

**这是好的评议互动模式**:每轮用户挑战都暴露我前一轮的盲点。最终推荐是用户追问驱动的,不是我单方面推导的。

---

## 5. 后续文档

- `详细设计-按图架构与hack清单.md`:Universal Taint Union + Graph Engine 的具体实现设计 + hack 清单
- 已有 `深审-纯图架构重定位(从按值追踪到血缘图引擎).md`:v1(已被本评审结论取代)
- 已有 `深审-纯图架构重定位(静态图,DESIGN-STATIC思路).md`:v2(已被本评审结论取代)

---

## 6. 签字

- 评议方:提议方 + 评议人
- 日期:2026-08-06
- 状态:**结题,转入实施阶段**


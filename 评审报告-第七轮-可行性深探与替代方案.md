# 评审报告（第七轮）：可行性深探、改进意见与替代方案全景

> 评审对象：《Web数据血缘-最优设计与规范提案.md》+《API-DOM绑定图-深入浅出.md》
> 性质：**换一把尺子**。前六轮做的是逐条规格的对抗性审查（9 个 P0 全修、残留 2 P1 + 7 P2，第六轮裁决"可开工"）。本轮回答三个方向级问题：①方案在真实工程环境里**是否可行**（性能宣称、机制风险、生态兼容）；②还有哪些前六轮没覆盖的**改进意见**；③是否存在**更好的替代/参考方案**。
> 评审日期：2026-07-30
> 承袭说明：第六轮残留的 2 个 P1（组件边界控制边断裂、交集约束与诚实原则矛盾）+ 7 个 P2 **仍然有效**，本报告不重复，修订时必须一并处理。
> 资料说明：本次评审环境联网检索不可用，§四替代方案部分基于评审者已知的学术文献与工业界系统写成（Timelapse/Replay.io/CodeQL/OpenLineage 等均为真实存在的系统），但具体数字与现状描述**引用前请自行复核**。

---

## 〇、总裁决

**方案可行，方向无替代。** 在"常驻 dev 工具、低开销、值级精度、框架无关"四个约束的交集里，WDPP 按值追踪是目前唯一自洽的设计——前六轮已证明它在规格层面收敛，本轮的结论是它在工程层面也站得住，但有三件事文档目前**宣称过度**：

1. **性能数字是估算不是测量**。"L0≈1.05× / L1~3×"没有测量来源，且忽略了 L0 的两个真实成本大头（大响应盖戳、长 session 内存）和一个引擎级风险（patch `CharacterData.prototype` 热路径去优化）。可行≠已验证，这三个数字必须由参考实现背书后才能留在文档里。
2. **DOM 拦截的侵入性被低估**。文档花了大量篇幅对齐 WebIDL 规格（做对了），但完全没讨论"还有一条零侵入通道"：**MutationObserver 可以观察全部文本/属性变化而不 patch 任何 DOM API**。由此可构造一个 L0-passive 变体，把 DOM 侧侵入性降为零——这是本轮最重要的架构补充。
3. **patch 生态互操作完全缺失**。fetch/XHR 是全行业 patch 密度最高的全局函数（Sentry、Datadog、OTel、MSW、各 analytics SDK 全在包装它）。WDPP 作为"后来者 patch"如何共存，规范一个字没写——这在真实 app 里会是第一周内必撞的墙。

替代方案方面（§四）：**没有发现"更好的方案"**——录放式全知调试（Replay.io/Timelapse）精度完美但形态与成本完全不同；静态数据流分析（CodeQL/Semgrep）零运行时开销但答不了记录级与动态形状；两者对 WDPP 是**互补与威胁并存**：静态分析会侵蚀 L1 插桩的价值主张，文档应把 L1 重新定位为"静态不可达的动态部分"。标准化路径应学 OpenLineage 的教训：**标准的成功靠发射器集成先行，不靠 spec 先行**。

---

## 一、可行性深审

### 1.1 机制可行性：逐拦截点核验

| 拦截点 | 业界先例 | 可行性裁决 |
|--------|----------|-----------|
| fetch / XHR | Sentry、Datadog RUM、OTel、MSW 全在生产环境 patch，久经考验 | ✅ 可行，但互操作是欠账（§二 I-1） |
| `JSON.parse` 全局重盖 | 先例少（安全产品做过）。纯函数替换无 receiver 语义问题，风险低 | ✅ 可行 |
| storage / postMessage / SSE / WebSocket | RUM 工具部分覆盖 | ✅ 可行 |
| `Element.setAttribute` / `innerHTML` / style | 安全监控产品有先例 | ✅ 可行 |
| **`CharacterData.prototype.data` / `nodeValue` setter** | **几乎没有生产先例**。这是 React/Vue 写文本的主通道、全应用最热路径，给它挂 JS accessor 意味着每次文本写入都从引擎快路径掉到 JS 包装 | ⚠️ **最大未验证风险**（见 1.2 R-2） |
| `HTMLInputElement.prototype.value` 等 property 表 | 无先例（React 官方都不推荐） | ⚠️ 同上，且 property 表是 React 内部实现细节（见 1.2 R-3） |

**结论**：I/O 侧（producer）整条链路有成熟先例，可行性无争议；DOM 侧（sink）的文本主通道拦截是全方案**唯一没有工业先例背书的机制**，它的可行性不能靠推理确立，只能靠参考实现实测。这正好印证第六轮的建议——下一步是写 L0 参考实现，而**第一个该测的就是大列表文本渲染场景的开销**。

### 1.2 新增机制风险（前六轮未覆盖）

**R-1 多重 patch 生态互操作（P1）。** fetch 是被 patch 最多的全局函数。WDPP 加载时，页面上可能已有 Sentry 的 wrapper；WDPP 加载后，MSW 可能又包一层。问题有三面：①wrap 顺序决定 sourceId 归因谁看得到（WDPP 在内层则看不到被外层拦截/改写的请求，MSW mock 场景下 fetch 根本不到网络层——但这反而对归因无害，响应体照常解析）；②别的工具**replace 而非 wrap** 时 WDPP 被静默摘除；③幂等——热更新/重复注入时不能双重盖戳。**修法**：规范加"互操作"一节：wrap-don't-replace、Symbol 标记幂等、定期自检 wrapper 链完整性（检测被摘除时告警降级而非静默失明）。

**R-2 `CharacterData.prototype` patch 的引擎级风险（P1，需实测）。** 给原型 `defineProperty` accessor 后：①V8/JSC 对走该 setter 的代码脱离内联快路径，大列表渲染（万级文本节点写入）可能放大开销；②任何用 `getOwnPropertyDescriptor` 做特征检测的库行为改变；③`Object.freeze` 了原型的环境（SES/Agoric lockdown shim、Locker/LWS 之外的冻结沙箱）静默失效。**这是 1.05× 宣称里没被计入的成本项。修法**：见 §二 I-2（L0-passive 变体——这同时是该风险的逃生通道）。

**R-3 React property 表是脆弱依赖（P2）。** §4.3 写"按 React DOM property 表逐接口 patch"——该表是 React 内部实现细节，版本间可变，且 Vue/Svelte 各有自己的写入习惯。**修法**：规范层改为"按 WebIDL 接口全量对齐"（标准接口是稳定地基），把 React property 表降为参考实现的性能优化注记。

**R-4 canonical URL 的 PII 问题（P2）。** sourceId = 原始 URL 定稿后，`/users/ada@example.com` 这类 URL 会把 PII 写进每条血缘记录，再经 `__wdpp__` 暴露。capability 模型（§4.4）管住了"谁能查"，但没说"记录本身该脱敏"。**修法**：Producer 规范加可配置 redact 钩子，与 routeTemplate 注解协同。

### 1.3 性能宣称审计

| 宣称 | 审计结论 |
|------|----------|
| L0 ≈ 1.05× | **无测量来源。** 合理但乐观：拦截本身便宜（每次 DOM 写多一次 Map.get），但 ①stampOrigin 递归遍历每个响应是 O(n) 同步工作，大列表接口冷启动可达数百 ms（第六轮 P2-6 已提分片，但那是修法不是测量）；②R-2 的热路径去优化未计入；③宣称只有 CPU，**内存宣称缺失**——长 session Map 稳态规模才是用户可感的指标。建议改为目标式表述："L0 目标 <1.2× CPU、中型 app 稳态内存增量 <50MB"，进 go/no-go |
| L1 ~ 3× | 可辩护的经验中值：Jalangi 系 26-96× 是"每条运算插桩 + 按位置影子内存"的税；L1 只插桩产生新值的运算（call/binary/template），理论轻一个量级，2-5× 是合理区间。但 "~5% 插桩面"（条件槽位侧车）与 L1 总开销的叠加关系文档没算清——侧车的 SM 双写（第六轮 P2-2）本身就是开销项 |
| 召回 ~60% / ~90% | **假设值**，依赖 app 数据形状（字符串占比、变换密度）。文档已在 conformance 里标 informative（做对了），但 §3.1 表格里的数字没有任何标注，读者会当承诺。**修法**：标注"参考实现在中型 app 真值集上的实测目标"，一致性套件背书前不得引用 |

**审计总结论**：三个数字的**方向**都可信（L0 轻、L1 数倍、召回字符串高数字低），但都还是"应该如此"而非"测得如此"。这不影响开工——影响的是文档措辞：把宣称改成目标，把目标挂到 go/no-go。

### 1.4 环境边界补遗

文档已写 Locker/LWS（✅）、CSP（✅）。补充三个：

- **SES / Agoric lockdown**：冻结全部 Intrinsics，比 Locker 更常见（加密钱包生态大量启用）。与 Locker 同列"做不到"。
- **React Native / 非 DOM 渲染目标**：方案是 Web 专精，无可非议，但"框架无关"的宣称边界是"Web 平台框架无关"，文档宜写一句防误读。
- **生产环境定位**：方案本质是 dev 工具（capability/opt-in 也这么说），但全文没一句明确的"WDPP-L0/L1 定位 dev-only，不建议生产常驻"。写上这句能挡掉安全评审和性能评审的各一半质疑。

---

## 二、改进意见（第六轮残留之外的新增）

**I-1 互操作一节（对应 R-1，P1）。** 规范加 §4.2b "Patch 互操作"：wrap-don't-replace、`Symbol.for('wdpp.wrapped')` 幂等标记、wrapper 链自检、被摘除时 Query API 返回降级状态而非静默无数据。这是真实 app 第一周必撞的墙，spec 先行成本极低。

**I-2 L0-passive 变体：MutationObserver 通道（对应 R-2，本轮最重要建议）。** 文档默认 sink 必须 patch DOM API，但 **MutationObserver（`characterData:true, attributes:true, childList:true, subtree:true`）能观察全部文本与属性变化，一行 patch 都不用**。由此构造 L0 的两个 profile：

| | L0-active（现行方案） | **L0-passive（新增）** |
|---|---|---|
| DOM 侧侵入 | patch 6 类接口 | **零 patch** |
| 归因时序 | 同步（写入瞬间） | microtask 批量延迟 |
| 归因粒度 | 知道是哪次 setter 调用 | 只知道"变成了这个值" |
| 引擎风险 | R-2 全部 | **无** |
| 冻结沙箱 | 全灭 | **DOM 侧存活**（I/O 侧仍需 patch fetch，Locker 下仍灭） |

价值有三：①R-2 的**逃生通道**——active 实测翻车时有退路；②**最保守环境的首采用形态**（"只加一个只读观察者"比"我 patch 了你的 DOM"好卖得多）；③恰好契合文档已有的设计——边清理本来就走 MutationObserver，passive 是把同一机制升为主通道，实现复用度高。**建议写进 §3.1 分层表（L0 拆两个 profile），规范层作为 L0 的可选一致性形态。**

**I-3 交付形态：浏览器扩展优先于裸 script（P2，但影响采纳曲线）。** 现行形态是"加个脚本"。改为"DevTools 扩展（MAIN world 注入 + DevTools 面板）"一举解决四个问题：①opt-in/capability 天然成立（装扩展即授权）；②不侵入宿主 app 的构建与 HTML；③发现性（点选反查的 UI 需要面板承载，script 形态做不了核心交互）；④与阶段 2"DevTools 采纳"的路线自然衔接。script 形态保留为 CI/一致性套件的注入方式。

**I-4 基准先行（P2）。** go/no-go 的三个指标（冷启动、帧率、内存曲线）需要先有一个可复现的基准 harness：一个中型 React app fixture（大列表 + 条件渲染 + 变换链 + 长 session 脚本），否则"L0 应近无感"无法裁决。建议 P-1 阶段把 fixture 与 golden case 同生。

**I-5 召回宣称标注（对应 1.3，措辞级）。** §3.1 表格的 ~60%/~90% 加脚注"参考实现实测目标，见 §4.6 informative 注记"。

**I-6 记录级血缘与批量接口的交互（P2）。** `entityKey` 对 `/users/1`、`/users/2` 单记录接口成立，但对 `POST /graphql` 批量返回 200 条记录、或 SSE 增量推送的场景，entityKey 的生成规则（数组 index 还是 id 字段？id 字段叫什么名各 API 不同）规范没写。建议：entityKey 生成规则降为 informative（启发式：优先 `id`/`_id`/`key` 字段，回退数组 index），不进 normative。

---

## 三、前六轮残留问题确认（不展开，修订时一并处理）

- **P1-1**（第六轮）：controlIndex 桥在组件边界断裂——按"诚实降级"处理，写明跨组件控制边属 L2。
- **P1-2**（第六轮）：传播侧交集约束与诚实原则矛盾——交集为空时应并集 + collision，不是 CUT。
- **P2×7**（第六轮）：pseudocode 控制边查询顺序、SM 双写、controlIndex 代际、拼接规则两条、GraphQL sourceId、大响应分片、一句话总结编辑矛盾。

---

## 四、替代方案全景：有没有更好的路？

以下五条替代路线逐一与 WDPP 对比，并回答"是否更好"。

### A. 录放式全知调试（Timelapse 学术原型 / Replay.io 商业产品）

**路线**：录制整段执行（DOM、网络、每次 JS 求值），事后在回放中检查任意时刻的任意值——provenance 问题变成"回放里向前追溯"的问题，精度天然完美（真按位置、真 per-instance，无碰撞）。

**对比 WDPP**：

| 维度 | 录放式 | WDPP |
|------|--------|------|
| 精度 | 完美（无碰撞、无 approx） | 字符串高、数字降格、布尔侧车 |
| 形态 | **事后会话**（录制→分享→回放分析） | **常驻面板**（指着屏幕即时问） |
| 成本 | 需改造浏览器/录制基础设施；录制开销与存储 | 一个 runtime + 可选构建插件 |
| 覆盖 | 录到的会话 | 当前 live session |

**裁决：不是替代，是形态不同的互补。** 用户问题"屏幕上这个是哪个字段"的交互形态是**即时的、live 的**——录放式要先录再查，答不了"我现在指着的这个"。且 Replay.io 的商业化坎坷（基础设施重、用户为调试体验付费意愿有限）是"重型换精度"路线的市场警示——反过来佐证 WDPP "L0 加个脚本"的低门槛形态是对的战略选择。**值得借鉴**：WDPP 的 Query API（§4.4）若设计得当，未来可作为录放系统的 provenance 查询层——协议的前瞻性可以留这个口。

### B. 静态数据流分析（CodeQL / Semgrep taint / TS 语言服务）

**路线**：不跑程序。从 JSX/模板出发，经组件 props、hook 链、store，回溯到 fetch/axios 调用与响应类型（TypeScript 类型给出字段路径）。CodeQL/Semgrep 的 taint 模式已有成熟的 source→sink 建模能力（业界主要用于 DOM-XSS 检测，方向与本提案正好镜像）。

**对比 WDPP**：

| 维度 | 静态分析 | WDPP |
|------|----------|------|
| 运行时开销 | **零** | L0 低 / L1 数倍 |
| 字段路径精度 | 高（有类型时精确到字段） | 高（值匹配） |
| **记录级**（"这行来自第几条记录"） | ❌ 原理性答不了 | ✅ entityKey |
| 动态数据形状（运行时才知道的字段名、条件分支取哪支） | ❌ 弱 | ✅ |
| 框架间接层（render props、高阶组件、store 穿透） | 精度衰减快 | 无感（只看值） |
| 消费形态 | 离线报告 / IDE | 常驻面板 + Query API |

**裁决：这是对 WDPP 最大的真实威胁，尤其对 L1。** "AI agent 消费者"场景（§1.1 点名）中，agent 本来就能读代码——静态分析 + 类型信息可能覆盖其 70% 的问题，成本为零。但它**原理性答不了两件事**：记录级血缘（哪行来自哪条记录）和动态形状（运行时才知道的值与分支）。**建议文档修订**：把 L1/WDPP 的价值主张从"答字段来源"收窄并强化为"答静态分析原理性答不了的部分：运行时实例、记录级、变换后的动态值"——与静态分析的关系从竞争改写成**分层互补**（静态出结构候选边，运行时出实例边），并点名这是最可能的混合终态。

### C. 安全污点学术谱系（Kudzu / JSFlow / DOMinator / Jalangi 系）

文档 §1.3 已正确收录该谱系并分析了其未标准化的原因（开销宣称不实、精度不敢诚实、只有合规拉动）。本轮只补一条裁决：**这些系统之死都不是算法之死，而是交付形态之死**——每个都要求换浏览器、换解释器或全量重写执行。WDPP 的"L0 纯 runtime、框架无关、分层采纳"正是对着这个死因开的药方，方向已被历史反证为正确。无需改文档，此条为信心背书。

### D. 数据工程血缘标准（OpenLineage + Marquez / W3C PROV）

**对 WDPP 标准化路径的三条启示**：

1. **OpenLineage 的成功不是因为 spec 写得好，而是因为发射器（emitter）集成先行**——Airflow、dbt、Spark 内置了 lineage 事件发射，标准是被集成拖成事实的。对应到 WDPP：阶段 2"催生第二个实现"太抽象，应具体化为**"给一个主流对象写 emitter PR"**——候选：TanStack Query 的 devtools 钩子、Vite 插件、Chrome DevTools 扩展面板。一个合入主流项目的 PR 胜过十版 spec。
2. **治理模式**：OpenLineage 走 LF AI & Data 基金会托管（TSC 治理）而非 W3C——对"社区工作组"（§5.3）是现成模板，也再次印证删 TC39、延后 W3C 的判断。
3. **W3C PROV 的 Entity-Activity-Agent 模型**可作为 ProvenanceRecord 的上位语义映射（值=Entity、变换=Activity、API 端点=Agent），写一句 informative 映射能堵住"为什么不复用 PROV"的评审问题——但**不要**引入 PROV 的术语体系，内核极小（一个 map）是立身之本。

### E. 平台原生路径现实检查（CDP / WICG）

CDP 无 provenance 域（文档已确认）。补一个现实性注记：CDP 加域需要 Chromium 团队的实现兴趣，历史上新域的诞生（如 FedCM、Attribution Reporting）都由浏览器厂商的内部需求驱动。**比 WICG 更现实的中间步**是：DevTools 扩展 API + 扩展面板（对应 I-3）跑出用户量，用"已有 N 个开发者周活"作为与浏览器厂商对话的筹码——这与 OpenLineage 教训同构：**采用拉动标准化，不是标准化拉动采用**。文档三阶段的方向对，建议在阶段 2 补这句策略。

### F. 替代方案总结论

```
                    精度        成本        形态          答记录级?
录放式(Replay)      完美        重          事后会话       ✅
静态分析(CodeQL系)  高(结构)    零运行时     离线/IDE       ❌(原理性)
WDPP(本提案)        高(值级)    低~中        常驻即时       ✅
```

**没有任何替代方案在"常驻、低门槛、值级、框架无关"的交集上优于 WDPP——方案成立，无更好替代。** 但两条边界必须在文档里写清楚：①对静态分析可答的部分（结构边），WDPP 不应宣称优势而应宣称互补；②对录放式覆盖的场景（事后深度分析），WDPP 的 Query API 保留成为其查询层的前瞻性。

---

## 五、修订清单（本轮新增，与前六轮残留合并执行）

| # | 级别 | 项 | 落点 |
|---|------|-----|------|
| 1 | P1 | patch 互操作一节（wrap/幂等/自检/降级告警） | WDPP 新增 §4.2b |
| 2 | P1 | L0-passive 变体（MutationObserver 通道，零 DOM patch） | WDPP §3.1 分层表 + §4.3；深入浅出 §九 |
| 3 | P1 | 性能宣称改目标式表述（<1.2× CPU / <50MB 内存 / 召回数字加脚注） | WDPP §3.1、§七 go/no-go |
| 4 | P2 | property 表改"WebIDL 全量对齐 + React 表降为优化注记" | WDPP §4.3 |
| 5 | P2 | sourceId PII redact 钩子 | WDPP §4.2 |
| 6 | P2 | 交付形态加"DevTools 扩展为首发形态" | WDPP §七 落地路线 |
| 7 | P2 | 基准 fixture 与 golden case 同生（P-1 阶段） | WDPP §七 |
| 8 | P2 | entityKey 生成规则降 informative（id 启发式 + index 回退） | WDPP §4.1 |
| 9 | P2 | 环境边界补 SES/lockdown、"Web 平台"限定、dev-only 定位 | 两文档边界章 |
| 10 | P2 | L1 价值主张改写为"静态分析原理性答不了的部分"；混合终态一段 | WDPP §3.3 + §六对比表 |
| 11 | P2 | 阶段 2 具体化为"emitter PR 策略" + OpenLineage/PROV 映射注记 | WDPP §五 |

---

## 六、最终判断

1. **可行性：通过，附条件。** 机制可行（I/O 侧有先例背书，DOM 侧文本主通道是唯一无先例风险点）；方向无更好替代；性能与召回数字是待验证的目标而非既成事实。条件只有一个：**L0 参考实现的第一个基准必须是大列表文本渲染 + 大响应盖戳场景**——这两个点是整个性能宣称的命门，也是 R-2 风险的试金石。
2. **改进意见集中在"宣称管理"与"生态现实"**：把估算改目标（1.3）、把互操作写进 spec（I-1）、把零侵入变体纳入分层（I-2）。这三件事都不动摇架构，都是"让宣称配得上实现"的诚实工程。
3. **替代方案的净效应是增强信心 + 收窄宣称**：没有更好的方案；但静态分析的存在要求文档把价值主张收窄到"动态、实例级、记录级"——这恰恰是按值追踪最强的部分，收窄是双赢。
4. **下一步建议不变且更紧迫**：第六轮已说"写 L0 参考实现优于第七轮纸面评审"。本轮补完后，**不应再有第八轮纯文档评审**——下一轮评审对象必须是代码与基准数字。
